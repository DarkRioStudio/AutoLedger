# v1.6.0 版本计划

更新日期：2026-06-24
文档状态：第一版推进中 / Active
内部开发线：v1.6.0
App Store 对外版本：1.5.0
承接版本：v1.5.1 release candidate
关联文档：`versions/v1.5.1-plan.md`、`versions/v1.5.1-regression-baseline.md`、`docs/recognition-learning-cache-design.md`

## 1. 版本定位

`v1.6.0` 承接 `v1.5.1` 已完成的 iPhone / iPad / Apple Watch / Mac Catalyst 发布基线，不在本版本重做主线架构。

本版本优先处理四类增量：

1. 订阅管理补强
2. 外部辅助识别后的商户 / 分类 / 订阅倾向学习缓存
3. tvOS / visionOS 展示版进入可运行版本
4. Mac 与全平台体验继续打磨

多账本继续顺延到后续版本，不进入 `v1.6.0` 实现范围。

## 2. 设计原则

- 继续保持本地优先，正式账单数据仍以本机和用户 iCloud 为主。
- 外部辅助识别继续默认关闭，由用户手动配置 API key 后启用。
- 外部模型不得决定金额和是否自动保存，只能辅助商户、分类、订阅倾向等候选信息。
- 识别缓存只缓存用户确认过的低风险结果，不缓存整张 OCR 到完整账单的强绑定结果。
- 新增订阅能力必须用户确认后写入订阅管理，不做静默自动创建。
- tvOS / visionOS 首版以只读展示为主，不承载导入、编辑、清洗、多账本等复杂流程。

## 3. 核心范围

### 3.1 订阅管理补强

目标：让订阅管理从“识别和展示”升级为可维护的基础功能。

计划能力：

- 手动新增订阅
- 编辑订阅
- 暂停 / 取消订阅
- 从账单详情一键创建订阅
- 从识别结果提示“这可能是订阅”
- 下次扣费日期、周期、金额、服务名可编辑
- 订阅列表按即将扣费排序
- 保留提醒和统计入口

边界：

- 不自动把模型判断结果写入订阅
- 不自动扣减、预测或修改正式账单
- 不做多账本订阅归属

### 3.2 AI 订阅判断

目标：在外部辅助识别已经可用的基础上，让模型辅助判断当前账单是否可能是订阅项目。

计划扩展字段：

- `subscriptionHint.isSubscription`
- `subscriptionHint.serviceName`
- `subscriptionHint.billingCycle`
- `subscriptionHint.confidence`

使用策略：

- 高置信时在入账后提示用户是否加入订阅管理
- 中低置信只进入调试记录或候选提示
- 用户确认后才创建订阅
- 调试记录记录是否走外部模型、模型耗时、订阅判断结果和置信度

### 3.3 识别学习缓存

目标：降低重复商户识别成本，提高常见消费场景稳定性。

设计文档：`docs/recognition-learning-cache-design.md`

允许缓存：

- 用户确认过的商户别名
- 商户默认分类
- 商户是否常见订阅服务
- 外部模型返回的商户候选摘要
- 同一脱敏 OCR hash 的短期外部辅助结果

禁止缓存：

- 完整原始 OCR
- 原始截图
- 完整支付截图文本
- 金额、时间、订单号、卡号等会随交易变化的字段
- “整张 OCR 直接复用完整账单”的结果

建议策略：

- merchant-level cache 优先
- OCR hash cache 只作为短期性能优化，默认 TTL 较短
- 缓存命中只能作为候选增强，不得覆盖金额和日期
- 用户编辑商户 / 分类后更新学习结果

### 3.4 tvOS 只读看板

目标：完成第一版可运行的 Apple TV 只读账本展示。

首版能力：

- 本月支出总览
- 分类占比
- 最近消费趋势
- 年度 / 月度摘要
- 家庭大屏看板

边界：

- 只读
- 单一正式账本
- 不做导入 / 编辑 / 清洗
- 不做多账本切换

### 3.5 visionOS 展示版

目标：完成第一版可运行的 visionOS 空间展示体验。

首版能力：

- 月度支出空间看板
- 分类支出卡片漂浮展示
- 年度消费时间线墙
- 最近账单悬浮列表

边界：

- 首版以 SwiftUI WindowGroup 为主
- immersive space 不作为硬门槛
- 只读
- 单一正式账本

### 3.6 Mac 与全平台体验打磨

目标：继续消化 v1.5.1 发布后发现的桌面端和多端细节问题。

候选项：

- Mac 默认窗口与表格布局继续优化
- Mac 导入 / 导出 / 批量编辑 smoke 自动化记录
- iPad 工作台二级页面切换稳定性继续观察
- Watch App 与 Widget 数据刷新链路继续复测
- iCloud 同步性能继续观察 P50 / P90
- 截图管线继续覆盖全平台素材

## 4. GOAL 队列

| GOAL | 状态 | 目标 | 验收 |
|---|---|---|---|
| GOAL-1700 | DONE | v1.6.0 计划冻结与范围确认 | 本文档、README Roadmap、设置页后续计划对齐；工程 `MARKETING_VERSION` 推进到 App Store 1.5.0 |
| GOAL-1710 | DONE | 订阅管理基础 CRUD | 可手动新增、编辑、暂停 / 取消订阅 |
| GOAL-1715 | DONE | 账单转订阅 | 账单详情可创建订阅，需用户确认 |
| GOAL-1720 | DONE | 外部辅助识别增加订阅判断 | 模型返回订阅 hint，调试记录可见，不自动创建 |
| GOAL-1730 | DONE | 商户 / 分类 / 订阅倾向学习缓存设计 | 不缓存完整 OCR，不影响金额和日期 |
| GOAL-1735 | DONE | 外部辅助识别短期结果缓存 | 同一脱敏 OCR hash 短期复用，带 TTL 和回退 |
| GOAL-1740 | DONE | tvOS 只读看板第一版 | tvOS target 可构建运行，展示月度只读数据 |
| GOAL-1750 | DONE | visionOS 展示版第一版 | visionOS target 可构建运行，展示空间看板雏形 |
| GOAL-1755 | DONE | tvOS / visionOS 只读看板同步入口 | 主 App 发布 CloudKit dashboard snapshot，大屏端优先拉取只读快照 |
| GOAL-1760 | DONE | Mac / iPad / Watch 多端 polish | 已知 UI、刷新和同步编辑问题回归通过 |
| GOAL-1770 | IN_PROGRESS | v1.6.0 发布资产与 smoke | 本地 smoke 与全平台截图管线完成；Xcode Cloud、TestFlight、ASC 新平台上传仍需人工门禁 |

## 5. 测试计划

- `git diff --check`
- `bash scripts/run_offline_regression.sh`
- `bash scripts/run_golden_regression.sh`
- 主 App iOS generic build
- Mac Catalyst build
- tvOS target build
- visionOS target build
- iPhone / iPad / Watch / Mac / tvOS / visionOS smoke
- 外部辅助识别 provider 真机测试
- iCloud 同步双设备 smoke
- 订阅管理 CRUD 手工 smoke

## 6. 发布边界

`v1.6.0` 当前作为 App Store 1.5.0 的开发线推进。最终是否提交 App Store 发布，取决于以下条件：

- 订阅管理基础功能没有账单数据风险
- AI 订阅判断只作为用户确认提示
- 缓存不保存原始 OCR / 截图 / 敏感字段
- tvOS / visionOS 首版如果未达到体验标准，可继续作为内部预览，不阻塞 iPhone / iPad / Watch / Mac 主发布
- 多账本仍然不作为本版本 blocker

## 7. 当前结论

`v1.6.0` 已切换为 App Store 1.5.0 的下一条开发线，优先补强订阅管理和识别学习能力，同时推进 tvOS / visionOS 展示版。

本版本不改变 `v1.5.1` 当前提审判断；`v1.5.1` 可继续按当前 release candidate 进入 ASC 审核。

## 8. 执行记录

### 8.1 GOAL-1700 v1.6.0 第一版推进与 ASC 版本号更新（2026-06-19）

状态：DONE。

本轮将 `v1.6.0` 从规划草稿推进为 App Store 1.5.0 开发线第一版，并把工程内所有 target 的 `MARKETING_VERSION` 从 `1.4.0` 提升到 `1.5.0`。本轮不实现新功能、不修改 Bundle ID、DEVELOPMENT_TEAM、App Group、iCloud Container、entitlements 或 Xcode Cloud 脚本。

改动范围：

- `AutoLedger/AutoLedger.xcodeproj/project.pbxproj`：全 target `MARKETING_VERSION = 1.5.0`。
- `README.md`、`README.en.md`：Roadmap 中 `v1.6.0` 对应 App Store 版本固定为 `1.5.0`，状态改为第一版推进中。
- `versions/v1.6.0-plan.md`：文档状态从 Draft 调整为 Active，`GOAL-1700` 标记完成。
- `CHANGELOG.md`、`process/iteration-log.md`：记录版本推进。
- `AutoLedger/AutoLedger/App/LedgerStore.swift`：版本读取兜底值同步为 `1.5.0`。

验证：

- `git diff --check`
- `xcodebuild -list -workspace AutoLedger/AutoLedger.xcworkspace`

下一步：

进入 `GOAL-1710`，优先做订阅管理基础 CRUD；tvOS / visionOS 继续按计划作为本开发线后续阶段推进。

### 8.2 GOAL-1710 订阅管理基础 CRUD（2026-06-19）

状态：DONE。

本轮将订阅管理从“自动识别 + 展示”补强为可维护的基础 CRUD。用户现在可以在订阅管理页手动新增订阅、编辑订阅、暂停 / 恢复订阅、取消订阅和删除订阅；暂停或取消后的订阅不再进入即将扣费、月度 / 年度摘要和本地提醒调度。

改动范围：

- `AutoLedgerCore`：`Subscription` 新增 `SubscriptionStatus`，支持 `active / paused / canceled`，旧备份 / 旧同步 payload 缺少 `status` 时默认按 `active` 兼容。
- `SQLiteTransactionStore`：`subscriptions` 表新增 `status` 字段和轻量迁移，保存、更新、读取订阅时保留状态。
- `LedgerStore`：新增手动创建订阅入口，订阅状态更新复用正式配置变更链路并触发备份 / iCloud 配置推送。
- `SubscriptionListView`：新增手动添加入口、状态 Picker、暂停 / 恢复 / 取消按钮和 context menu；非活跃订阅降权展示。
- `NotificationService`、`InboxView`：暂停 / 取消订阅不再生成本地提醒，也不进入首页即将扣费提示。
- 三语本地化：补齐新增、暂停、恢复、取消、状态等订阅管理文案。
- `scripts/OfflineRegression.swift`：补充订阅状态 Codable 兼容、SQLite 持久化、备份恢复回归。

未改动范围：

- 未实现账单详情一键创建订阅，继续留在 `GOAL-1715`。
- 未实现外部模型订阅判断，继续留在 `GOAL-1720`。
- 未引入多账本订阅归属。
- 未修改 Bundle ID、DEVELOPMENT_TEAM、App Group、iCloud Container、entitlements、Xcode Cloud 脚本或 App Store Connect 配置。

验证：

- `git diff --check`
- 三语 `Localizable.strings` plist lint
- `bash scripts/run_offline_regression.sh`
- `xcodebuild -workspace AutoLedger/AutoLedger.xcworkspace -scheme AutoLedger -configuration Debug -destination 'generic/platform=iOS' build`

手工 smoke 建议：

- 在 iPhone / iPad / Mac Catalyst 订阅管理页手动新增订阅。
- 编辑商户、计划名、周期、金额、下次扣费日、状态。
- 暂停 / 恢复 / 取消订阅后确认摘要和即将扣费列表同步变化。
- 删除订阅后确认列表、提醒和 iCloud 配置同步没有残留。

下一步：

进入 `GOAL-1715`，把账单详情中的“创建订阅”作为显式确认链路补上；随后再推进 `GOAL-1720` 外部辅助识别订阅 hint。

### 8.3 GOAL-1715 账单详情创建订阅（2026-06-19）

状态：DONE。

本轮在账单编辑 / 详情页补齐“从这笔账单创建订阅”显式确认链路。用户从已有账单进入编辑页后，可以打开订阅确认页，系统预填当前账单的商户、金额和扣费时间；用户确认周期、金额和下次扣费时间后，订阅会写入订阅管理，不会自动修改当前账单。

改动范围：

- `AutoLedgerCore.Subscription`：新增 `draft(from:)`，从正式账单生成订阅草稿，默认按月付、进行中状态、账单时间作为最近扣费时间。
- `TransactionEditorView`：已有账单模式下新增订阅区和创建订阅按钮；新增订阅确认 sheet，保存后走 `LedgerStore.createSubscription`。
- 三语本地化：补齐账单转订阅入口、确认说明、重复提示和创建成功提示。
- `scripts/OfflineRegression.swift`：新增订阅草稿回归，覆盖商户、金额、周期、状态、最近扣费和下次扣费计算。

未改动范围：

- 新增账单模式不显示该入口，避免用户在账单尚未保存时创建订阅。
- 未实现外部模型订阅 hint，继续留在 `GOAL-1720`。
- 未实现识别学习缓存或订阅倾向缓存，继续留在 `GOAL-1730 / 1735`。
- 未修改 SQLite schema、Bundle ID、DEVELOPMENT_TEAM、App Group、iCloud Container、entitlements、Xcode Cloud 脚本或 App Store Connect 配置。

验证：

- `git diff --check`
- 三语 `Localizable.strings` plist lint
- `bash scripts/run_offline_regression.sh`
- `xcodebuild -workspace AutoLedger/AutoLedger.xcworkspace -scheme AutoLedger -configuration Debug -destination 'generic/platform=iOS' build`

手工 smoke 建议：

- 在 iPhone / iPad / Mac Catalyst 账本中打开一笔已有账单，点击“从这笔账单创建订阅”。
- 修改周期、金额、下次扣费时间后保存，确认订阅管理页出现新订阅。
- 对已有同商户同周期订阅的账单再创建一次，确认重复提示可见且不会阻断用户显式新增。

下一步：

进入 `GOAL-1720`，在外部辅助识别结果里增加订阅 hint，但仍保持“只提示、用户确认后才写入订阅管理”的边界。

### 8.4 GOAL-1720 外部辅助识别订阅 hint（2026-06-19）

状态：DONE。

本轮把外部辅助识别的返回结构扩展为同时包含商户候选、分类 hint 和订阅 hint。模型现在只返回订阅候选判断，不返回金额、订单号、卡号或个人信息；普通餐饮、购物、出行和超市消费在提示词中明确要求按非订阅处理。调试记录会展示 `subscriptionHint` 的 `isSubscription / serviceName / billingCycle / confidence`，方便真机观察外部模型是否判断为订阅。

改动范围：

- `ExternalReceiptAssistSuggestion`：新增 `subscriptionHint`，兼容 camelCase 与 snake_case 模型返回。
- `ExternalReceiptAssistSubscriptionHint`：新增订阅候选结构，支持 `isSubscription`、服务名、周期和置信度。
- `ExternalReceiptAssistOpenAICompatibleCodec`：提示词要求模型返回订阅 hint，并补充 snake_case 解码兼容。
- `SmartReceiptParser`：外部 Assist trace 中加入订阅 hint 摘要；当模型只返回订阅 hint、没有有效商户增强时，保留规则解析结果并仅记录 trace，不自动创建订阅。
- `scripts/OfflineRegression.swift`：新增订阅 hint 请求、camelCase 解码和 snake_case 解码回归。

未改动范围：

- 不自动写入订阅管理。
- 不在入账后弹出订阅创建提示。
- 不实现订阅倾向学习缓存或短期识别缓存。
- 不修改 SQLite schema、Bundle ID、DEVELOPMENT_TEAM、App Group、iCloud Container、entitlements、Xcode Cloud 脚本或 App Store Connect 配置。

验证：

- `git diff --check`
- `bash scripts/run_offline_regression.sh`
- `bash scripts/run_golden_regression.sh`
- `xcodebuild -workspace AutoLedger/AutoLedger.xcworkspace -scheme AutoLedger -configuration Debug -destination 'generic/platform=iOS' build`

手工 smoke 建议：

- 使用真实外部 Assist provider 识别一个非订阅消费，确认调试记录中的 `subscriptionHint.isSubscription=false`。
- 使用 App Store / 数字会员续费截图测试，确认调试记录中可见服务名、周期和置信度。
- 确认识别结果不会自动新增订阅；仍需用户从账单详情显式创建订阅。

下一步：

进入 `GOAL-1730`，设计商户 / 分类 / 订阅倾向学习缓存边界，继续保持不缓存原始 OCR 和敏感字段。

### 8.5 GOAL-1730 商户 / 分类 / 订阅倾向学习缓存设计（2026-06-19）

状态：DONE。

本轮冻结识别学习缓存边界，新增 `docs/recognition-learning-cache-design.md`。设计将缓存拆成三层：L1 用户确认规则、L2 商户级低风险画像、L3 短期脱敏 OCR hash 缓存。缓存只用于候选增强，不覆盖金额和日期，不自动保存账单，不自动创建订阅。

设计结论：

- L1 继续复用现有 `merchantAliases`、`categoryCorrections` 和用户确认后的 `subscriptions`。
- L2 未来可新增商户级低风险画像，只保存商户、别名、分类投票和订阅倾向统计。
- L3 短期脱敏 OCR hash 缓存顺延到 `GOAL-1735`，默认不进入 iCloud、不进入 JSON 备份。
- 金额、时间、订单号、卡号、手机号、地址、原始 OCR、截图和完整支付文本都不得进入学习缓存。
- 外部模型候选只有在用户最终保存或确认后，才能进入长期学习结果。

未改动范围：

- 本轮不实现运行时代码。
- 不新增 SQLite schema。
- 不改变 iCloud 同步 schema。
- 不修改 Bundle ID、DEVELOPMENT_TEAM、App Group、iCloud Container、entitlements、Xcode Cloud 脚本或 App Store Connect 配置。

验证：

- `git diff --check`

下一步：

进入 `GOAL-1735`，实现同一脱敏 OCR hash 的短期外部辅助结果缓存，带 TTL、provider 配置变化清理和严格隐私回归。

### 8.6 GOAL-1735 外部辅助识别短期结果缓存（2026-06-19）

状态：DONE。

本轮实现外部 Assist 的 L3 短期结果缓存。缓存以脱敏 OCR 文本的 SHA-256 指纹、来源、provider、model 和 endpoint 指纹共同组成 key；缓存值只保存模型候选结果，不保存原始 OCR、脱敏 OCR 原文、截图、金额、日期、订单号、卡号、手机号或地址。缓存默认 TTL 为 24 小时，最多保留 80 条，provider / endpoint / model / API key 变化时会主动清理。

改动范围：

- `ExternalReceiptAssistCache.swift`：新增 Core 层缓存记录和策略，覆盖 cache key、TTL、过期判定和 prune 逻辑。
- `ExternalReceiptAssistClient.swift`：在请求外部 provider 前读取短期缓存，网络成功后写入缓存；缓存仅存本机 `UserDefaults` 专用 key，不进入现有备份和 iCloud 配置快照。
- `scripts/OfflineRegression.swift`：新增缓存 key 隔离、TTL 过期、过期 prune 和隐私断言。
- `scripts/run_offline_regression.sh`：把新增 Core 缓存策略文件纳入离线编译清单。

未改动范围：

- 不缓存原始 OCR、截图、脱敏 OCR 原文或任何单笔交易敏感字段。
- 不把缓存结果用于金额、日期或自动保存。
- 不把短期缓存写入 SQLite 正式 schema、iCloud 配置快照或 JSON 备份。
- 不新增用户可见 UI。
- 不修改 Bundle ID、DEVELOPMENT_TEAM、App Group、iCloud Container、entitlements、Xcode Cloud 脚本或 App Store Connect 配置。

验证：

- `git diff --check`
- `bash scripts/run_offline_regression.sh`
- `xcodebuild -workspace AutoLedger/AutoLedger.xcworkspace -scheme AutoLedger -configuration Debug -destination 'generic/platform=iOS' build`

下一步：

进入 `GOAL-1740` 前，建议先根据优先级决定是否补一个高置信订阅 hint 的用户确认提示；如果继续按队列推进，则进入 tvOS 只读看板第一版。

### 8.7 GOAL-1740 tvOS 只读看板第一版（2026-06-19）

状态：DONE。

本轮把 `AutoLedgerTV` 从 Xcode 模板入口推进为第一版可运行的只读账本看板。tvOS target 现在显式链接 `AutoLedgerCore`，根页面提供 `总览 / 分类 / 趋势 / 摘要` 四个页面，复用 `MonthlySnapshot` 和 `TodaySpendingSummary` 计算月度总览、分类占比、最近 7 天趋势、近 6 个月趋势、年度累计、Top 商户和最近账单。

改动范围：

- `AutoLedger/AutoLedger.xcodeproj/project.pbxproj`：为 `AutoLedgerTV` target 增加本地 SPM 产品 `AutoLedgerCore` 依赖。
- `AutoLedger/AutoLedgerTV/ContentView.swift`：替换模板 `Hello, world!`，新增 tvOS dashboard、只读加载状态、隐私隐藏切换、刷新入口和四页展示 UI。
- `docs/tvos-implementation-assessment.md`：更新当前状态，记录第一版 UI 已落地与剩余数据入口限制。

产品边界：

- 只读展示正式账本。
- 不导入、不编辑、不删除、不清洗、不做多账本切换。
- 不展示候选账单、OCR 原文、截图、调试记录或最近删除。
- 没有数据时展示空状态，不注入样例数据。

保留限制：

- 本轮没有为 tvOS target 新增 iCloud / CloudKit capability、entitlements 或 CloudKit 只读拉取逻辑。
- Apple TV 当前读取的是 tvOS 本机账本 SQLite；跨设备同步后的 CloudKit dashboard snapshot / 只读正式账本拉取仍需后续单独 GOAL。
- tvOS 尚未接入 App Store Connect 平台素材、截图管线和真机 smoke。

验证：

- `git diff --check`
- `xcodebuild -workspace AutoLedger/AutoLedger.xcworkspace -scheme AutoLedgerTV -configuration Debug -destination 'generic/platform=tvOS' build`
- `xcodebuild -workspace AutoLedger/AutoLedger.xcworkspace -scheme AutoLedger -configuration Debug -destination 'generic/platform=iOS' build`

下一步：

进入 `GOAL-1750`，推进 visionOS 展示版第一版；tvOS 后续如果要具备真实跨设备展示能力，应单独实现 CloudKit 只读快照 / dashboard snapshot 数据入口。

### 8.8 账单编辑同步冲突修复（2026-06-19）

状态：DONE。

用户真机观察到两条地铁账单在编辑补全箭头后的终点站时，保存后修改不生效。本轮确认不处理南宁地铁系统自身计费异常，也不修改地铁识别规则，只修复“编辑后的本地账单被旧远端同步结果覆盖”的风险。

根因判断：

- `syncRevision` 是设备本地递增值，不是 CloudKit 全局单调版本。
- 旧策略优先比较 `syncRevision`，可能让“远端旧内容但 revision 更高”的记录覆盖刚保存的本地编辑。
- 手动编辑会更新本地 `updatedAt`，因此同一账单跨端合并应先比较 `updatedAt`，仅在时间相同后再用 `syncRevision` 作次级判断。

改动范围：

- `TransactionSyncConflictResolver`：同一账单冲突解析改为优先比较 `updatedAt`，再比较 `syncRevision`。
- `scripts/OfflineRegression.swift`：新增 resolver 层回归，覆盖 `地铁：琅西 →` 手动编辑为 `地铁：琅西→金湖广场` 后，旧远端高 revision 不应覆盖本地编辑。
- `scripts/OfflineRegression.swift`：新增 SQLite apply 层回归，确认 `applyRemoteSyncRecord` 会返回 `.keptLocal` 且账本仍保留编辑后的商户名。

未改动范围：

- 不修改地铁 / 南宁地铁解析规则。
- 不修改金额计算。
- 不修改 CloudKit record schema、SQLite schema、Bundle ID、DEVELOPMENT_TEAM、App Group、iCloud Container、entitlements、Xcode Cloud 脚本或 App Store Connect 配置。

验证：

- `bash scripts/run_offline_regression.sh`
- `bash scripts/run_golden_regression.sh`
- `xcodebuild -workspace AutoLedger/AutoLedger.xcworkspace -scheme AutoLedger -configuration Debug -destination 'generic/platform=iOS' build`

真机复测建议：

1. 在 iPhone 编辑一条地铁账单，将箭头后的终点站补全并保存。
2. 等待 iCloud 同步或在另一台设备手动刷新。
3. 确认 iPhone / iPad 都保留编辑后的商户名，且不会回退到 `地铁：起点 →`。

### 8.9 GOAL-1750 visionOS 展示版第一版（2026-06-19）

状态：DONE。

本轮把 `AutoLedgerVision` 从 Xcode 模板入口推进为第一版可运行的只读空间展示窗口。首版保持 `WindowGroup`，不启用 immersive space，不引入 Volume，不新增写入链路。

改动范围：

- `AutoLedger/AutoLedger.xcodeproj/project.pbxproj`：为 `AutoLedgerVision` target 增加本地 SPM 产品 `AutoLedgerCore` 依赖，继续保留 `RealityKitContent` 模板 package 但不让它承载首版产品 UI。
- `AutoLedger/AutoLedgerVision/ContentView.swift`：替换模板 `Model3D + Hello, world!`，新增月度空间看板、分类支出卡片、年度消费时间线墙、最近账单悬浮列表、隐私模式、刷新入口和 loading / empty / unavailable 状态。
- `docs/visionos-implementation-assessment.md`：更新当前实现状态与 smoke 结果。

产品边界：

- 只读展示正式账本。
- 单一正式账本，不做多账本切换。
- 不导入、不编辑、不删除、不清洗。
- 不读取候选账单、OCR 原文、截图原图、调试记录或最近删除。
- 不新增 CloudKit schema、SQLite schema 或 visionOS 专属统计口径。

数据口径：

- 复用 `SQLiteTransactionStore` 读取本机正式账本。
- 复用 `MonthlySnapshot` 和 `TodaySpendingSummary` 生成本月总览、今日支出、分类占比、Top 商户和最近账单。
- 年度时间线在 Vision target 内按最近 12 个月正式账本派生，只作为只读展示。

验证：

- `xcodebuild -workspace AutoLedger/AutoLedger.xcworkspace -scheme AutoLedgerVision -configuration Debug -destination 'generic/platform=visionOS' build`
- `xcodebuild -workspace AutoLedger/AutoLedger.xcworkspace -scheme AutoLedgerVision -configuration Debug -destination 'platform=visionOS Simulator,name=Apple Vision Pro' build`
- `xcrun simctl install` + `xcrun simctl launch top.darkrio326.AutoLedger.vision`
- `xcrun simctl io ... screenshot`，确认主窗口可打开，empty 状态不空白、不重叠，模板 3D 内容不再遮挡 UI。

保留限制：

- Apple Vision Pro 当前模拟器没有真实账本数据，因此本轮视觉 smoke 覆盖的是 empty 状态；真实数据展示仍需在有账本数据的 visionOS 运行环境或后续 snapshot 数据入口上复测。
- visionOS 尚未接 CloudKit 只读拉取或 dashboard snapshot。
- visionOS 尚未接 App Store Connect 平台素材、截图管线和真机 smoke。

下一步：

进入 `GOAL-1760`，集中处理 Mac / iPad / Watch 多端 polish 与已知刷新、布局、真机体验问题。

### 8.10 GOAL-1755 tvOS / visionOS 只读看板同步入口（2026-06-20）

状态：DONE。

本轮补上 tvOS / visionOS 展示版的跨设备数据入口：iPhone / iPad / Mac 主 App 在 iCloud 账本同步完成后，额外发布一份只读 dashboard snapshot 到 CloudKit private database；tvOS / visionOS 启动看板时优先读取该快照，没有快照或读取失败时继续回退到本机 SQLite。

设计边界：

- 快照只包含展示需要的账单字段：商户、金额、时间、分类、来源、备注。
- 不同步原始截图、支付截图、小票图片、OCR 原文、调试记录、idempotencyKey、syncRevision 或冲突状态。
- tvOS / visionOS 继续保持只读，不导入、不编辑、不删除、不清洗、不做多账本切换。
- 主 App 原有账单双向同步仍是正式账本来源；dashboard snapshot 只是大屏展示缓存，不作为写入源。

改动范围：

- `AutoLedgerCore`：新增 `LedgerDashboardCloudSnapshot` / `LedgerDashboardCloudTransaction` 展示快照模型，并在 `CloudLedgerSyncSchema` 中登记 `LedgerDashboardSnapshot` record type 和固定 record name。
- `LedgerCloudKitSyncAdapter`：新增 `pushDashboardSnapshot`，将展示快照编码为 `payloadJSON` 保存到 CloudKit。
- `LedgerStore`：在手动全量同步、启动 / 下拉拉取、账单变化推送后发布大屏快照；快照发布失败不阻断主账本同步。
- `AutoLedgerTV` / `AutoLedgerVision`：看板加载时优先读取 CloudKit dashboard snapshot，再回退到本机 SQLite。
- `AutoLedgerTV.entitlements` / `AutoLedgerVision.entitlements`：为新平台 target 增加 iCloud CloudKit entitlement。
- `scripts/run_offline_regression.sh`：补齐离线回归编译清单和 CloudKit adapter stub。

验证：

- `git diff --check`
- `bash scripts/run_offline_regression.sh`
- `xcodebuild -quiet -workspace AutoLedger/AutoLedger.xcworkspace -scheme AutoLedger -configuration Debug -destination 'generic/platform=iOS' build`
- `xcodebuild -quiet -workspace AutoLedger/AutoLedger.xcworkspace -scheme AutoLedgerTV -configuration Debug -destination 'generic/platform=tvOS' CODE_SIGNING_ALLOWED=NO build`
- `xcodebuild -quiet -workspace AutoLedger/AutoLedger.xcworkspace -scheme AutoLedgerVision -configuration Debug -destination 'generic/platform=visionOS' CODE_SIGNING_ALLOWED=NO build`

人工配置项：

- Apple Developer Portal 中需要确认主 App ID `top.darkrio326.AutoLedger` 已启用 iCloud / CloudKit，并包含 `iCloud.top.darkrio326.AutoLedger`；tvOS / visionOS 新平台 target 与 ASC 现有 App 记录共用该 Bundle ID。
- Xcode 需要重新刷新 / 生成包含 CloudKit entitlement 的 tvOS、visionOS provisioning profile。
- CloudKit Console 的 Development schema 会新增 `LedgerDashboardSnapshot`，进入 TestFlight / App Store 前需要按环境确认并部署到 Production schema。

下一步：

继续进入 `GOAL-1760`，集中处理 Mac / iPad / Watch 多端 polish，并在真机 / 模拟器上补 tvOS / visionOS 的实际 CloudKit 只读展示 smoke。

### 8.11 GOAL-1760 同秒账单编辑同步保护补强（2026-06-20）

状态：DONE。

用户在真机上观察到两条地铁账单中，一条可以保存补全后的站名，另一条 `地铁：埌西 →` 补全终点后仍会回退。本轮继续收口账本编辑后的 iCloud 同步保护，不修改地铁识别和金额计算。

根因判断：

- `syncRevision` 是设备本地递增值，不具备跨设备全局顺序语义。
- 上一轮已改为优先比较 `updatedAt`，但如果本地编辑和远端旧记录落在同一时间戳精度内，旧远端仍可能依靠更高 revision 覆盖本地编辑。
- 增量推送使用 `updatedAt > checkpoint`，在日期精度或同秒边界上存在漏推刚保存编辑的风险。

改动范围：

- `TransactionSyncConflictResolver`：只有当本地和远端来自同一 `deviceID` 时，才在相同 `updatedAt` 下使用 `syncRevision` 决胜；跨设备同秒分歧进入冲突保护，不覆盖本地账单。
- `LedgerSyncPlanner`：增量推送边界改为包含 checkpoint 秒，避免 `updatedAt == changedAfter` 的本地编辑被过滤。
- `scripts/OfflineRegression.swift`：新增 `地铁：埌西 →` 同秒旧远端高 revision 不能覆盖本地编辑的回归；新增 checkpoint 秒内变更仍进入增量推送的回归。

验证：

- `bash scripts/run_offline_regression.sh`

真机复测建议：

1. 在 iPhone 账本里再次编辑 `地铁：埌西 →`，补全为目标站名并保存。
2. 保存后等待至少 2 秒，让自动 CloudKit 推送任务触发。
3. 手动刷新 iPad / 另一端账本，确认商户名保持补全后的站名。
4. 如果仍回退，请在保存后立刻导出 Debug 记录，并记录同步日志中的 `新增 / 更新 / 保留本地 / 冲突` 数字。

### 8.12 GOAL-1760B 最近本机编辑保护窗口与同步日志导出（2026-06-20）

状态：DONE。

用户继续在真机上复现 `地铁：埌西 →` 补全终点站保存后仍失败的情况。本轮没有改地铁识别规则，也不处理南宁地铁系统自身把记录算成消费的问题，只补账本编辑后的本机保护窗口和现场诊断能力。

根因判断：

- 真机日志中出现的 `RemoteTextInput`、第三方输入法 RunningBoard entitlement 和 LaunchServices `process may not map database` 更像系统 / 输入法噪声，不能证明 AutoLedger 保存或同步失败。
- 需要从 App 内部记录保存链路：保存前 metadata、保存后 metadata、是否安排 iCloud 推送、后续远端拉取时是否命中本机保护。
- 即使同秒冲突策略已补强，App 层仍需要明确告诉批量远端拉取“这条账单刚刚由本机编辑过”，防止旧远端记录在保护窗口内静默回写。

改动范围：

- `SQLiteTransactionStore.applyRemoteSyncRecords` 新增 `protectedLocalTransactionIDs` 参数，默认空集合，不影响旧调用。
- `LedgerStore.updateTransaction` 在保存成功后记录最近编辑交易 ID，加入 10 分钟本机保护窗口，并写入 iCloud 同步日志。
- `LedgerStore.pullRemoteLedgerChanges` 在批量拉取远端账单时传入最近编辑 ID，保护窗口内遇到跨设备旧内容时保留本地。
- `DebugView` 整页导出和单条导出追加最近 iCloud 同步日志，便于真机失败时直接复制现场证据。
- `scripts/OfflineRegression.swift` 新增 `地铁：埌西 →` 编辑为 `地铁：埌西→万象城` 后，同秒旧远端批量拉取不会覆盖本地保护编辑的回归。

未改动范围：

- 不修改地铁 / 南宁地铁解析规则。
- 不修改金额计算。
- 不修改 CloudKit record schema、SQLite schema、Bundle ID、DEVELOPMENT_TEAM、App Group、iCloud Container、entitlements、Xcode Cloud 脚本或 App Store Connect 配置。

验证：

- 先新增失败回归并确认 `bash scripts/run_offline_regression.sh` 因缺少 `protectedLocalTransactionIDs` 参数失败。
- 修复后执行 `bash scripts/run_offline_regression.sh`，结果 PASS。

真机复测建议：

1. 重新安装 / 运行包含本轮修复的 build。
2. 编辑 `地铁：埌西 →`，补全终点站并保存。
3. 观察账本是否立即保持补全后的商户名；等待 iCloud 同步 / 手动刷新另一端。
4. 如果仍回退，进入设置调试页复制单条或整页测试记录，重点看 `账单编辑开始`、`账单编辑落盘`、`账单编辑已安排 iCloud 推送`、`远端拉取保护`、`远端拉取保护结果` 日志。

### 8.13 GOAL-1760C 交易编辑页输入提交修复（2026-06-21）

状态：DONE。

用户在真机上继续复现 `地铁：埌西→` 无法补全终点站。新的 Debug 导出显示：

- iCloud 启动链路正常：启动先增量推送 0 条，再拉取 365 条，新增 / 更新 / 删除均为 0，保留本地 365。
- 编辑后链路正常：`06:09:21` 开始增量推送，`06:09:27` 推送 1 条成功。
- 关键异常在保存前：`账单编辑开始：F453E243 地铁：埌西→ -> 地铁：埌西→`，说明 `TransactionEditorView` 传给 `LedgerStore.updateTransaction` 的新商户名本身就是旧值，并没有包含用户刚输入的终点站。

根因判断：

- Xcode Console 中的 RunningBoard / LaunchServices 报错仍属于系统 / 输入法噪声，不能证明 AutoLedger 保存失败。
- 真正问题更符合中文输入法组合文本未提交：用户在商户 TextField 中输入终点站后直接点导航栏保存，SwiftUI 的 `@State merchant` 可能仍停在旧值，导致保存旧商户名。

改动范围：

- `TransactionEditorView` 将商户输入框替换为 UIKit-backed `CompositionSafeTextField`，在编辑变化和选区变化时同步 `UITextField.text`。
- 商户输入框失焦时，如果系统取消 marked text 导致 UIKit 文本短于已同步绑定值，则恢复绑定值，避免中文组合文本消失。
- 点击保存时结束输入会话并等待一个主线程 tick 后调用 `editedTransaction()`。
- 保存流程继续沿用原有分类刷新提示、商户别名提示、`LedgerStore.updateTransaction` 和 iCloud 自动推送链路。

未改动范围：

- 不修改 iCloud 同步策略。
- 不修改 SQLite / CloudKit schema。
- 不修改地铁识别、金额计算或外部模型链路。

验证：

- `git diff --check`
- `xcodebuild -workspace AutoLedger/AutoLedger.xcworkspace -scheme AutoLedger -configuration Debug -destination 'generic/platform=iOS' build`

真机复测建议：

1. 在商户名输入框中补全 `万象城` 后，先手动点空白或切到其他字段，确认 `万象城` 不再消失。
2. 不必手动收键盘，直接点击保存。
3. 保存后查看 Debug 导出中的 `账单编辑开始`，应出现 `地铁：埌西→ -> 地铁：埌西→万象城` 或对应完整终点站。
4. 若仍显示旧值，说明该输入法的 marked text 没有进入 `UITextField.text`，需要继续考虑用 UIKit `shouldChangeCharactersIn` 本地草稿缓冲或改为显式“完成输入”按钮。

### 8.14 GOAL-1760D tvOS / visionOS 多端 polish 收口（2026-06-22）

状态：DONE（代码侧完成；真机 / TestFlight 数据读取 smoke 进入 `GOAL-1770`）。

本轮集中处理 tvOS 和 visionOS 展示端在模拟器和大屏环境里的体验问题：Apple TV 模拟器无法稳定登录 iCloud 时不应阻断 UI 验证；四个 tvOS tab 的内容顶部位置应保持一致；visionOS 首版需要更明显的宽屏 / 空间展示形态，而不是只像一块放大的普通面板。

改动范围：

- `AutoLedgerTV/ContentView.swift`：统一 header、tab 和内容区布局，固定 dashboard 内容高度，调整右侧列宽和按钮样式；新增遥控器 / 键盘左右方向切换 tab；补充 CloudKit 账号状态、dashboard snapshot、远端账本兜底读取诊断；在 DEBUG simulator 下提供虚构演示数据。
- `AutoLedgerVision/ContentView.swift`：在宽屏窗口下切换到三栏空间布局，展示月度支出看板、年度时间线 / 最近账单和分类支出卡片，并加入轻量 `rotation3DEffect` 与阴影层次；紧凑窗口继续使用原布局；DEBUG simulator 下提供虚构演示数据。
- `AutoLedgerTV.xcscheme` / `AutoLedgerVision.xcscheme`：新增 shared schemes，保证 tvOS / visionOS target 可被命令行和后续 CI / smoke 稳定发现。

未改动范围：

- 不让 tvOS / visionOS 写入正式账本。
- 不新增导入、编辑、删除、清洗、多账本或平台专属统计口径。
- 不修改 Bundle ID、DEVELOPMENT_TEAM、主 App App Group、主 App iCloud Container、CloudKit schema 或 Xcode Cloud 脚本。
- 模拟器演示数据只在 `DEBUG && targetEnvironment(simulator)` 下启用，不进入 Release。

验证：

- `git diff --check`
- `xcodebuild -workspace AutoLedger/AutoLedger.xcworkspace -scheme AutoLedgerTV -configuration Debug -destination 'generic/platform=tvOS' build`
- `xcodebuild -workspace AutoLedger/AutoLedger.xcworkspace -scheme AutoLedgerVision -configuration Debug -destination 'generic/platform=visionOS' build`

保留限制：

- tvOS / visionOS 的真实 iCloud private database 读取仍需真机或 TestFlight 环境复测；模拟器 mock 只能证明 UI、焦点和布局可用。
- `LedgerDashboardSnapshot` 进入发布前仍需确认 CloudKit Production schema。
- tvOS / visionOS App Store 平台截图、Preview 和 Review Notes 尚未进入本轮。

下一步：

进入 `GOAL-1770`，做 v1.6.0 发布资产与 smoke 收口：完整构建矩阵、Xcode Cloud / TestFlight、当前平台截图策略，以及是否把 tvOS / visionOS 随 ASC 1.5.0 正式开放的人工决策。

### 8.15 GOAL-1770 v1.6.0 发布资产与 smoke 基线（2026-06-24）

状态：IN_PROGRESS（本地命令级门禁完成；Xcode Cloud / TestFlight / ASC 平台素材仍需人工执行）。

本轮进入 `GOAL-1770` 的发布收口阶段，先建立 `v1.6.0` 对应 App Store / ASC `1.5.0` 的本地 smoke baseline、审核说明草稿和人工门禁清单。`GOAL-1760` 代码侧已收尾，tvOS / visionOS 继续按只读展示端处理，不把模拟器 mock 当成真实 CloudKit 同步证据。

新增文档：

- `versions/v1.6.0-regression-baseline.md`：记录当前命令级回归、全平台构建矩阵、既有 warning、平台 smoke 边界和 go / no-go 条件。
- `versions/v1.6.0-review-notes.md`：记录 ASC / TestFlight 可用的审核说明草稿、平台边界和人工提交前检查项。

本地验证：

- `git diff --check`：PASS。
- `bash scripts/run_offline_regression.sh`：PASS。
- `bash scripts/run_golden_regression.sh`：PASS，36 个 golden case 通过。
- `xcodebuild -list -workspace AutoLedger/AutoLedger.xcworkspace`：PASS，确认 `AutoLedger`、`AutoLedgerWatch Watch App`、`AutoLedgerTV`、`AutoLedgerVision` 等 scheme 可见。
- `xcodebuild -quiet -derivedDataPath /tmp/autoledger-dd-ios -workspace AutoLedger/AutoLedger.xcworkspace -scheme AutoLedger -configuration Debug -destination 'generic/platform=iOS' build`：PASS。
- `xcodebuild -quiet -derivedDataPath /tmp/autoledger-dd-mac -workspace AutoLedger/AutoLedger.xcworkspace -scheme AutoLedger -configuration Debug -destination 'platform=macOS,variant=Mac Catalyst' build`：PASS。
- `xcodebuild -workspace AutoLedger/AutoLedger.xcworkspace -scheme 'AutoLedgerWatch Watch App' -configuration Debug -destination 'generic/platform=watchOS' CODE_SIGNING_ALLOWED=NO build`：PASS。
- `xcodebuild -quiet -derivedDataPath /tmp/autoledger-dd-tv -workspace AutoLedger/AutoLedger.xcworkspace -scheme AutoLedgerTV -configuration Debug -destination 'generic/platform=tvOS' build`：PASS。
- `xcodebuild -quiet -derivedDataPath /tmp/autoledger-dd-vision -workspace AutoLedger/AutoLedger.xcworkspace -scheme AutoLedgerVision -configuration Debug -destination 'generic/platform=visionOS' build`：PASS。

已知 warning：

- `AppFormatters`、App Intent logger、部分 Intent / OCR / CloudKit 调用存在 Swift 6 / deprecated warning；当前为 warning，未阻断构建。
- Mac Catalyst 构建仍提示 MediaPipe xcframework 没有 Catalyst slice；当前返回码为 0，Mac 路径不依赖本地 Gemma / MediaPipe 推理作为发布硬门槛。
- 首次并行跑 iOS / Mac 时遇到 `build.db: database is locked`，改用独立 `-derivedDataPath` 顺序构建后通过，归类为本地构建并发环境问题，不是代码失败。

人工门禁：

- Xcode Cloud archive 需要重新跑 ASC `1.5.0` 构建，并确认主 App / Watch / Mac Catalyst / 新平台 target 的 signing profile 正常。
- TestFlight 需要覆盖 iPhone、iPad、Apple Watch、Mac Catalyst；tvOS / visionOS 若随本版开放，需要额外确认真机或 TestFlight 下 iCloud private database 账号、快照和只读读取可用。
- CloudKit Console 需要确认 `LedgerDashboardSnapshot` 已从 Development schema 部署到 Production schema。
- ASC 需要确认 iPhone / iPad / Apple Watch / Mac 当前素材；tvOS / visionOS 如果本版上架，需要补平台截图、Review Notes 和平台元数据。

当前判断：

- iPhone / iPad / Watch / Mac Catalyst 主发布线命令级门禁通过，可进入 Xcode Cloud / TestFlight 发布验证。
- tvOS / visionOS 代码侧可构建，产品边界为只读展示；如素材、真机数据读取或 ASC 平台准备未完成，可继续作为内部预览，不阻塞主平台发布。

### 8.16 GOAL-1770B tvOS / visionOS 截图管线扩展（2026-06-24）

状态：DONE（脚本与 `zh-Hans` 导出完成；`zh-Hant` / `en` 和 ASC 上传前目检仍需人工执行）。

本轮在现有 `tools/appstore-screenshots` 管线中补齐 Apple TV 和 visionOS 平台截图能力，不新建平行 `marketing/appstore` 目录，也不修改线上 App Store Connect。截图模式继续使用虚构演示数据，避免读取真实账本、iCloud、网络、相册、相机、OCR、LLM 或用户截图。

完成内容：

- `tools/appstore-screenshots/config/screenshots.json`：新增 `tvos` / `visionos` app 配置、3840x2160 target、Apple TV 4 张截图和 visionOS 3 张截图的三语文案。
- `tools/appstore-screenshots/scripts/export_tvos.sh`、`export_visionos.sh`：新增按平台导出脚本，支持 `--locale`、模拟器选择、构建、安装、按 scene 启动和 raw 截图。
- `tools/appstore-screenshots/scripts/export.sh`：新增 `--tvos-only`、`--visionos-only` / `--vision-only`。
- `tools/appstore-screenshots/scripts/render_marketing.py`、`build_preview.py`：新增 tvOS / visionOS store 渲染和 preview 分组。
- `AutoLedgerTV` / `AutoLedgerVision`：screenshot mode 支持 `--screenshot-scene`，可直接进入对应截图页面；visionOS 按 dashboard / categories / timeline 切换宽屏展示布局。
- `tools/appstore-screenshots/README.md` 和审计文档：同步记录新平台导出方式、输出目录和仍需人工完成的部分。

验证：

- `python3 -m json.tool tools/appstore-screenshots/config/screenshots.json`：PASS。
- `python3 -m py_compile tools/appstore-screenshots/scripts/render_marketing.py tools/appstore-screenshots/scripts/build_preview.py`：PASS。
- `xcodebuild -quiet -derivedDataPath /tmp/autoledger-shot-tv-build -workspace AutoLedger/AutoLedger.xcworkspace -scheme AutoLedgerTV -configuration Debug -destination 'generic/platform=tvOS' build`：PASS。
- `xcodebuild -quiet -derivedDataPath /tmp/autoledger-shot-vision-build -workspace AutoLedger/AutoLedger.xcworkspace -scheme AutoLedgerVision -configuration Debug -destination 'generic/platform=visionOS' build`：PASS。
- `bash tools/appstore-screenshots/scripts/export.sh --tvos-only --locale zh-Hans`：PASS，生成 4 张 `3840x2160` store PNG。
- `bash tools/appstore-screenshots/scripts/export.sh --visionos-only --locale zh-Hans`：PASS，生成 3 张 `3840x2160` store PNG。

输出目录：

- `tools/appstore-screenshots/output/store/tvos/zh-Hans`
- `tools/appstore-screenshots/output/store/visionos/zh-Hans`

保留限制：

- 本轮只验证 `zh-Hans`；`zh-Hant` / `en` 仍需发布前导出和目检。
- tvOS / visionOS 真实 iCloud private database 读取仍需真机或 TestFlight smoke，不由截图模拟器导出替代。
- visionOS 截图为模拟器窗口合成画面，适合首版 ASC 素材验证；如后续要更强空间感，可再单独做 App Preview / 录屏素材。

### 8.17 GOAL-1770C ASC 新平台 Bundle ID 与 visionOS 图标修复（2026-06-24）

状态：DONE（代码侧修复完成；需重新 archive / 上传验证）。

ASC 上传新平台构建时报错：

- `ITMS-90055`：上传包 Bundle ID 不能从当前 App 记录的 `top.darkrio326.AutoLedger` 改为新值。
- `ITMS-90970`：visionOS App 缺少 `CFBundleIcons.CFBundlePrimaryIcon`。

处理结果：

- `AutoLedgerTV` 和 `AutoLedgerVision` 的 `PRODUCT_BUNDLE_IDENTIFIER` 统一改为 `top.darkrio326.AutoLedger`，与现有 ASC App 记录一致。
- `tools/appstore-screenshots/config/screenshots.json` 和 README 中 tvOS / visionOS 的 bundle id 同步为 `top.darkrio326.AutoLedger`。
- `AutoLedgerVision/Info.plist` 新增 `CFBundleIcons -> CFBundlePrimaryIcon = AppIcon`，显式声明 visionOS App Icon 名称。

注意：

- 这是新平台 target 对齐现有 ASC App 记录，不是修改主 App Bundle ID；主 App 本来就是 `top.darkrio326.AutoLedger`。
- 重新上传前建议在 Xcode / Xcode Cloud 刷新 tvOS 和 visionOS 的 signing profile，确认同一个 App ID 下 iCloud CloudKit capability 可用。

### 8.18 GOAL-1770D tvOS / visionOS ASC 资产与 entitlement 修复（2026-06-24）

状态：DONE（代码侧修复完成；等待 Xcode Cloud / ASC 重新上传验证）。

继续处理 ASC 新平台上传校验问题。本轮不修改业务代码、不改主 App Bundle ID、不改 DEVELOPMENT_TEAM、不改 CloudKit schema，也不把 tvOS / visionOS 从只读展示端扩成可写端。

ASC 报错：

- `ITMS-90963`：`AutoLedgerVision.app` 缺少 visionOS App Icon image。
- `ITMS-90575`：带有 `com.apple.developer.icloud-services` 的 tvOS App 缺少 `aps-environment` entitlement。
- `ITMS-90513`：tvOS App 缺少 `CFBundleIcons.CFBundlePrimaryIcon`。
- `ITMS-90513`：tvOS App 缺少 `TVTopShelfImage.TVTopShelfPrimaryImageWide`。

完成内容：

- `AutoLedgerVision`：补齐 `AppIcon.solidimagestack` 的 `Back / Middle / Front` 三层 PNG，改为不套用 iOS 方形图标外壳的 visionOS 专用圆形安全区表达；产物 `Assets.car` 中可查到 `AppIcon` 及其三层内容。
- `AutoLedgerTV`：补齐 tvOS 分层 App Icon、App Store Icon、Top Shelf Image 和 Top Shelf Wide Image；Top Shelf 只放品牌英文与抽象账本卡片，避免不可本地化资产中出现中文字体渲染问题。
- `AutoLedgerTV/Info.plist`：新增 `CFBundleIcons` 与 `TVTopShelfImage`，显式声明 `CFBundlePrimaryIcon`、`TVTopShelfPrimaryImage` 和 `TVTopShelfPrimaryImageWide`。
- `AutoLedgerTV/AutoLedgerTV.entitlements`：新增 `aps-environment = $(APS_ENVIRONMENT)`；Debug / Release build settings 分别配置为 `development` / `production`。
- `AutoLedger.xcodeproj`：为 `AutoLedgerTV` synchronized folder 增加 `Info.plist` membership exception，避免 `Info.plist` 既被处理又被复制到 bundle 造成 build 双产物冲突。

验证：

- `find AutoLedger/AutoLedgerTV/Assets.xcassets AutoLedger/AutoLedgerVision/Assets.xcassets -name Contents.json -print0 | xargs -0 -n1 python3 -m json.tool >/dev/null`
- `plutil -lint AutoLedger/AutoLedgerTV/Info.plist AutoLedger/AutoLedgerTV/AutoLedgerTV.entitlements AutoLedger/AutoLedgerVision/Info.plist`
- `xcodebuild -quiet -derivedDataPath /tmp/autoledger-tv-asset-check -workspace AutoLedger/AutoLedger.xcworkspace -scheme AutoLedgerTV -configuration Debug -destination 'generic/platform=tvOS' build`
- `xcodebuild -quiet -derivedDataPath /tmp/autoledger-vision-asset-check -workspace AutoLedger/AutoLedger.xcworkspace -scheme AutoLedgerVision -configuration Debug -destination 'generic/platform=visionOS' build`
- tvOS 产物检查：`CFBundleIcons.CFBundlePrimaryIcon`、`TVTopShelfImage.TVTopShelfPrimaryImageWide`、`aps-environment` 和 `Assets.car` 均存在。
- visionOS 产物检查：`CFBundleIcons.CFBundlePrimaryIcon = AppIcon`，`Assets.car` 内含 `AppIcon / Back / Middle / Front`。

保留限制：

- 本轮只验证 Debug generic build 和产物结构；最终 ASC 结论仍以 Xcode Cloud archive / 上传后校验为准。
- tvOS / visionOS signing profile 需要在 Xcode Cloud 或 Xcode 上传前重新生成 / 刷新，确保同一个 App ID 的 iCloud CloudKit 与 push entitlement 都进入发布 profile。
- 当前图标资产为首版平台适配素材，已避免 iOS 方图硬套，但仍建议在 ASC / TestFlight 中做一次平台展示目检。
