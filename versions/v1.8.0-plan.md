# v1.8.0 版本计划：Global Readiness、Review & Close 与可信同步

更新日期：2026-07-28
文档状态：Draft / Early Execution
内部开发线：v1.8.0
App Store 对外版本：ASC 1.7.0
承接版本：v1.7.0 / ASC 1.6.0
当前项目状态：[PROJECT_STATUS.md](../PROJECT_STATUS.md)
产品路线图：[docs/ROADMAP.md](../docs/ROADMAP.md)
全球产品战略：[docs/product/GLOBAL_PRODUCT_STRATEGY.md](../docs/product/GLOBAL_PRODUCT_STRATEGY.md)
跨版本语言路线图：[docs/product/I18N_ROADMAP.md](../docs/product/I18N_ROADMAP.md)
本版本语言矩阵：[versions/v1.8.0-i18n-release-matrix.md](v1.8.0-i18n-release-matrix.md)

## Planning Status

本文件已进入 `Draft / Early Execution`：`GOAL-2460`、`GOAL-2410`、`GOAL-2420`、`GOAL-2430` 与 `GOAL-2440` 的第一批实现已经进入主线，ASC `1.7.0` 本地工程版本基线已通过回归与构建门禁。2026-08-02 用户另行授权推送一次 `xcbuild-v1.8.0` 内部验证构建；该单次授权不表示整个 `v1.8.0` 已达到候选冻结或发布就绪，也不授权创建不可移动产品标签、绑定 ASC、提交审核或发布。

当前前置条件状态：

- 已满足：`v1.7.0 / ASC 1.6.0` 已完成候选冻结、发布决定、证据归档和正式发布；
- 已满足：当前五语发布缺口已有结论，韩语母语审校作为非阻断质量增强继续积累；
- 已满足：ASC Primary Language 已在线确认为 `English (U.S.) / en-US`；
- 已满足第一批边界：PendingAction 决定复用既有 CloudKit configuration payload，不新增 SQLite / CloudKit schema；后续若新增 record / field / index，仍需独立设计评审和 Production schema 门禁；
- 未满足：美国、英国、加拿大、澳大利亚和新加坡的地区格式、价格、截图、支持、隐私、样本与人工验收责任尚未全部锁定；
- 未满足：可信同步、月结、批量处理、完整新来源和发布观测仍未形成 P0 闭环。

只有剩余 P0 合同、验收责任和回滚边界冻结后，本文档才能升级为 `Execution Ready`；只有全部发布门禁有证据后，才可以进入候选冻结。

## Implementation Progress

### 2026-08-03 / 账户迁移后构建重触发

- 首次 `xcbuild-v1.8.0` 因 Xcode Cloud 账户迁移未触发；同 SHA 标签重建只回读到删除事件，无法作为可靠的新建触发证据；
- 新增一个仅包含构建追溯文档、无业务代码变化的重触发切片，并将唯一可移动标签 `xcbuild-v1.8.0` 移到该新提交；
- 商店版本继续为 `1.7.0`，`CURRENT_PROJECT_VERSION` 与产品标签均不变；迁移后的 Xcode Cloud run、实际 build number、四平台 Archive 和 processing 必须实时回读；
- 如果仍无 Apple 侧 run，下一步是检查迁移后的 Xcode Cloud 产品与 GitHub 仓库连接，不继续用重复空切片掩盖连接问题。

### 2026-08-02 / ASC 1.7.0 内部验证构建

- 将已完成的工程版本基线、可理解同步状态第一批和新版微信退款账单识别收敛到同一 `main` 构建基线；
- 创建 `v1.8.0` 开发线唯一可移动 Xcode Cloud 触发标签 `xcbuild-v1.8.0`，旧 `xcbuild-v1.7.0` 与不可移动产品标签 `v1.7.0` 保持不变；
- 所有 target 的 `MARKETING_VERSION` 保持 `1.7.0`，不修改 `CURRENT_PROJECT_VERSION`；实际云端 build number、Archive、processing、ASC 绑定和设备验收必须在触发后实时回读；
- 本次内部验证构建不改变本文档 `Draft / Early Execution` 状态，也不关闭可信同步、月结、批量处理、五市场或发布门禁。

### 2026-07-28 / ASC 1.7.0 工程版本基线

- 主 App、Watch、Widget、Share Extension、Control Widget、tvOS 与 visionOS 的全部 Debug / Release build configuration 已统一使用 `MARKETING_VERSION = 1.7.0`；
- `LedgerStore` 的运行时版本兜底同步为 `1.7.0`，正常构建仍以 `CFBundleShortVersionString` 为真源；
- 本轮不修改 `CURRENT_PROJECT_VERSION`、build number、Bundle ID、签名、entitlement、StoreKit、CloudKit / SQLite / D1 schema、ASC 在线版本或构建标签。

### 2026-07-28 / GOAL-2440 第一批

- `AutoLedgerCore` 新增稳定的普通用户同步状态合同，覆盖 `disabled / checkingAccount / syncing / waitingToUpload / upToDate / offline / needsConflictReview / failedWithLocalDataSafe`，并明确离线、冲突和失败场景下本地数据仍可用；
- `LedgerStore` 将启用状态、账号检查、上传 / 拉取阶段、待上传变更、上次成功时间、冲突数量和错误结果收敛到该合同；完整 CloudKit 阶段日志继续留在开发者诊断；
- 普通用户的数据管理页只展示可理解的状态、上次成功时间和冲突入口，不再直接暴露调试日志；五语状态与冲突文案已补齐；
- 网络中断、CloudKit 暂时不可用和递归 partial failure 会映射为离线状态，其它失败映射为本地数据安全的失败状态；
- 新增同步状态 resolver 回归和静态 smoke，并接入完整离线回归；五语 strings lint、完整离线回归与 iOS generic workspace build 已通过。

第一批关闭稳定状态合同与普通用户展示，不关闭整个 `GOAL-2440`。真实 iPhone / iPad 的离线、账号受限、无权益、待上传和恢复路径，重庆 Moxy 已知冲突回归，以及同步冲突作为 PendingAction 来源的跨设备往返仍需独立证据。

### 2026-07-20 / GOAL-2460 第一批

- 从 `main` / `origin/main` 精确提交 `022dba591c77b40b5a936b9d9e1d87f51a4f6796` 建立 `codex/v1.8.0-foundation`；
- `AppFormatters` 的用户可见金额、日期、时间和月份改为 locale-aware，导出时间戳保持稳定机器合同；
- 未显式配置币种的旧账本由固定 CNY 改为系统地区币种 fallback，显式账本币种继续优先；
- WatchConnectivity、Watch App、iPhone Widget 与 Watch Widget 传递或读取账本币种，移除本轮触达路径中的中国地区显示硬编码；
- 离线回归覆盖 USD / GBP / JPY / KRW / CAD、MDY / DMY 与月份本地化，Widget smoke 防止 CNY / `¥` / 固定日期格式回退；
- 完整离线回归与 iOS generic workspace build 已通过。

该进度只关闭格式基础的第一批实现，不关闭 `GOAL-2460`。语音 locale、歧义日期复核、主 App 全量格式审计、五个英语市场设备 / 样本 / 商店验收仍为未完成门禁。

### 2026-07-20 / GOAL-2460 第二批

- 用户可见通知、Watch 快速记账 / 语音确认、酒店草稿 fallback、支持页预览和分享卡去除固定人民币默认；
- Speech locale 跟随 App 语言与系统英语地区，不再固定 `zh_CN`；
- 纯数字日期按 locale 的 MDY / DMY 顺序解析，双解日期在 OCR 确认页明确提示用户通过 DatePicker 复核；
- 新增全球格式静态 smoke 并接入完整离线回归，普通运行时硬编码审计已排除人民币 OCR 规则、截图演示和机器日期协议；
- 完整离线回归、本地化覆盖、Widget / 全球格式 smoke 与 iOS generic workspace build 已通过。

第二批关闭了已知运行时中国地区默认与歧义日期入口，但仍不关闭 `GOAL-2460`。五个英语市场必须继续取得真实设备语音、票据 / 酒店样本、税费 / 服务费、12 / 24 小时、截图、支持和 ASC 证据；不能用代码审计替代市场 Ready。

### 2026-07-20 / GOAL-2410 第一批

- `AutoLedgerCore` 已冻结第一版 `PendingActionItem` 合同：版本化稳定 ID、kind / category、source type / ID / revision、reason code、priority、Pro 属性、目标入口、动作集合和生命周期时间；
- 状态机覆盖 `pending / deferred / resolved / dismissed`，终态只能显式 reopen，非法或重复迁移由合同拒绝；
- 当前 OCR 待确认、酒店草稿、疑似重复、订阅异常和数据清洗建议已生成真实来源条目，再由条目统一推导现有分组和数量；
- 数据清洗 preview ID 可能包含商户文本，进入 PendingAction 前改为稳定 opaque fingerprint；合同不复制账本、金额、商户、PDF、OCR 原文或邮箱正文；
- 五语原因文案、稳定 identity、revision 变化、状态迁移、JSON 往返、隐私 smoke、完整离线回归和 iOS generic workspace build 已通过。

第一批只冻结并接通当前已有五类来源，不关闭整个 `GOAL-2410`。本地邮箱候选、云收件箱候选、月结缺资料和同步冲突需要在目标入口明确后增量加入；用户决定尚未持久化，重启不复活、来源更新 / 删除、备份恢复和跨设备合并仍属于 `GOAL-2420`，任何 SQLite / CloudKit schema 必须先通过独立设计评审。

### 2026-07-21 / GOAL-2420 第一批

- 新增 `PendingActionDecision` overlay，只保存 kind、source identity / revision、决定、更新时间和可选延后时间；不复制来源业务内容；
- `deferred / resolved / dismissed / reopened` 决定按 `pa1` identity 应用，同一 revision 保持决定，新 revision 自动重新进入待处理；延后到期后恢复可见；
- `reopened` 作为显式同步 tombstone，避免删除本机决定后被旧远端 resolved / dismissed 记录重新合并；
- `LedgerStore` 使用 UserDefaults 保存决定，并接入既有自动备份和 CloudKit 配置推送调度；
- 手工 / iCloud 备份及 CloudKit configuration `payloadJSON` 增加向后兼容成员；旧备份和旧配置缺字段时读取为空；
- 跨设备配置合并按 action ID 和 `updatedAt` latest-wins，同一时间戳确定性优先 reopened tombstone；外部字典键会按决定计算 identity 重新规范化；
- 完整离线回归、PendingAction smoke 和 iOS generic workspace build 已通过。

本批不新增 SQLite table 或 CloudKit record / field / index，因此不需要 schema migration 或 Production schema deploy。它完成了决定持久化底座，但现有分组 UI 尚未暴露通用延后、忽略和重开入口，真实 iPhone / iPad CloudKit 往返与 tombstone retention 仍待 `GOAL-2430` 联合验收，所以不关闭整个 `GOAL-2420`。

### 2026-07-21 / GOAL-2430 第一批

- iPhone sheet 与 iPad / Mac 共用工作区从分类导航升级为逐条待处理卡片，显示来源类型、原因和 Pro 标识；
- “复核”只导航到原业务页面，由账单确认、酒店复核、订阅异常或数据清洗真源完成最终修改；酒店条目优先使用精确 draft ID；
- “稍后”支持明天或 7 天后，“忽略”需要二次确认；未来延后和终态项目分别进入“稍后”“已处理”分区，并可显式重新打开；
- 首页数量继续只统计当前可处理项目，避免已处理历史污染用户关注数字；新分区只保存 source-backed item，不会从孤立决定生成业务内容；
- 快照新字段对旧 payload 向后兼容，五语操作与状态文案、PendingAction smoke、完整离线回归和 iOS generic workspace build 已通过。

该批完成当前五类来源的逐条处理闭环，但不关闭 `GOAL-2430`：批量安全操作、新来源接入、20,000 条账本性能证据，以及 iPhone + iPad 的 CloudKit 忽略 / 重开往返仍未完成。`GOAL-2420` 的 tombstone retention 也继续保持开放。

## Product Theme

`v1.8.0` 的主题是：

> **Global Readiness & Review/Close / 全球基础与账本闭环。**

目标不是增加更多记账入口或追求语言数量，而是先让全球 Apple 用户获得正确的日期、币种、隐私和商店体验，再让用户回答三个问题：

1. 还有什么没有处理？
2. 为什么需要处理，处理后会发生什么？
3. 这个月是否已经可以放心归档？

产品主循环收敛为：

> 导入或发现 -> 进入待处理 -> 复核 / 忽略 / 延后 -> 规则反哺 -> 月结检查 -> 完成本月。

## Release Positioning

- 免费版继续提供完整的手动记账、单张识别、逐条复核、基础月报、基础导出和历史数据访问。
- Pro 提供集中待处理、批量安全操作、自动检查、月结资料包和可解释的整理建议。
- 语言识别和 UI 本地化属于免费基础体验，不进入 Pro gate。
- 云端不可用时，本地账本、逐条复核、规则和月结检查必须继续可用。
- 当前酒店水单能力作为凭证归档的差异化来源进入统一工作流，但本版本不扩张为企业报销系统。

## P0 Scope

### 1. Global Foundation

- `en-US` 作为工程与 ASC 主语言，完成美国、英国、加拿大、澳大利亚和新加坡的市场验收；
- 用户可见日期、时间、数字和金额按 locale / 账本币种展示，不保留中国地区硬编码默认；
- 原始币种、本位币、换算金额、汇率日期和提供方有稳定展示与失败 fallback；
- App Store 名称、副标题、关键词和截图从“一键记账”转为私密账本、自动化导入与旅行 / 酒店；
- 本地、iCloud、邮件、PDF 暂存、Common API、汇率和权益校验分别有一致的隐私说明；
- iPhone、iPad、Mac、Watch、Widget、Share Extension、Siri / Shortcuts 共同通过英语与地区 smoke。

第一批 Swift 格式基础已经落地；后续继续按独立批次审计和迁移，不能把当前局部完成误写为英语五市场 Ready。

### 2. Persistent Pending Actions

当前 `PendingActionCenterSnapshot` 只聚合分类和数量。`v1.8.0` 先冻结真实待处理条目合同，建议至少包含：

- 稳定 ID；
- kind / category；
- source type 与 source ID；
- reason code 与可本地化解释；
- priority；
- `pending / deferred / resolved / dismissed` 状态；
- created / updated / resolved 时间；
- 是否属于 Pro 自动化；
- 可执行动作集合与目标入口。

来源范围：

- OCR / 截图待确认；
- 酒店水单草稿；
- 本地邮箱候选；
- 云收件箱候选；
- 疑似重复；
- 订阅异常；
- 本地与 hash-only 云端数据清洗建议；
- 月结前缺资料项；
- 需要用户处理的同步冲突。

事实边界：原始账单、订阅、水单草稿和同步记录仍是业务真源。待处理表只保存引用、原因和用户决策，不复制完整账本、PDF、OCR 原文或邮箱正文。

### 3. Review Lifecycle

- iPhone 提供逐条复核、稍后处理、确认和忽略路径，并能从首页看见剩余数量。
- iPad / Mac 提供多选、分组和批量操作；只有可逆且可预览的动作允许批量执行。
- 在来源模块处理后，待处理中心必须同步消失；从待处理中心处理后，来源模块状态也必须更新。
- 已解决或已忽略条目不能因重启、重新分析或 CloudKit 拉取无条件复活。
- 忽略和撤销必须有清晰的作用范围，不能污染未来无关交易。

### 4. Human-Readable Sync State

把当前面向调试的长文本同步日志收敛成稳定状态模型：

- disabled；
- checkingAccount；
- syncing；
- waitingToUpload；
- upToDate；
- offline；
- needsConflictReview；
- failedWithLocalDataSafe。

用户界面只展示人能理解的状态、上次成功时间、是否有待上传内容和是否需要处理冲突。完整计数与阶段日志继续留在开发者诊断，不作为普通用户的主状态文案。

### 5. Month Close MVP

月结检查从现有待处理事实生成，不建立第二套不一致的异常扫描：

- 未确认记录；
- 疑似重复；
- 订阅异常；
- 未归档酒店水单；
- 低置信度或分类待确认；
- 缺失凭证；
- 月结包是否生成。

用户可以完成本月、查看完成时间和重新打开。完成本月不是锁账，不阻止后续编辑，也不能替代备份。

### 6. English-Market Cohort

本版本不新增 UI 语言，改为完成第一阶段英语市场质量：

- ASC 主语言与主翻译源：`English (U.S.) / en-US`；
- 市场：美国、英国、加拿大、澳大利亚、新加坡；
- 格式：MDY / DMY、12 / 24 小时、USD / GBP / CAD / AUD / SGD、小数与千分位；
- 票据：普通收据、税费 / 服务费、多币种和酒店水单；
- 商店：地区关键词、截图、订阅价格、支持页和隐私说明；
- 不复制无差异 locale；只有存在地区价值时才新增 ASC localization。

六项语言门禁以 [v1.8.0-i18n-release-matrix.md](v1.8.0-i18n-release-matrix.md) 为版本验收源。

## P1 Scope

P1 只有在 P0 合同、英语市场组和性能基线稳定后才能进入：

- 云端别名建议成功响应缓存；
- 持久化 cooldown / failure backoff；
- Worker 配额和最小日志策略；
- 规则来源、最近命中和影响范围的只读解释；
- 现有 AL Dashboard 增加匿名聚合的待处理产生 / 解决、月结开始 / 完成、同步结果和建议采纳漏斗。

完整规则中心、Saved Views 和订阅省钱看板保留给 `v1.9.0`，不能因 P0 提前完成而扩入本版本。

## Data And Sync Contract

实施前必须先回答：

1. 哪些待处理条目可以从来源确定性重建？
2. 哪些用户决策必须持久化，避免已处理事项复活？
3. 哪些决策需要跨设备同步，哪些只属于单设备临时状态？
4. 忽略、解决和撤销如何与来源对象的 revision 对齐？
5. 过期来源或已删除来源如何清理待处理引用？

推荐原则：

- 可重建的建议本体不直接跨设备同步；
- 用户的解决 / 忽略决策以稳定 source identity 和 revision 作为边界；
- 任何新的 SQLite table、CloudKit record type / field / index 都必须有迁移、旧备份兼容、离线回归和 Production schema 部署证据；
- 不能用云端同步状态阻塞本机手动记账。

最终 schema 只有在设计评审和迁移测试通过后才能写入本文档，不在 Planning 阶段假定具体表结构。

## English Primary Language Carry-Forward

`v1.8.0` 继承以下前置条件：

- Xcode `developmentRegion = en`；
- ASC Primary Language 为 `English (U.S.) / en-US`；
- 英语 key 集是其它语言覆盖检查的基准；
- 新增 target、截图场景、远端 release notes 和错误 fallback 默认先提供英语源文案；
- App 语言与 Foundation Models locale 支持分开判断，不支持时回退到确定性本地能力。

## GOAL Queue

| GOAL | 主线 | 优先级 | 目标 | 完成判定 |
|---|---|---|---|---|
| `GOAL-2400` | Planning | P0 | v1.8 范围、Free / Pro、语言和隐私边界冻结 | 本计划升级为 Execution Ready，非目标与回滚边界明确 |
| `GOAL-2410` | Workflow | P0 | PendingAction 合同 | Core 模型、稳定 identity、来源引用、原因和状态机通过回归 |
| `GOAL-2420` | Persistence | P0 | 待处理决策持久化 | 重启、来源更新、删除、备份恢复和迁移回归通过 |
| `GOAL-2430` | UI | P0 | iPhone / iPad / Mac 统一复核 | 逐条与批量路径一致，来源和中心双向更新，无状态复活 |
| `GOAL-2440` | Sync | P0 | 可理解的同步状态与冲突入口 | 普通用户状态稳定，重庆 Moxy 冲突回归通过，本地数据失败安全 |
| `GOAL-2450` | Month Close | P0 | 月结检查和完成 / 重开 | 检查项来自统一事实，完成不锁账，月结包可继续生成 |
| `GOAL-2460` | Global Format | P0 | 日期、数字、币种、语音、Watch / Widget 地区默认治理 | 五个英语市场无中国地区硬编码泄漏，歧义日期可复核 |
| `GOAL-2470` | Store / L10N | P0 | 英语五市场 metadata、截图、价格、支持与隐私 | 市场矩阵通过，ASC 线上变更仍需独立发布授权与回读 |
| `GOAL-2480` | Cloud Assist | P1 | 缓存、backoff、配额和最小日志 | 不扩大上传字段，离线 fallback 与服务端限流可验证 |
| `GOAL-2490` | Observability / Release | P0 | 匿名漏斗、发布材料和证据归档 | 不采集账本内容或用户 ID，Dashboard / ASC / 隐私口径一致 |

## Acceptance

### Functional

- 同一来源只产生一个有效待处理事项，状态迁移可解释。
- App 重启后 pending / deferred / resolved / dismissed 结果保持一致。
- 来源模块和待处理中心无双重状态或孤立引用。
- 用户能从一个入口处理完所有 P0 待处理类型并达到 empty state。
- 月结完成后仍可编辑账本并重新打开检查。

### Reliability And Performance

- 20,000 条隔离账本下，待处理中心打开和 Tab 切换不低于 TS 117 已验证体感基线。
- 待处理分析和月结检查不在 SwiftUI `body` 或 UI actor 重复扫描全量账本。
- iCloud 离线、401、超时和冲突时，本机账本与逐条处理保持可用。
- 解决后的同步冲突和待处理事项不会因 stale remote metadata 复活。

### Localization

- 英语、简中、繁中、日语、韩语基线不回退。
- 英语五市场完成日期、币种、语音、Watch / Widget、截图、价格、支持和隐私验收。
- 日语、简中、繁中、韩语现有基线不回退；本版本不宣称新增 UI 语言。

### Privacy And Entitlement

- 待处理持久化不新增原始 PDF、OCR、邮箱正文或精确账本数据的云端副本。
- 匿名观测只上传 allow-list 内的操作、结果、数量和耗时 bucket，不上传用户标识或原始待处理条目。
- Free 用户能逐条完成全部基础处理；Pro 只提升集中、批量和自动检查效率。

## Product Evidence

现有 AL Dashboard 只增加聚合口径：

- pending generated / resolved / dismissed count；
- unresolved age bucket；
- month close started / completed count；
- suggestion shown / accepted / later rule-hit count；
- sync completed / failed / conflict-review count；
- pending center open duration bucket。

不引入稳定用户 ID，因此不能把事件次数解释为 DAU、留存率或用户级转化率。

## Non-Goals

- 不新增 UI 语言；本版本语言 / 市场组固定为英语五市场质量。
- 不做银行账户直连、预算、投资组合、信用评分或财务建议。
- 不做通用 AI 聊天首页或让模型直接计算金额、执行规则、完成月结。
- 不完成全量附件保险箱、公开分享网页或团队协作。
- 不为 tvOS / visionOS 增加新的可写业务能力。
- 不新增 Worker 服务；P1 优先复用现有 folio Worker 和 Common API 职责边界。
- 不改变 StoreKit Product ID、历史数据访问权或基础识别免费承诺。

## Execution Order

1. 完成 `v1.7.0` 发布决定和语言 / ASC 证据归档。
2. 先冻结全球格式、币种、隐私和英语五市场验收合同，再冻结 PendingAction、月结和同步状态合同。
3. 先写 Core / persistence 回归，再实现持久化和迁移。
4. iPhone 逐条复核与 iPad / Mac 批量工作台并行，但共用同一状态机。
5. 产品文案冻结后完成全 target 英语一致性、地区格式、截图和 ASC metadata 建议。
6. 最后接匿名聚合指标、真实设备性能、CloudKit Production schema 和发布材料。

## Risks

- 待处理状态持久化与来源确定性重建如果边界不清，会产生重复、复活和孤立记录。
- 跨设备同步全部建议会放大 stale state；只同步用户决策也需要稳定 source identity。
- 当前核心格式器、Watch、Widget 和语音入口存在 CNY / `¥` / `zh_CN` / 固定日期格式，迁移范围可能跨多个 target。
- 同为英语不代表地区行为相同；MDY / DMY、税费、币种、价格和支持内容仍需逐市场验证。
- 多币种汇总若缺少清晰汇率日期、提供方和 fallback，会损害本地优先产品信任。

## Rollback

- 每个 GOAL 必须可独立回滚；语言包保持纯数据和 fallback-friendly。
- schema 变更必须提供向前迁移和旧备份恢复测试，不能依赖删除数据库回滚。
- 云端辅助增强可以服务端关闭，且不影响本机建议。
- 若某英语市场未通过验收，不新增无证据的地区 locale，也不能把共享英语文案视为该市场 Ready。
