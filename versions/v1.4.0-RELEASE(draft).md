# v1.4.0 / v1.4.x Release Notes（草稿）

更新日期：2026-05-25

## 1. 发布定位

- 内部开发线：v1.4.x
- 对外 App Store 版本：v1.3.0（沿用 `versions/v1.4.0-plan.md` 的版本号策略）
- 承接版本：v1.3.5
- 迭代记录：ITER-066 ~ ITER-079
- 当前状态：代码侧核心能力已完成并通过构建 / 离线回归；用户可见主路径的简体中文、繁体中文、英文 UI 文案已补齐，Debug / 调试导出文本仍保留中文。

v1.4.x 的产品主题是“更容易随手记一笔”：把 iPhone 截图记账之外的 Watch、快捷指令、语音、月报查看和历史账单整理能力收口到一个可测试版本。

## 2. 当前已实现功能

### 2.1 Apple Watch 伴侣 App

- 新增 Watch App 基础入口，可查看今日支出与最近账单。
- 支持 Watch 快速记账：选择分类、输入金额、可选商户后同步到 iPhone 主账本。
- 支持 Watch 语音记账确认流：在 Watch 上输入 / 听写账单文本，解析为金额、商户、分类后确认保存。
- 新增 WatchConnectivity 链路：Watch 端提交的快速记账请求转发到 iPhone，由 iPhone 侧 `LedgerStore` 入库并刷新账本。
- Watch 端定位为轻量入口，不独立管理完整账本，不引入云端账户或服务器同步。

### 2.2 辅助功能

- iPhone 端已覆盖账本、导入收件箱、关键指标卡片等主路径 VoiceOver 标注。
- iPhone 导入收件箱已适配 Reduce Motion，减少动态效果依赖。
- Watch 端主列表、快速记账、语音录入、确认保存页面已补充 VoiceOver 标签、选中态和按钮提示。
- 目前完成的是主要交互路径可读性与可操作性，不等同于完整辅助功能审核完成；仍需真机 VoiceOver / 大字号 / Switch Control 点验。

### 2.3 Shortcuts / App Intents

- 新增 `AddTransactionIntent`：通过快捷指令手动新增一笔账单，直接写入本地 SQLite，并刷新 Widget。
- 新增 `ParseLedgerTextIntent`：输入账单文本后调用本地解析器，返回结构化摘要。
- 新增 `OpenQuickAddIntent`：从快捷指令打开 App 内快速记账入口。
- 三个 Intent 已注册到 `AutoLedgerShortcuts`，与既有截图 / 语音入口并存。

### 2.4 月报历史月份浏览

- 月报页支持按月份前后翻页查看历史数据。
- 分类占比、Top 商户、6 个月趋势会随选中月份重新计算。
- 异常消费提醒仅保留在当前月，查看历史月份时自动隐藏，避免误导。
- 切换月份时会清空当前分类选中状态，减少跨月筛选残留。

### 2.5 解析与分类质量修复

- 修复微信支付详情页缺少“商户全称”时，把 `• 交易详情` 误识别为商户的问题。
- 新增拼多多“先用后付”详情页 Golden Case，覆盖金额、商户、分类、来源。
- `拼多多` 已归入购物分类。
- 同步修复既有“羊汤 / 羊肉汤”餐饮分类残留。

### 2.6 历史账单批量整理

- 编辑单笔账单分类时，如果分类发生变化，会提示选择“仅保存本笔”或“刷新全部同商户账单分类”。
- `LedgerStore.updateTransaction` 支持按同一商户批量刷新分类，并写回 SQLite、刷新 Widget、触发自动备份。
- 设置页“商户别名”每条映射新增刷新按钮，可将所有现存账单中的原商户名更新为对应别名。

## 3. 本地化状态

### 3.1 Key 级别检查

| Bundle | zh-Hans | zh-Hant | en | 结论 |
|---|---:|---:|---:|---|
| 主 App `Localizable.strings` | 457 / 457 | 457 / 457 | 457 / 457 | key 集合一致 |
| Watch App `Localizable.strings` | 36 / 36 | 36 / 36 | 36 / 36 | key 集合一致 |
| ControlWidgetExtension | 5 / 5 | 5 / 5 | 5 / 5 | key 集合一致 |
| ShareExtension | 13 / 13 | 13 / 13 | 13 / 13 | key 集合一致 |

结论：主 App、Watch App、Widget、Share Extension 的 `.strings` 文件在简体中文、繁体中文、英文三套 key 上是对齐的。

### 3.2 已覆盖的 UI 文案

本轮已补齐 v1.4.x 用户可见主路径三语文案：

- Watch App 首页、最近账单、快速记账、语音记账、确认保存、同步反馈。
- 账本筛选、月份 / 年份导航、最近删除、删除 / 恢复确认。
- 月报历史月份、异常提醒、分类图表、Top 商户、趋势图和空状态。
- 分类批量刷新弹窗、商户别名单条刷新入口。
- 设置页消费分析、分类学习、数据管理、订阅管理。
- 问题反馈撰写、预览、附件说明、反馈邮件正文、发送 / 复制状态。
- OCR / iCloud 等用户可见错误提示。
- `AddTransactionIntent`、`ParseLedgerTextIntent`、`OpenQuickAddIntent` 的参数摘要与类型展示。

### 3.3 保留中文的非主路径内容

以下内容本轮不纳入 UI 全球化范围：

- DebugView、调试记录导出文本、开发者诊断字段。
- 日志、解析规则关键词、OCR label 匹配、LLM prompt / response 调试内容。
- Golden Case、回归脚本输出和内部文档。

发布判断：可以在 RN 中声明“用户可见主路径已补齐简体中文、繁体中文、英文 UI 文案”。不建议声明“所有内部调试与开发者工具文本也已完整本地化”。

## 4. 回归验证

最近一轮代码门禁结果：

- PASS：`find AutoLedger -path '*lproj/Localizable.strings' -print0 | xargs -0 plutil -lint`
- PASS：`bash scripts/run_offline_regression.sh`
- PASS：`xcodebuild -workspace AutoLedger/AutoLedger.xcworkspace -scheme AutoLedger -destination 'generic/platform=iOS' build`
- PASS：`git diff --check`

最近一次已推送提交：

- `062c05b Fix receipt parsing and bulk ledger refresh`

## 5. TestFlight RN 建议文案

本次更新让 AutoLedger 更容易随手记一笔：

- 新增 Apple Watch 伴侣体验，可在手表上查看今日支出、最近账单，并快速记录支出。
- 新增 Watch 语音记账与快速记账，记录后同步到 iPhone 本地账本。
- 新增快捷指令动作：手动记账、解析账单文字、打开快速记账。
- 月报支持查看历史月份，趋势、分类和商户排行会随月份切换。
- 优化 VoiceOver 与 Reduce Motion 体验。
- 优化微信支付详情页解析，修复部分商户名识别为页面标题的问题。
- 编辑分类和商户别名时，支持批量刷新历史账单。

## 6. 测试重点

1. Apple Watch：今日支出、最近账单、快速记账、语音记账、同步到 iPhone。
2. iPhone 账本：编辑分类后选择“仅保存本笔 / 刷新全部”是否符合预期。
3. 设置页商户别名：单条别名刷新是否只影响对应原商户名。
4. 月报：历史月份翻页、6 个月趋势、历史月异常提醒隐藏。
5. Shortcuts：手动记账、解析账单文字、打开快速记账。
6. 辅助功能：VoiceOver、Reduce Motion、大字号和 Watch 端按钮可读性。
7. 解析回归：微信支付 / 拼多多先用后付详情页不再识别为 `• 交易详情`。

## 7. 已知限制 / 发布风险

- DebugView 与调试记录导出文本仍以中文为主，不适合宣称开发者 / 回归工具完整三语本地化。
- Watch 真机端到端同步、听写输入、VoiceOver、大字号仍需实机点验。
- App Store 对外版本号仍按计划保持 v1.3.0；v1.4.x 是内部开发线，RN 对外发布时需避免混淆版本号。
- 当前 RN 草稿基于代码、迭代日志和本地静态检查整理，未新增真机多语言截图与审核材料。

## 8. 发布结论

- 功能结论：v1.4.x 主体功能已进入可测状态。
- 本地化结论：用户可见主路径的简体中文 / 繁体中文 / 英文 UI 文案已补齐；Debug / 调试导出和内部开发者文本仍保留中文。
- 建议：按本 RN 进入 TestFlight 内测；正式对外发布前补充真机多语言截图、Watch 同步、听写、VoiceOver 与大字号点验。
