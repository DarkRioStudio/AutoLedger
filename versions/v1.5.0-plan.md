# v1.5.0 开发线规划

更新日期：2026-06-01
文档状态：当前开发线，随开发进程持续校正
内部开发线：v1.5.0 / v1.5.x
App Store 对外版本：v1.4.0
承接版本：App Store v1.3.0 / 内部 v1.4.0 发布基准
关联设计：`versions/v1.4.0-plan.md`、`versions/v1.4.0-RELEASE(draft).md`、`AutoLedger_Watch_Design.md`

## 1. 版本定位

v1.5.0 是 AutoLedger 在内部 v1.4.0（对外 App Store v1.3.0）已过审发布之后，面向下一轮 App Store v1.4.0 的开发线。

当前项目版本已切换到：

| 项目 | 当前值 |
|------|--------|
| 内部开发线 | v1.5.0 / v1.5.x |
| App Store 对外版本 | v1.4.0 |
| 承接基准 | `v1.4.0` tag（App Store v1.3.0 已发布） |

本轮不只是继续补 OCR 规则，而是把 AutoLedger 从“手机上的截图记账工具”推进到“全平台本地优先账单工作流”：

1. Apple Watch 第一屏改为更有用的“今日支出”摘要。
2. Watch 向左滑第二屏显示当前账本关联的最近支出。
3. 设计并实现表盘小组件适配，让今日消费信息能在手表表盘上被快速查看。
4. 增加 iPad 端，把 iPad 定位为统计分析、批量导入、批量识别、数据清洗和多账本管理的主工作台。
5. iPad 端完善后，使用 Mac Catalyst 将工作台推进到 Mac 端，承载拖拽导入、CSV / JSON 导入导出、键盘快捷键、菜单栏和更大表格编辑区域。
6. 规划 tvOS 只读大屏看板，面向家庭场景展示本月支出、分类占比、最近趋势和年度 / 月度摘要。
7. 规划 visionOS 展示版本，面向空间看板、漂浮分类卡片、年度消费时间线墙和最近账单悬浮列表。
8. 继续优化识别链路，拓展更多真实账单场景。

## 1.1 版本号策略

v1.5.x 是内部开发线；App Store 对外发布版本是 v1.4.0。

开发过程中允许使用 v1.5.1、v1.5.2、v1.5.3 等内部阶段号拆分任务、生成 TestFlight 构建和记录回归节点。用户最终在 App Store 看到的是：

```text
AutoLedger v1.4.0
```

| 场景 | 版本规则 |
|------|----------|
| 内部开发文档 / Git Tag | v1.5.x |
| TestFlight | App Store 版本保持 1.4.0，Build Number 持续递增 |
| App Store | 对外发布 1.4.0 |
| 承接基准 | 内部 v1.4.0 tag / App Store v1.3.0 已发布版本 |
| 后续开发线 | 另起 v1.6.0 或后续版本计划 |

## 2. 产品目标

### 2.1 核心目标

- Watch 端从“可操作入口”升级为“随手看今日支出”：
  - 第一屏显示今日支出总金额。
  - 显示今日支出笔数。
  - 显示最近一笔商户。
  - 左滑第二屏显示当前账本关联的最近支出列表。
  - 保留快速记账和最近账单入口。

- 表盘小组件完成第一版适配：
  - 在表盘上显示今日支出金额或今日支出笔数。
  - 点击小组件进入 Watch App 今日支出页。
  - 支持隐私隐藏、空状态和数据过期提示。

- iPad 端完成第一版产品框架：
  - 统计分析。
  - 数据批量导入。
  - 批量识别。
  - 数据清洗整理。
  - 结构化成账单。
  - 多账本管理。

- Mac Catalyst 端作为 iPad 工作台稳定后的生产力延伸：
  - 支持 Finder 拖拽截图 / 文件导入。
  - 支持 CSV / JSON 导入导出。
  - 支持键盘快捷键和基础菜单栏。
  - 支持更大的表格 / 列表编辑区域。
  - 支持批量选择、批量修正和重复账单检查。

- tvOS 端定位为只读家庭大屏看板：
  - 本月支出总览。
  - 分类占比。
  - 最近消费趋势。
  - 年度 / 月度摘要。
  - 家庭大屏展示模式。

- visionOS 端定位为空间展示版本：
  - 月度支出空间看板。
  - 分类支出卡片漂浮展示。
  - 年度消费时间线墙。
  - 最近账单悬浮列表。

- 截图与发布资产管线覆盖 iPad：
  - 当前 App Store 截图管线只覆盖 iPhone 与 Apple Watch，不生成 iPad 截图。
  - v1.5.0 需要把 iPad 端纳入截图管线，覆盖关键页面与 App Store 发布资产。
  - iPad 首版 UI 需要从设计阶段就考虑截图可展示性。
  - iPhone、Watch、iPad 三端截图口径统一管理。

- 识别链路继续拓展：
  - 覆盖更多支付、订单、票据、订阅和跨场景账单。
  - 让“识别 → 复核 → 入账 → 清洗 → 统计”的链路更稳定。

### 2.2 非目标

v1.5.0 第一版规划不把 AutoLedger 扩展成完整财务系统。

本轮暂不包含：

- 服务端账户系统。
- 多人协作账本。
- 银行级资产负债管理。
- 预算、投资、贷款、发票报销等完整财务模块。
- 不经用户确认的历史账单大规模自动改写。
- 付费能力边界设计。
- Mac 原生 App 重写；Mac 首版优先走 Catalyst 评估。
- tvOS / visionOS 的编辑、导入、清洗或写入账本能力；两者首版只读展示。

## 3. Apple Watch 主线

### 3.1 第一屏：今日支出

Watch App 打开后的第一屏改为“今日支出”，让用户抬腕就能确认当天消费状态。

第一屏信息结构：

| 信息 | 展示要求 | 说明 |
|------|----------|------|
| 今日支出总金额 | 最大视觉层级 | 使用当前账本默认币种；无支出时显示 0 或空状态文案 |
| 今日支出笔数 | 次级指标 | 显示 `共 N 笔`，便于判断今天消费频率 |
| 最近一笔商户 | 次级摘要 | 优先显示商户名；无商户时显示分类或账单来源 |
| 同步状态 | 轻量提示 | 仅在数据过期、同步失败、等待 iPhone 时显示 |
| 操作入口 | 底部或卡片按钮 | 快速记账、语音记账、最近账单 |

基础状态：

- 空状态：今天还没有支出，保留快速记账入口。
- 加载状态：本地数据读取或等待 iPhone 回传时显示轻量占位。
- 离线状态：显示上次同步时间，允许继续本地暂存记账。
- 隐私状态：用户开启隐私隐藏时，金额显示为占位符，点按后进入 App 可查看。

执行状态（2026-06-01 / GOAL-1511）：首屏已切换为今日支出摘要。当前展示今日总金额、今日笔数、最近展示名、更新时间、语音记账和快速记账入口；隐私隐藏开关尚未实现。

### 3.2 左滑第二屏：账本关联最近支出

Watch 今日支出页向左滑进入第二屏，显示当前账本关联的最近支出。

第二屏信息结构：

| 信息 | 展示要求 | 说明 |
|------|----------|------|
| 当前账本 | 顶部轻量标识 | 显示默认账本或当前选中账本名称 |
| 最近支出列表 | 主体内容 | 展示最近 3～5 笔支出，按交易时间倒序 |
| 单笔摘要 | 金额 + 商户 / 分类 | 空商户时用分类兜底 |
| 时间提示 | 今天 / 昨天 / 日期 | Watch 小屏上避免完整时间过长 |
| 详情入口 | 点按单笔进入详情 | 第一版只读详情，复杂编辑留给 iPhone / iPad |

数据口径：

- 只展示当前账本关联的支出记录。
- 默认使用用户当前默认账本；多账本上线后，可跟随 iPhone / iPad 最近使用账本。
- 如果账本没有最近支出，显示空状态并保留快速记账入口。
- Watch 上不做账本切换的复杂管理，最多提供轻量账本名称提示。

交互原则：

- 第一屏负责“今天花了多少”，第二屏负责“最近花在哪里”。
- 左右滑动不应隐藏核心操作，快速记账入口仍需容易到达。
- 第二屏列表不承载批量清洗、分类学习、账本移动等重操作。

执行状态（2026-06-01 / GOAL-1511）：为避免首屏改造后丢失旧列表能力，最近 5 笔支出已保留为 Watch 左滑第二页。

执行状态（2026-06-01 / GOAL-1512）：第二页已补齐当前账本提示、今天 / 昨天 / 日期短时间文案和单笔只读详情。详情页展示金额、商户、分类、来源、时间和备注，不提供编辑、删除或账本移动。

### 3.3 默认账本与今日支出口径（GOAL-1501）

今日支出应按用户本地日历日统计，不按 UTC 自然日统计。该口径同时服务 Watch 首屏、Watch 表盘小组件、iPad 总览、Mac Catalyst 表格摘要、tvOS 大屏看板和 visionOS 空间看板。

#### 3.3.1 默认账本口径

当前工程还没有 `Ledger` 模型，也没有 `Transaction.ledgerId`。在多账本上线前，系统使用一个虚拟默认账本承载所有正式账单：

| 项目 | v1.5.0 当前口径 | 多账本上线后的迁移口径 |
|------|------------------|------------------------|
| 默认账本 ID | `default-local-ledger`（文档与迁移约定，不要求当前代码立即新增字段） | 旧账单迁移时统一写入该默认账本，后续可重命名但 ID 保持稳定 |
| 默认账本显示名 | `本地账本` / `Local Ledger` | 用户可改名；Watch / Widget 无复杂切换时继续跟随默认账本 |
| 当前账本 | iPad / Mac 可有显式选中账本；未选择时回退默认账本 | 选中账本用于工作台筛选，默认账本用于轻量展示和新账单默认归属 |
| 全部账本 | 当前等同于默认账本 | 多账本后作为聚合统计范围，必须与单账本统计明确区分 |

默认规则：

- 当前所有 `store.transactions` 中的活跃账单都视为属于默认账本。
- `deletedTransactions` 不参与默认账本活跃统计，但备份和最近删除视图仍保留。
- Raw Input、Candidate、Rejected 等候选状态不属于正式账本，确认入账前不参与默认账本统计。
- Watch / Widget / tvOS / visionOS 首版默认展示默认账本，不跟随 iPad / Mac 的临时筛选状态。
- iPad / Mac 工作台可以显示当前选中账本；若没有显式选择，则显示默认账本。

#### 3.3.2 今日支出统计范围

今日支出只统计“正式、活跃、支出型”的账单：

| 项目 | 是否计入今日支出 | 说明 |
|------|------------------|------|
| 正式账单 `Transaction` | 是 | 当前模型里的账单均视为支出账单，前提是金额大于 0 |
| `amount > 0` | 是 | 当前 App 只保存支出；未来支持收入 / 退款后需新增类型字段 |
| `amount == 0` 或负数 | 否 | 视为异常或未来退款 / 调整，不进入今日支出 |
| 已删除账单 | 否 | 软删除后从活跃统计中排除 |
| 候选账单 | 否 | 用户确认前不污染统计 |
| 被标记疑似重复但仍为正式账单 | 是 | 只提示风险，不静默排除；用户确认删除 / 合并后再变化 |
| 订阅元数据 | 否 | 只有已经生成正式 `Transaction` 的订阅扣费才计入 |
| 手动 / Watch / 语音 / 快捷指令 / Share Extension 入账 | 是 | 只要进入正式账本并满足日期与金额条件，来源不影响统计 |

#### 3.3.3 日期边界

今日支出使用用户当前本地日历日：

- 日区间为 `[localStartOfDay, nextLocalStartOfDay)`。
- 日历使用用户设备当前 calendar / timezone；不能使用 UTC 自然日。
- 账单以 `occurredAt` 为准，不以导入时间、识别时间或备份时间为准。
- 未来如果保存 `createdAt` / `updatedAt` 到模型层，仍只作为排序兜底，不改变今日归属。
- 夏令时或时区变化时，以设备当前本地 calendar 算出的日期区间为准。

#### 3.3.4 最近支出口径

Watch 第二屏、iPad 总览和展示平台的“最近支出”使用同一排序：

1. 仅取当前统计范围内的正式活跃账单。
2. 按 `occurredAt` 倒序。
3. 若未来模型提供 `createdAt` / `updatedAt`，同一 `occurredAt` 下再按创建 / 更新时间倒序。
4. 商户为空时用分类标题兜底；分类也不可读时显示“待确认”。

#### 3.3.5 币种与金额

当前 `Transaction` 没有币种字段，`AppFormatters.currency` 固定按 CNY / `¥` 展示。因此 v1.5.0 今日支出暂定：

- 所有正式账单金额按当前默认币种显示。
- 多币种 OCR 可以进入备注或候选 warning，但确认入账前必须让用户明确最终金额。
- 未做汇率换算前，不在总览中混合显示不同币种金额。
- 后续若新增 `currencyCode` / `originalAmount` / `exchangeRate`，默认账本统计需明确“本币合计”和“原币展示”的关系。

#### 3.3.6 测试用例设计

GOAL-1510 实现今日支出数据服务时，至少补齐以下离线用例：

| Case | 输入 | 期望 |
|------|------|------|
| today_active_expense_included | 今天 2 笔正金额正式账单 | 总金额为两笔合计，笔数为 2 |
| yesterday_expense_excluded | 昨天 1 笔正金额账单 | 今日支出不包含该笔 |
| deleted_expense_excluded | 今天 1 笔已删除账单 | 今日支出不包含该笔 |
| candidate_excluded | 今天 1 条候选账单 | 今日支出不包含该候选 |
| zero_or_negative_excluded | 今天 0 或负数金额 | 今日支出不包含该笔 |
| custom_category_source_included | 今天 1 笔自定义分类 / 来源账单 | 今日支出包含，分类和来源原样显示 |
| timezone_boundary | 本地 00:00 前后各 1 笔 | 只包含本地当天区间内账单 |
| default_ledger_scope | 多账本迁移后默认账本与全部账本各有数据 | Watch / Widget 默认取默认账本，iPad / Mac 可显示全部账本 |

#### 3.3.7 今日支出数据服务（GOAL-1510）

GOAL-1510 已在 `AutoLedgerCore` 中新增 `TodaySpendingSummary`，作为 Watch 首屏、Watch 表盘小组件、iPad 总览、Mac Catalyst 表格摘要、tvOS 和 visionOS 展示平台的共同数据口径。

服务输出：

| 字段 | 说明 |
|------|------|
| `ledgerID` | 当前为 `default-local-ledger` |
| `ledgerName` | 当前为 `本地账本` |
| `dayInterval` | 按传入 calendar 计算的本地日区间 |
| `totalExpense` | 今日正金额正式账单合计 |
| `transactionCount` | 今日正金额正式账单笔数 |
| `recentTransaction` | 今日最近一笔正式支出 |
| `recentDisplayName` | 最近一笔展示名，优先商户，商户为空时回退分类 / 来源 |
| `isEmpty` | 今日无有效支出时为 true |

服务输入契约：

- 输入数组必须是“活跃正式账单”。当前由 `LedgerStore.transactions` 提供，不传入 `deletedTransactions`。
- 候选账单、raw input、rejected、debug events 不进入该服务输入。
- 当前没有 `ledgerId` 字段，因此所有输入账单暂视为默认账本。
- 多账本上线后，调用方负责按默认账本、当前账本或全部账本过滤后再调用 summary。

过滤规则：

- 只统计 `amount > 0`。
- 只统计 `occurredAt` 落在 `[localStartOfDay, nextLocalStartOfDay)` 的账单。
- 日期区间由注入的 `Calendar` 计算；默认使用设备当前 calendar。
- 最近支出按 `occurredAt` 倒序；当前模型没有 `createdAt` / `updatedAt`，后续新增字段后可补充二级排序。

App 层接入：

- `LedgerStore.todaySpendingSummary` 现在返回 `TodaySpendingSummary.build(from: transactions, referenceDate: .now)`。
- 本轮不改 Watch UI、不改 Widget、不改 WatchConnectivity payload；GOAL-1511 / GOAL-1520 后续复用该 summary。

### 3.4 Watch 交互原则

- Watch 上只做轻量查看和快速记录，不做复杂账单清洗。
- 今日支出页优先保证一眼读懂，不堆图表。
- 编辑、批量操作、数据清洗统一引导到 iPhone / iPad。
- VoiceOver、大字号、减动、增强对比度必须在主路径可用。

## 4. 表盘小组件适配

### 4.1 第一版范围

表盘小组件先围绕“今日支出”做最小可用集：

| Widget Family | 第一版内容 | 备注 |
|----------------|------------|------|
| Accessory Inline | `今日支出 ¥88` | 适合文字型表盘区域 |
| Accessory Circular | 金额缩写或笔数 | 空间不足时显示 `¥88` / `3笔` |
| Accessory Rectangular | 今日金额 + 笔数 | 可附带最近商户一行 |
| Accessory Corner | 金额缩写 | 视实现成本决定是否进入首版 |

执行状态（2026-06-01 / GOAL-1521A）：现有 `DailyExpenseWidget` 已补齐 `.accessoryInline`、`.accessoryCircular`、`.accessoryRectangular` 三种 accessory family UI，并复用今日支出数据口径。当前工程仍没有独立 watchOS WidgetKit complication target，因此 Apple Watch 表盘上线需要后续 GOAL-1521B 专门处理 target、embedding、bundle id、entitlements、signing 和 Xcode Cloud 验证。

### 4.2 更新时间与隐私

- 小组件不追求秒级刷新，按 WidgetKit / watchOS timeline 能力做合理刷新。
- 新增账单保存后尽量触发刷新，但不能依赖实时刷新作为唯一正确性来源。
- 默认显示金额；如果后续增加隐私开关，小组件应支持金额隐藏。
- 当数据超过一定时间未同步时，显示轻量过期提示或回退到最后一次有效数据。

### 4.2.1 iPhone 桌面 / 负一屏小组件口径（GOAL-1520）

当前工程已有 `AutoLedgerWidgetsExtension`，其中 `DailyExpenseWidget` 是 iPhone 桌面小组件，也可作为 iPhone 负一屏 / Today View 小组件添加。GOAL-1520 将它纳入与 Watch 今日支出相同的数据口径。

本轮完成：

- `DailyExpenseWidget` 继续从 App Group SQLite 只读加载数据，不新增网络或服务端依赖。
- 今日支出使用设备当前 calendar 的 `[localStartOfDay, nextLocalStartOfDay)`。
- 今日支出只统计 `deleted_at IS NULL` 且 `amount > 0` 的正式账单。
- 日期查询格式对齐 `SQLiteTransactionStore` 当前 ISO8601 存储格式，并保留旧 `yyyy-MM-dd HH:mm:ss` 兼容解析。
- 最近一笔展示名按商户 -> 分类 -> 来源回退，避免商户为空时小组件直接空白。
- 新增 / 编辑 / 删除 / 备份恢复后，主 App 现有 `WidgetCenter.shared.reloadAllTimelines()` 仍会刷新桌面与负一屏小组件。

本轮未做：

- 未新增 Watch complication / 表盘小组件 target。
- 未把 `AutoLedgerWidgetsExtension` 强行改为依赖 `AutoLedgerCore`，避免本轮改动 Xcode target 依赖关系；当前通过同一口径规则保持一致。
- 未新增隐私隐藏开关。
- 未新增 Widget 截图管线。

### 4.3 验收标准

- 表盘小组件能显示今日支出金额。
- 点击小组件能打开 Watch App 今日支出页。
- 今日无支出、同步失败、离线、隐私隐藏都有明确状态。
- 简体中文、繁体中文、英文文案可本地化。

### 4.4 表盘小组件 target 缺口（GOAL-1521B）

GOAL-1521A 已完成 accessory UI，但它仍运行在现有 iPhoneOS `AutoLedgerWidgetsExtension` 中。要让 Apple Watch 表盘真正可添加，需要新增或调整 watchOS WidgetKit extension target，并嵌入 Watch App。

后续 GOAL-1521B 需要单独处理：

- 新增 watchOS WidgetKit extension target。
- 设置独立 Bundle Identifier，不能修改现有主 App / Watch App Bundle ID。
- 复用 App Group entitlements，确保只读访问同一 SQLite 或后续共享快照。
- 将 extension 正确嵌入 `AutoLedgerWatch Watch App`。
- 保留现有 `AutoLedgerWidgetsExtension` 的 iPhone 桌面 / 负一屏 / 锁屏能力。
- 分别验证：
  - `xcodebuild -workspace AutoLedger/AutoLedger.xcworkspace -scheme AutoLedgerWidgetsExtension -configuration Debug -destination 'generic/platform=iOS' build`
  - `xcodebuild -workspace AutoLedger/AutoLedger.xcworkspace -scheme 'AutoLedgerWatch Watch App' -configuration Debug -destination 'generic/platform=watchOS' build`
  - `xcodebuild -workspace AutoLedger/AutoLedger.xcworkspace -scheme AutoLedger -configuration Debug -destination 'generic/platform=iOS' build`

在 GOAL-1521B 完成前，不应在发布说明中声称 Apple Watch 表盘小组件已经上线。
- 小尺寸表盘区域不出现文字截断或金额不可读。

## 5. iPad 主线

### 5.1 产品定位

iPad 端不是简单放大 iPhone UI，而是 AutoLedger 的“账单整理工作台”。

用户在 iPhone / Watch 上负责快速记录，在 iPad 上完成更重的整理工作：

1. 批量导入截图、图片、文件或文本。
2. 批量识别候选账单。
3. 复核、清洗、合并、去重和分类。
4. 结构化为正式账单。
5. 按账本、月份、分类、商户做统计分析。

### 5.2 信息架构

建议第一版采用 iPad 常见的三段式结构：

| 区域 | 内容 | 目标 |
|------|------|------|
| 左侧边栏 | 账本、月份、导入箱、清洗任务、统计入口 | 快速切换工作上下文 |
| 中间列表 / 表格 | 账单、候选记录、批量识别结果 | 支持批量选择和排序筛选 |
| 右侧详情 / 检查器 | 识别字段、原图预览、清洗建议、账单详情 | 让复核和修正不离开当前页面 |

### 5.3 统计分析

iPad 统计分析优先服务“看懂钱花到哪里”：

- 月度支出总览：总金额、笔数、日均支出、环比变化。
- 分类分析：分类金额、占比、趋势变化。
- 商户分析：Top 商户、重复消费、订阅类消费。
- 账本分析：个人、家庭、旅行、工作等账本维度的对比。
- 时间分析：日历热力、周内分布、月内峰值。
- 异常提示：大额、重复、疑似误识别、分类异常。

第一版不追求复杂图表堆叠，优先保证筛选、钻取和数据可信。

### 5.4 数据批量导入

第一版导入场景：

- 从系统照片选择多张账单截图。
- 从 Files 拖入图片、PDF 或文本文件。
- 从剪贴板粘贴多条账单文本。
- 从分享入口进入 AutoLedger 导入箱。

导入后先进入“候选区”，不直接写入正式账本。

候选区字段：

- 来源类型。
- 原始文件名或截图来源。
- 识别状态。
- 识别置信度。
- 候选金额、商户、时间、分类。
- 是否疑似重复。
- 是否需要人工复核。

### 5.5 批量识别

批量识别采用队列模型：

1. 导入文件进入队列。
2. 逐项 OCR / 文本解析。
3. 生成候选账单。
4. 标记置信度和异常原因。
5. 用户批量确认或逐条修正。

队列需要支持：

- 暂停 / 继续。
- 失败重试。
- 识别结果重新解析。
- 按状态筛选：待识别、待复核、可入账、失败、疑似重复。
- 大批量导入时不阻塞 UI。

### 5.6 数据清洗整理

数据清洗是 iPad 端的重点能力。

第一版清洗项：

- 商户统一：把不同写法归并为同一商户。
- 分类修正：批量修改同类商户或同类记录的分类。
- 重复检测：按金额、时间、商户、来源相似度提示疑似重复。
- 字段补全：时间、商户、分类、备注缺失时提示补齐。
- 金额异常：识别金额与文本上下文冲突时标记。
- 来源整理：记录账单来自截图、文本、手动、Watch、快捷指令等路径。

所有批量清洗动作都必须有确认步骤，并尽量提供撤销或可回滚路径。

### 5.7 结构化成账单

候选记录进入正式账本前，应经过结构化确认：

| 阶段 | 状态 | 说明 |
|------|------|------|
| Raw Input | 原始输入 | 图片、PDF、文本、剪贴板内容 |
| Candidate | 候选账单 | OCR / 文本解析后的结构化草稿 |
| Reviewed | 已复核 | 用户确认字段正确 |
| Transaction | 正式账单 | 写入账本并进入统计 |
| Rejected | 已忽略 | 非账单或用户放弃 |

第一版可先在 UI 和存储层建立清晰状态，后续再逐步增强自动化。

### 5.7.1 候选账单状态模型（GOAL-1502）

候选账单模型是 iPad / Mac 批量导入、批量识别、数据清洗和重复检查的共同基础。它不替代当前 `Transaction`，也不能在用户确认前污染正式账本统计。

#### 5.7.1.1 状态流

第一版状态流保持线性、可追溯、可回滚：

```text
Raw Input -> Candidate -> Reviewed -> Transaction
Raw Input -> Rejected
Candidate -> Rejected
Candidate -> Candidate (retry / reparse)
Reviewed -> Candidate (commit failed or user reopens review)
```

| 状态 | 含义 | 是否进入正式账本 | 是否进入统计 | 典型入口 |
|------|------|------------------|--------------|----------|
| `rawInput` | 已导入原始图片、PDF、文本、剪贴板内容或分享内容，尚未产生可信结构化草稿 | 否 | 否 | iPad / Mac 批量导入、剪贴板、Share Extension、快捷指令 |
| `candidate` | OCR / 文本解析后得到结构化草稿，但字段仍需用户复核 | 否 | 否 | 批量识别、重新解析、低置信解析 |
| `reviewed` | 用户已确认字段，可准备写入指定账本 | 否 | 否 | 单条确认、批量确认后的暂存阶段 |
| `transaction` | 已成功写入正式 `Transaction`，进入活跃账本 | 是 | 是 | 保存成功后的目标状态 |
| `rejected` | 用户忽略、非账单、识别失败且不再处理 | 否 | 否 | 非账单图片、重复项、用户放弃 |

状态约束：

- 只有 `transaction` 状态能写入当前 `transactions` 表并进入 Watch / Widget / iPad / Mac / tvOS / visionOS 统计。
- `rawInput`、`candidate`、`reviewed`、`rejected` 均不计入今日支出、月报或 Top 商户。
- `reviewed` 是入账前的事务性暂存状态；如果提交失败，应保留用户修正字段并回到 `candidate` 或继续显示为待提交。
- 重新识别不创建正式账单，只更新候选记录的解析版本、置信度、warning 和失败原因。
- `rejected` 仍应保留最小审计信息，便于撤销忽略、排查批量导入问题和避免重复导入。

#### 5.7.1.2 最小字段草案

GOAL-1502 只定义字段契约，不要求本轮写 SQLite schema。后续 GOAL-1503 再决定字段落库、索引、迁移和 BackupBundle 版本。

| 字段组 | 字段 | 说明 |
|--------|------|------|
| 身份与批次 | `id`、`batchId`、`createdAt`、`updatedAt` | 候选记录唯一标识、导入批次和排序追踪 |
| 原始输入 | `sourceKind`、`originalFileName`、`originalUTType`、`inputHash`、`rawText`、`thumbnailRef`、`fileRef` | 记录来源、文件类型、去重 hash、OCR / 文本结果和本地文件引用 |
| 解析草稿 | `merchant`、`amount`、`occurredAt`、`category`、`source`、`note` | 对齐当前 `Transaction` 的核心字段 |
| 扩展草稿 | `currencyCode`、`originalAmount`、`ledgerId` | 当前可为空；为多币种和多账本预留 |
| 状态与质量 | `state`、`confidence`、`needsReview`、`warnings`、`failureReason` | 驱动候选列表筛选、复核优先级和失败重试 |
| 重复提示 | `duplicateGroupId`、`duplicateScore`、`duplicateReason`、`possibleDuplicateTransactionId` | 只提示，不自动删除或静默跳过 |
| 复核与入账 | `reviewedAt`、`targetLedgerId`、`convertedTransactionId` | 记录确认时间、目标账本和成功转入的正式账单 ID |
| 隐私与清理 | `rawContentPurgedAt`、`retentionPolicy` | 允许用户清理原图 / 原文，同时保留必要状态 |

隐私约束：

- 原始图片、OCR 全文、PDF 文本和支付截图可能包含个人财务信息，不能进入公开 fixtures、公开 issue 或示例仓库数据。
- Debug / backup / export 必须区分“正式账单字段”和“原始敏感输入”；后续备份升级应明确是否包含原始输入。
- 如果用户清理原始输入，候选记录可以保留安全摘要、状态和已确认字段，但不能继续暴露原图或 OCR 全文。

#### 5.7.1.3 失败原因

第一版失败原因建议使用稳定枚举，避免 UI 和回归脚本依赖任意字符串：

| Reason | 场景 | 默认处理 |
|--------|------|----------|
| `emptyInput` | 文件或文本为空 | 标记失败，允许删除 |
| `ocrFailed` | 图片 OCR 失败 | 允许重试或手动输入 |
| `nonBillImage` | 缺少账单信号 | 标记为已忽略建议 |
| `missingAmount` | 没有可信金额 | 进入候选，强制复核 |
| `missingMerchant` | 没有可信商户 | 进入候选，商户字段待补全 |
| `missingDate` | 没有可信日期 | 进入候选，默认不自动入账 |
| `lowConfidence` | 解析置信度低 | 进入候选并置 `needsReview = true` |
| `multipleReceipts` | 一张图可能包含多笔账单 | 拆分前不写入正式账本 |
| `duplicateSuspected` | 疑似与已有账单或候选重复 | 提示用户确认，不自动删除 |
| `unsupportedFileType` | 不支持的文件类型 | 标记失败并提示支持范围 |
| `parseFailed` | 解析器异常或无草稿 | 保留 raw input，允许重试 |
| `userRejected` | 用户主动忽略 | 转为 `rejected` |
| `permissionDenied` | 文件 / 照片权限不足 | 停留 raw input 或失败状态 |

#### 5.7.1.4 置信度与复核策略

当前工程已有 `ImportedReceipt.confidence`、`InterpretResult.confidence`、`needsReview` 和 `warnings` 等概念。候选模型应收敛这些信号，统一驱动 iPad / Mac 复核工作台：

| 置信度 | 建议阈值 | 默认行为 |
|--------|----------|----------|
| High | `confidence >= 0.80` | 可以预选为“可确认”，但批量入账前仍需用户确认 |
| Medium | `0.50 <= confidence < 0.80` | 进入候选区，要求复核关键字段 |
| Low | `confidence < 0.50` | 保留候选或 raw input，显示失败原因和重试入口 |

规则：

- 批量导入场景不做无确认自动入账，即使置信度高也只进入可确认队列。
- `needsReview = true` 时，UI 必须突出显示待确认字段。
- 多币种、疑似重复、缺日期、缺商户、金额冲突等 warning 优先级高于单纯高置信度。
- 用户手动修正后的字段应覆盖解析草稿，并记录 `reviewedAt`。

#### 5.7.1.5 重复提示策略

重复检测只负责提示，不负责静默删除。

候选账单应同时与两类对象比较：

1. 活跃正式账单：避免用户把同一截图再次入账。
2. 同批或历史候选账单：避免批量导入重复图片、重复 PDF 页或同一文本多次进入队列。

第一版比较因子：

- `inputHash` 完全一致。
- 金额相同或非常接近。
- `occurredAt` 在同一天或短时间窗口内。
- 商户相同、别名相同或文本相似。
- OCR / raw text 相似度高。
- 来源文件名或图片元数据高度相似。

重复提示落地规则：

- 疑似重复候选设置 `duplicateScore`、`duplicateReason` 和 `needsReview = true`。
- 不自动把候选转成 `rejected`，除非用户确认忽略。
- 不自动删除已有正式账单。
- 如果用户确认仍然入账，该正式账单照常进入统计，后续由数据清洗 / 合并能力处理。

#### 5.7.1.6 后续落库与备份边界

后续 GOAL-1503 评估 schema 时，建议至少考虑：

- `import_batches`：批量导入会话、来源、创建时间、完成状态。
- `raw_inputs`：原始文件 / 文本引用、hash、OCR 文本、权限和清理状态。
- `candidate_transactions`：候选字段、状态、置信度、失败原因、重复提示和目标账本。
- `candidate_events`：重试、修正、忽略、确认、入账失败、入账成功等审计事件。

BackupBundle 需要谨慎升级：

- `schemaVersion = 1` 当前只覆盖正式账单与删除状态，不应强行塞入候选队列。
- 如果 v1.5.x 要备份候选区，应升级到 schema v2，并让用户知道是否包含原始敏感输入。
- 导出 CSV 默认只导出正式账单；JSON backup 可以选择是否包含候选队列。

#### 5.7.1.7 测试用例设计

后续实现候选模型、队列和持久化时，至少补齐：

| Case | 输入 | 期望 |
|------|------|------|
| raw_text_creates_raw_input | 粘贴一段账单文本 | 生成 `rawInput`，不进入统计 |
| ocr_failure_preserves_raw | OCR 失败图片 | 保留 raw input、失败原因为 `ocrFailed` |
| non_bill_rejected | 非账单图片 | 可转 `rejected`，不进入正式账本 |
| missing_amount_candidate | 缺金额文本 | 生成 candidate，`needsReview = true` |
| high_confidence_candidate_not_auto_saved | 高置信账单 | 进入可确认候选，不自动入账 |
| duplicate_suspected_not_deleted | 与已有账单相似 | 标记重复提示，不删除候选或正式账单 |
| reviewed_then_transaction | 用户确认字段并保存 | 先 reviewed，再生成正式 `Transaction` |
| rejected_excluded_from_stats | 用户忽略候选 | 不进入今日 / 月度统计 |
| purge_raw_keeps_summary | 用户清理原始输入 | 原图 / 原文不可见，状态摘要保留 |

### 5.8 多账本管理

多账本用于把不同生活场景分开：

- 个人账本。
- 家庭账本。
- 旅行账本。
- 工作 / 报销账本。
- 临时项目账本。

第一版能力：

- 创建、重命名、归档账本。
- 设置默认账本。
- 导入时选择目标账本。
- 账单可在账本间移动。
- 统计分析可按单账本或全部账本查看。

暂不做多人协作和服务端同步。

### 5.9 SQLite / BackupBundle 迁移方案（GOAL-1503）

GOAL-1503 只输出 schema 缺口与迁移方案，不在本轮修改数据库、模型或备份代码。核心原则是：现有正式账单继续可读可写；旧数据自动归入默认账本；候选区独立于正式账单；备份恢复必须兼容现有 schema v1。

#### 5.9.1 当前 schema 事实

当前 `SQLiteTransactionStore` 初始化时使用 `CREATE TABLE IF NOT EXISTS` 加少量列存在性迁移，没有集中式 schema version 表。

| 对象 | 当前状态 | 缺口 |
|------|----------|------|
| `Transaction` | `id`、`merchant`、`amount`、`occurredAt`、`category`、`source`、`note` | 无 `ledgerId`、`currencyCode`、`transactionType`、`createdAt`、`updatedAt` 模型字段 |
| `transactions` 表 | `id`、`merchant`、`amount`、`occurred_at`、`category`、`source`、`note`、`created_at`、`updated_at`、`deleted_at` | 无 `ledger_id`、`currency_code`、`transaction_type`、候选来源、重复组或清洗历史 |
| `debug_events` 表 | 保存 OCR / LLM 调试事件、raw text、解析结果、交易 ID、LLM metadata | 不是候选队列；不能直接承担 iPad / Mac 批量复核状态 |
| `subscriptions` 表 | 保存订阅识别结果 | 与多账本、候选入账、交易归属尚未关联 |
| `category_corrections` 表 | merchant -> 内置 `TransactionCategory` | 不能表达自定义分类的持久化关系和多账本作用域 |
| `merchant_aliases` 表 | original -> alias | 可复用，但清洗动作历史尚未记录 |
| 自定义分类 / 来源 | 主要在 UserDefaults，BackupBundle 会导出数组 | 不在 SQLite，未来 iPad / Mac / 多平台工作台需要统一持久化 |
| `BackupBundle` | `schemaVersion = 1`，validator 只接受 1 | 不覆盖 ledgers、candidate queue、raw inputs、cleaning actions、transaction v2 fields |

#### 5.9.2 迁移总原则

- 迁移必须是 additive first：先加表、加可空列或有默认值的列，不重建现有正式账单表。
- 旧正式账单迁移后全部写入 `default-local-ledger`，不改变 ID、金额、商户、日期、分类、来源和备注。
- 候选账单不得复用 `transactions` 表表达未确认状态，避免污染今日支出、月报、Watch、Widget 和展示平台。
- BackupBundle v1 必须继续可导入；导入 v1 时自动映射到默认账本。
- BackupBundle v2 导出前必须明确是否包含候选区和原始敏感输入。
- Xcode Cloud / App Store 发布链不依赖本地数据库 fixture；schema 迁移不得修改 Bundle ID、signing、entitlements、App Group 或 iCloud Container。

#### 5.9.3 schema version 策略

当前迁移依赖列存在性判断，短期可以继续兼容；进入多账本与候选队列前，建议补一个明确版本机制：

| 方案 | 建议 |
|------|------|
| `PRAGMA user_version` | 推荐用于本地 SQLite schema 主版本，轻量、无需新表 |
| `schema_migrations` 表 | 后续如果 migration 很多，可记录每次 migration id、执行时间和结果 |
| 列存在性迁移 | 保留用于兼容历史小迁移，但不再作为复杂 schema 的唯一依据 |

建议迁移顺序：

1. 读取 `PRAGMA user_version`；旧库视为 `0`。
2. 在 transaction 内逐步执行 v1、v2、v3 迁移。
3. 每步成功后更新 `user_version`。
4. 任何一步失败都 rollback，保持旧库可继续打开。

#### 5.9.4 多账本 schema 草案

新增 `ledgers` 表：

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | TEXT PRIMARY KEY | 默认账本固定为 `default-local-ledger` |
| `name` | TEXT NOT NULL | 默认 `本地账本` |
| `is_default` | INTEGER NOT NULL DEFAULT 0 | 只能有一个默认账本 |
| `archived_at` | TEXT NULL | 归档后不作为新账单默认目标 |
| `created_at` | TEXT NOT NULL | 创建时间 |
| `updated_at` | TEXT NOT NULL | 更新时间 |

扩展 `transactions` 表：

| 字段 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `ledger_id` | TEXT NOT NULL | `default-local-ledger` | 旧账单统一归入默认账本 |
| `transaction_type` | TEXT NOT NULL | `expense` | 为未来收入 / 退款 / 调整预留 |
| `currency_code` | TEXT NOT NULL | `CNY` | 对齐当前 `¥` 展示事实 |
| `original_amount` | REAL NULL | NULL | 多币种原币金额，后续再启用 |
| `exchange_rate` | REAL NULL | NULL | 汇率，后续再启用 |

推荐索引：

- `idx_transactions_ledger_occurred_at`：`ledger_id, occurred_at DESC`
- `idx_transactions_deleted_at`：`deleted_at`
- `idx_transactions_merchant_date_amount`：`merchant, occurred_at, amount`

正式账单查询口径：

- `loadTransactions()` 默认继续只取 `deleted_at IS NULL`。
- Watch / Widget 默认额外限定 `ledger_id = default-local-ledger`。
- iPad / Mac 工作台可按单账本或全部账本查询。
- 旧 `Transaction` Swift struct 可以先保持不变；GOAL-1560 再决定是否引入 `TransactionV2` 或给 `Transaction` 添加字段。

#### 5.9.5 候选区 schema 草案

候选区建议独立表，不进入正式账单表。

`import_batches`：

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | TEXT PRIMARY KEY | 批次 ID |
| `source_kind` | TEXT NOT NULL | photos / files / clipboard / share / shortcut / camera |
| `status` | TEXT NOT NULL | pending / running / completed / failed / cancelled |
| `item_count` | INTEGER NOT NULL DEFAULT 0 | 批次项目数 |
| `created_at` | TEXT NOT NULL | 创建时间 |
| `updated_at` | TEXT NOT NULL | 更新时间 |

`raw_inputs`：

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | TEXT PRIMARY KEY | 原始输入 ID |
| `batch_id` | TEXT NULL | 关联导入批次 |
| `source_kind` | TEXT NOT NULL | 输入来源 |
| `original_file_name` | TEXT NULL | 文件名 |
| `original_uttype` | TEXT NULL | 文件类型 |
| `input_hash` | TEXT NULL | 去重 hash |
| `file_ref` | TEXT NULL | 本地文件引用，不建议直接存大 blob |
| `thumbnail_ref` | TEXT NULL | 缩略图引用 |
| `raw_text` | TEXT NULL | OCR / 文本内容，敏感 |
| `raw_content_purged_at` | TEXT NULL | 用户清理原始内容时间 |
| `created_at` | TEXT NOT NULL | 创建时间 |
| `updated_at` | TEXT NOT NULL | 更新时间 |

`candidate_transactions`：

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | TEXT PRIMARY KEY | 候选账单 ID |
| `raw_input_id` | TEXT NULL | 关联原始输入 |
| `batch_id` | TEXT NULL | 关联批次 |
| `state` | TEXT NOT NULL | rawInput / candidate / reviewed / transaction / rejected |
| `merchant` | TEXT NULL | 候选商户 |
| `amount` | REAL NULL | 候选金额 |
| `occurred_at` | TEXT NULL | 候选交易时间 |
| `category` | TEXT NULL | 候选分类 |
| `source` | TEXT NULL | 候选来源 |
| `note` | TEXT NOT NULL DEFAULT '' | 备注 |
| `ledger_id` | TEXT NULL | 目标账本，默认可为空直到确认 |
| `currency_code` | TEXT NULL | 候选币种 |
| `confidence` | REAL NOT NULL DEFAULT 0 | 置信度 |
| `needs_review` | INTEGER NOT NULL DEFAULT 1 | 是否需要复核 |
| `warnings_json` | TEXT NOT NULL DEFAULT '[]' | warning 列表 |
| `failure_reason` | TEXT NULL | 失败原因 |
| `duplicate_group_id` | TEXT NULL | 疑似重复分组 |
| `duplicate_score` | REAL NULL | 重复分数 |
| `duplicate_reason` | TEXT NULL | 重复原因 |
| `possible_duplicate_transaction_id` | TEXT NULL | 可能重复的正式账单 |
| `reviewed_at` | TEXT NULL | 复核时间 |
| `converted_transaction_id` | TEXT NULL | 成功入账后的正式账单 ID |
| `created_at` | TEXT NOT NULL | 创建时间 |
| `updated_at` | TEXT NOT NULL | 更新时间 |

`candidate_events`：

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | TEXT PRIMARY KEY | 事件 ID |
| `candidate_id` | TEXT NOT NULL | 候选账单 |
| `event_type` | TEXT NOT NULL | created / parsed / retried / edited / reviewed / rejected / converted / failed |
| `summary` | TEXT NOT NULL DEFAULT '' | 简短说明 |
| `payload_json` | TEXT NULL | 结构化差异或错误信息 |
| `created_at` | TEXT NOT NULL | 事件时间 |

推荐索引：

- `idx_raw_inputs_batch`
- `idx_raw_inputs_hash`
- `idx_candidates_state_updated`
- `idx_candidates_batch_state`
- `idx_candidates_duplicate_group`
- `idx_candidate_events_candidate_created`

#### 5.9.6 自定义分类 / 来源与清洗历史

当前自定义分类、自定义来源主要存在 UserDefaults，并通过 BackupBundle 数组导出。进入 iPad / Mac 工作台后，建议逐步迁入 SQLite：

| 表 | 目的 |
|----|------|
| `custom_categories` | 统一保存用户分类名、排序、归档状态和更新时间 |
| `custom_sources` | 统一保存用户来源名、排序、归档状态和更新时间 |
| `cleaning_actions` | 记录批量商户统一、分类批改、账本移动、重复处理等动作 |
| `cleaning_action_items` | 记录每个批量动作影响的交易 ID、旧值、新值和结果 |

清洗历史不必阻塞 GOAL-1503，但 GOAL-1552 实现批量应用与回滚前必须补齐，否则难以做到可追溯和可恢复。

#### 5.9.7 BackupBundle v2 草案

BackupBundle v2 应保持“正式账本恢复优先、候选原始输入谨慎导出”的策略。

v2 建议新增：

| 字段 | 说明 |
|------|------|
| `ledgers` | 多账本列表，至少包含默认账本 |
| `transactionExtensions` 或扩展后的 `BackupTransaction` | `ledgerId`、`transactionType`、`currencyCode`、`originalAmount`、`exchangeRate` |
| `candidateBatches` | 可选导出导入批次摘要 |
| `candidateTransactions` | 可选导出候选账单字段，不默认包含原图 |
| `rawInputs` | 默认不导出；只有用户选择完整备份 / 诊断备份时才包含安全摘要或原始文本 |
| `customCategoriesV2` / `customSourcesV2` | 带 ID、排序、归档状态的结构化版本 |
| `cleaningActions` | 可选导出清洗历史，便于审计和回滚 |

兼容要求：

- `BackupValidator` 需接受 v1 和 v2，不能只判断 `schemaVersion == 1`。
- 解码 v1 文件时不能因为 v2 新字段缺失而失败；需要 optional 字段、默认值或自定义 `Decodable`。
- 恢复 v1 文件时，所有正式账单进入 `default-local-ledger`，`currencyCode = CNY`，`transactionType = expense`。
- v2 自动 iCloud 备份默认不包含原始图片、PDF 或 OCR 全文；若提供完整备份，UI 必须明确提示可能包含敏感财务数据。
- CSV 导出只覆盖正式账单，不包含候选区、raw input、debug events 或清洗历史。

#### 5.9.8 推荐实施顺序

| 顺序 | 目标 | 说明 |
|------|------|------|
| 1 | 引入 schema version | 使用 `PRAGMA user_version`，保留旧列检测兼容 |
| 2 | 新增 `ledgers` 表 | 插入 `default-local-ledger` |
| 3 | 扩展 `transactions` 表 | 添加 `ledger_id`、`transaction_type`、`currency_code` 等 additive columns |
| 4 | 回填旧账单 | 所有旧账单归入默认账本，不改变业务字段 |
| 5 | 新增候选区表 | `import_batches`、`raw_inputs`、`candidate_transactions`、`candidate_events` |
| 6 | 加查询 API | 先提供只读 / 写入候选区 API，再接 UI |
| 7 | BackupBundle v2 | 先支持读取 v1 + 导出 v2，再启用恢复 v2 |
| 8 | 迁移自定义分类 / 来源 | 从 UserDefaults 迁入 SQLite，同时保留 UserDefaults 同步兼容期 |

#### 5.9.9 回归要求

后续编码 GOAL-1503 方案时，至少补齐：

- 旧 SQLite 空库初始化成功。
- 旧 SQLite 有账单时迁移成功，旧账单进入 `default-local-ledger`。
- 重复运行迁移不会重复创建默认账本或破坏交易数据。
- `loadTransactions()` 默认不返回已删除账单、不返回候选账单。
- v1 BackupBundle 仍可恢复。
- v2 BackupBundle 缺少候选区字段时仍可恢复正式账单。
- 候选区表写入后不影响今日支出、月报和 Watch / Widget 数据。
- iCloud 自动备份不意外包含 raw input 敏感内容。

## 6. Mac Catalyst 主线

iPad 端程序完善后，Mac 端优先采用 Mac Catalyst 路线承接 iPad 工作台能力。Mac 不再只是“能不能复用”的评估项，而是 v1.5.x / v1.6.0 的明确全平台扩展方向。

Mac 端定位为桌面账本整理与导入导出工作台，重点不是快速记一笔，而是处理大量文件、批量修正、表格编辑和备份导入导出。

### 6.1 可直接复用的部分

- 账单数据模型、账本模型、候选账单状态模型。
- OCR 后文本解析、商户别名、分类学习、重复检测等 core 逻辑。
- SQLite / 本地持久化层中与平台 UI 无关的部分。
- iCloud Drive 备份、导入导出、离线回归样本和 Golden Case。
- SwiftUI 中足够纯粹的列表、表格、筛选器、详情检查器组件。
- iPad 截图管线中用于准备演示数据、固定状态和多语言文案的脚本思路。

### 6.2 Mac 首版能力

Mac Catalyst 首版建议覆盖以下能力：

| 能力 | 说明 | 依赖 |
|------|------|------|
| 拖拽截图 / 文件导入 | 支持从 Finder 拖入图片、PDF、文本文件或文件夹；进入导入队列而不是直接入账 | iPad 批量导入队列、macOS 文件权限 |
| CSV / JSON 导入导出 | CSV 用于表格交换，JSON 用于完整备份 / 迁移；导入必须有预览和确认 | BackupBundle、导入队列、字段映射 |
| 键盘快捷键 | 导入、导出、搜索、新增、编辑、删除、批量选择、确认入账、撤销等 | SwiftUI commands / Catalyst 菜单 |
| 大表格 / 列表编辑区域 | 更高密度表格、可排序列、可筛选列、详情检查器常驻 | iPad 账本工作区与候选区组件 |
| 批量选择 / 批量修正 | 多选账单后批量改分类、商户别名、来源、目标账本 | 数据清洗预览与事务写入 |
| 重复账单检查 | 按金额、商户、时间、OCR 文本相似度和来源提示重复 | 重复检测服务 |
| 基础菜单栏 | `Import` / `Export` / `Backup` / `Settings` 菜单，后续可扩展 Help / Window | Catalyst commands |

### 6.3 Mac 菜单栏草案

第一版菜单保持基础、可发现、可用键盘驱动：

| Menu | Command | 说明 |
|------|---------|------|
| Import | Import Files... | 打开文件选择器，支持图片 / PDF / 文本 / CSV / JSON |
| Import | Paste Ledger Text | 从剪贴板导入文本账单 |
| Export | Export CSV... | 导出当前筛选账单或选中账单 |
| Export | Export JSON Backup... | 导出完整备份 bundle |
| Backup | Backup Now | 立即写入本地 / iCloud Drive 备份 |
| Backup | Restore from JSON... | 选择备份文件并进入恢复预览 |
| Settings | Open Settings | 打开设置窗口或设置页 |

### 6.4 需要单独适配的部分

- Mac 窗口、菜单栏、快捷键、工具栏和多窗口行为。
- 文件导入体验：Mac 更依赖 Finder 拖拽、菜单导入、文件夹批处理。
- 表格密度和键盘操作：Mac 用户会期待更强的批量选择、复制、撤销和快捷键。
- 沙盒权限、文件访问、安全范围书签等 macOS 细节。
- App Store 截图、审核说明和平台能力描述需要单独准备。
- Watch / Widget 联动在 Mac 端不是核心卖点，不能简单复制 iPad 叙事。

### 6.5 产品判断

v1.5.0 首版建议按“iPad 优先，Mac Catalyst 跟进”处理：

- iPad 工作台先跑通批量导入、批量识别、清洗和多账本。
- 所有核心业务逻辑尽量放在跨平台模块，避免绑死 iPad UI。
- SwiftUI 组件拆分时考虑 macOS 可用性，但不为 Mac 牺牲 iPad 首版体验。
- 当 iPad 端候选账单、清洗队列、多账本管理稳定后，优先开启 Mac Catalyst 工程接入。
- Mac 端第一版不承诺 Watch / Widget parity，也不做复杂多窗口；先保证文件导入、表格整理和导入导出闭环。

结论：工程上可以复用大部分业务层和部分 SwiftUI 组件；产品上不把 Mac 端放进 App Store v1.4.0 的首版承诺，但把 Mac Catalyst 明确列为 iPad 端稳定后的下一阶段生产力平台。

### 6.6 tvOS 只读大屏看板

tvOS 不承担导入、编辑、清洗和写入账本，首版只做美观、稳定、可远距离阅读的只读展示。它更接近家庭大屏财务看板，适合在客厅查看家庭消费状态。

#### 6.6.1 tvOS 展示内容

- 本月支出总览：总金额、笔数、日均支出、与上月对比。
- 分类占比：餐饮、购物、交通、数字服务等分类的占比和金额。
- 最近消费趋势：最近 7 / 30 天消费折线或柱状趋势。
- 年度 / 月度摘要：年度累计、月度对比、Top 分类和 Top 商户。
- 家庭大屏看板：以大字号、低信息密度和清晰颜色层级展示，适配遥控器焦点导航。

#### 6.6.2 tvOS 设计边界

- 只读，不新增账单、不导入文件、不批量修正。
- 依赖 iCloud / 本地共享数据或后续同步方案；不为 tvOS 单独设计账本写入链路。
- 遥控器操作只做时间范围、账本、分类焦点切换。
- 必须有隐私模式，避免家庭大屏直接暴露具体账单明细。

### 6.7 visionOS 空间展示版本

visionOS 首版定位为漂亮的空间展示体验，不替代 iPad / Mac 的生产力工作台。它强调“看见自己的消费结构”，而不是在空间里做复杂表格编辑。

#### 6.7.1 visionOS 展示内容

- 月度支出空间看板：总览指标以大卡片形式固定在主视线区域。
- 分类支出卡片漂浮展示：分类金额、占比、变化趋势以卡片组展示，可按分类聚合。
- 年度消费时间线墙：按月份排列年度支出、Top 分类、异常月份和重要事件。
- 最近账单悬浮列表：展示最近消费摘要，支持轻量筛选和只读详情。

#### 6.7.2 visionOS 设计边界

- 首版只读展示，不做拖拽文件导入、批量编辑或数据清洗。
- 视觉重点是空间层次、阅读距离、手势焦点和低疲劳信息密度。
- 数据口径必须与 iPad / Mac / iPhone 一致，避免展示端产生另一套统计逻辑。
- 隐私模式必须可用，金额和商户可一键隐藏。

## 7. 截图与发布资产

当前截图管线尚未覆盖 iPad 端。`tools/appstore-screenshots/README.md` 中也明确记录现状：管线支持 iPhone 与 Apple Watch，但不生成 iPad screenshots。

v1.5.0 需要把 iPad 截图扩展作为正式发布资产工作项，而不是在发布前临时手工补图。该工作项应与 iPad 信息架构、演示数据、横竖屏布局和多语言文案同步设计。

第一版截图覆盖建议：

- iPhone：快速识别、账本列表、月报、设置 / 支持入口。
- Watch：今日支出第一屏、左滑最近支出第二屏、快速记账、表盘小组件。
- iPad：统计分析工作台、批量导入队列、候选账单复核、数据清洗、多账本切换。
- Mac：拖拽导入、表格整理、批量修正、CSV / JSON 导入导出、菜单栏工作流。
- tvOS：本月总览、分类占比、趋势看板、年度 / 月度摘要。
- visionOS：空间看板、漂浮分类卡片、年度时间线墙、最近账单悬浮列表。

iPad 截图管线扩展范围：

- 在截图配置中增加 iPad target size、设备候选和输出目录，例如 `raw/ipad/{locale}/` 与 `store/ipad/{locale}/`。
- 增加 iPad 专用 screenshot host 或让现有 host 支持 `--screenshot-platform ipad`，避免直接放大 iPhone 画面。
- 为 iPad 工作台准备稳定 fixture：账本、月份、分类、商户、候选账单、批量导入状态和清洗建议。
- 渲染脚本需要支持 iPad 横屏主画布；必要时保留竖屏关键页，但第一版以横屏工作台为主。
- `preview.html` 需要把 iPhone、Watch、iPad 三端分组展示，便于发布前目检。
- 导出脚本需要支持 `--ipad-only`，以及与现有 `--ios-only` / `--watch-only` 不冲突的组合。

截图管线要求：

- iPad 截图必须使用稳定演示数据，覆盖批量与统计场景。
- 三端截图中的金额、商户、分类、账本名称要保持同一套演示口径。
- iPad 截图需要验证横屏主场景，必要时补充竖屏关键页。
- 简体中文和英文优先准备；繁体中文可沿用本地化能力，是否上传截图按发布节奏决定。
- 截图导出后继续保留黑屏 / 空白页 / 文字截断检查。
- 在 iPad 管线完成前，App Store iPad 截图不得标记为已准备完成。

## 8. 识别链路优化

### 8.1 目标

v1.5.0 继续强化“识别 → 解释 → 复核 → 入账”的可信链路。

重点不是让系统永远自动判断正确，而是在不确定时明确告诉用户：

- 哪些字段可信。
- 哪些字段需要复核。
- 为什么被归到某个商户或分类。
- 是否可能重复。
- 是否适合批量应用同样规则。

### 8.2 场景拓展

优先补充以下场景样本和规则：

- 微信支付、支付宝、Apple Pay、银行卡消费通知。
- 电商订单和支付详情。
- 订阅服务账单。
- 餐饮、小票、外卖、咖啡、便利店。
- 出行、打车、地铁、停车、酒店、机票。
- 海外消费、多币种金额、汇率备注。
- 邮件 / 短信 / 剪贴板文本账单。
- 长截图、多张截图组成的一笔账单。

### 8.3 质量门禁

每次拓展识别场景时，应同步增加：

- Golden Case。
- 离线回归样本。
- 误识别反例。
- 低置信度展示规则。
- 分类 / 商户别名回归检查。

## 9. 里程碑草案

| 阶段 | 主题 | 交付物 |
|------|------|--------|
| Phase 0 | 设计与数据口径校准 | Watch 今日支出口径、Widget 信息设计、iPad 信息架构、候选账单状态设计 |
| Phase 1 | Watch 今日支出与最近支出 | 第一屏今日金额 / 笔数 / 最近商户，左滑第二屏账本最近支出 |
| Phase 2 | 表盘小组件 | Inline / Circular / Rectangular 第一版，点击进入 Watch App |
| Phase 3 | iPad 工作台骨架 | Sidebar、列表 / 表格、详情检查器、基础统计入口 |
| Phase 4 | 批量导入与识别队列 | 多文件导入、识别状态、失败重试、候选账单生成 |
| Phase 5 | 数据清洗与结构化入账 | 商户统一、分类修正、重复检测、候选转正式账单 |
| Phase 6 | 多账本管理 | 创建 / 归档 / 默认账本、账单移动、按账本统计 |
| Phase 7 | Mac Catalyst 生产力工作台 | 拖拽导入、CSV / JSON 导入导出、快捷键、菜单栏、大表格、批量选择和重复检查 |
| Phase 8 | tvOS 只读大屏看板 | 本月支出总览、分类占比、最近消费趋势、年度 / 月度摘要、家庭大屏展示 |
| Phase 9 | visionOS 空间展示版本 | 月度支出空间看板、漂浮分类卡片、年度消费时间线墙、最近账单悬浮列表 |
| Phase 10 | 回归与发布准备 | iPhone / iPad / Watch / Mac / tvOS / visionOS 分平台回归、截图、平台说明、Review Notes |

## 10. 验收口径

### 10.1 Watch

- 打开 Watch App 首屏即能看到今日支出总金额、笔数和最近商户。
- 向左滑第二屏能看到当前账本关联的最近支出。
- 今日无支出时状态清晰，不误导用户。
- Watch 新增账单后，今日支出能在合理时机刷新。
- 表盘小组件显示今日支出，并能跳转到 Watch App。
- 大字号、VoiceOver、隐私隐藏场景可用。

### 10.2 iPad

- iPad 上可以完成一批截图或文件的导入。
- 批量识别结果进入候选区，而不是直接污染正式账本。
- 用户可以批量确认、逐条修正、忽略或重试。
- 用户可以对商户、分类、重复项进行清洗整理。
- 候选账单确认后能结构化写入指定账本。
- 统计分析能按账本、月份、分类、商户查看。
- iPad 截图管线能导出稳定的工作台、批量导入、清洗和多账本页面。
- iPad 截图产物能进入统一 `preview.html`，并能按 locale 分别目检。

### 10.3 Mac Catalyst

- iPad 工作台稳定后，Mac Catalyst 工程能复用核心模型、解析、导入队列、清洗和备份能力。
- Mac 支持拖拽截图 / 文件导入，导入内容进入候选区或导入预览，不直接污染正式账本。
- Mac 支持 CSV / JSON 导入导出；恢复类操作必须有预览和确认。
- Mac 支持键盘快捷键和基础菜单栏：Import / Export / Backup / Settings。
- Mac 提供更大的表格 / 列表编辑区域，支持排序、筛选、搜索和详情检查器。
- Mac 支持批量选择、批量修正和重复账单检查，批量写入前必须展示影响范围。

### 10.4 tvOS

- tvOS 首版只读，不提供导入、编辑、删除、清洗或备份恢复。
- 本月支出总览、分类占比、最近消费趋势、年度 / 月度摘要能在远距离清晰阅读。
- 家庭大屏看板支持遥控器焦点导航和隐私隐藏。
- tvOS 展示口径与 iPhone / iPad / Mac 的统计口径一致。

### 10.5 visionOS

- visionOS 首版只读展示，不提供复杂表格编辑或文件导入。
- 月度支出空间看板、分类支出卡片、年度时间线墙和最近账单悬浮列表布局清晰。
- 空间展示支持隐私隐藏，避免金额和商户在共享空间中直接暴露。
- visionOS 展示口径与其他平台一致，不新增独立统计逻辑。

### 10.6 识别链路

- 新增场景必须有样本和回归。
- 低置信度结果必须清楚标记。
- 批量规则应用前必须让用户确认影响范围。
- 商户别名、分类学习、重复检测不能破坏既有账单数据。

## 11. 风险与待确认问题

- Watch 小组件刷新频率受系统限制，不能承诺实时金额。
- 今日支出口径需要与多账本能力提前对齐，避免后续迁移成本过高。
- Watch 左滑第二屏与快速记账入口的导航层级需要实机验证，避免常用操作变深。
- iPad 批量导入可能引入大量候选数据，需要设计失败恢复和取消路径。
- PDF、长截图、多币种等场景复杂度较高，需要明确首版支持边界。
- 多账本涉及现有账单数据迁移，必须先设计默认账本和兼容策略。
- 数据清洗动作可能影响历史账单，必须有确认、预览和回滚思路。
- iPad 到 Mac 可以复用大量业务层，但 macOS 交互、菜单、文件权限、拖拽导入、安全范围书签和窗口体验需要单独设计。
- Mac Catalyst 可能暴露 iPad UI 在桌面上的密度和焦点问题，必须单独做键盘、菜单、表格和批量操作验收。
- tvOS 与 visionOS 不应抢跑写入能力；展示端如果没有稳定同步 / 共享数据口径，容易出现与主账本不一致。
- tvOS 家庭大屏和 visionOS 空间展示都需要隐私隐藏策略，避免在公共空间暴露个人消费明细。
- App Store v1.4.0 是否同时发布 iPad 截图、Watch 小组件截图，需要根据实现质量再定。
- 当前截图管线不支持 iPad；v1.5.0 若要发布 iPad 端，必须预留足够时间实现、导出并人工复核 iPad 截图。

## 12. 第一版施工前清单

1. 确认 v1.5.x 内部开发线与 App Store v1.4.0 版本号策略。
2. 校准 Watch 今日支出统计口径：日期、账本、支出类型、币种。
3. 校准 Watch 最近支出第二屏的数据口径：账本、时间、支出类型、列表数量。
4. 输出 Watch 今日支出首屏和左滑最近支出第二屏低保真稿。
5. 输出表盘小组件各尺寸信息设计。
6. 梳理现有账单模型对多账本、候选账单、识别状态的支持缺口。
7. 输出 iPad 信息架构和首屏布局稿。
8. 确认截图管线中的 iPad 覆盖页面和演示数据。
   - 当前管线尚未支持 iPad；施工前需先设计 `--ipad-only`、iPad target size、横屏工作台画布、输出目录和 `preview.html` 分组。
9. 明确批量导入第一版支持格式。
10. 建立新增识别场景的样本目录和回归命名规则。
11. 输出 iPad 到 Mac Catalyst 的复用评估记录，明确拖拽导入、CSV / JSON、快捷键、菜单栏、大表格和批量操作的首版边界。
12. 输出 tvOS 只读看板信息架构，明确展示口径、遥控器焦点和隐私隐藏策略。
13. 输出 visionOS 空间展示低保真稿，明确空间看板、漂浮卡片、时间线墙和最近账单列表的层级关系。

## 13. 计划评审与 GOAL 拆解

### 13.1 评审结论

当前 v1.5.0 规划方向成立，并已扩展为全平台路线：iPhone / Watch 负责快速记录与随手查看，iPad 负责整理工作台，Mac Catalyst 承接桌面生产力，tvOS 和 visionOS 承接只读展示。若直接从 UI 开始实现，容易出现以下问题：

- 多账本、候选账单、批量识别状态没有底层模型时，iPad 工作台只能做静态页面，后续返工成本高。
- Watch 今日支出和表盘小组件依赖“今日支出统计口径”，该口径又会受到多账本影响，需要先做一个兼容默认账本的最小口径。
- iPad 批量导入、数据清洗和截图管线都需要同一套稳定演示数据，应在 UI 与截图脚本前统一 fixture。
- Mac Catalyst 不应在 iPad 稳定前抢跑，但 iPad 的模型、队列、清洗、表格和导入导出能力必须提前按桌面可复用方式拆分。
- tvOS / visionOS 不应引入写入账本能力，展示端必须跟随统一统计口径和隐私策略。
- iPad 截图管线必须排在 iPad 工作台可展示之后，但不能拖到发布最后一天才补。

因此 v1.5.0 应按“底座 → Watch 小闭环 → iPad 工作台 → 批量与清洗 → Mac Catalyst → 只读展示平台 → 发布资产”的顺序推进。每个 GOAL 都应能单独提交、单独回归、单独回滚。

### 13.2 GOAL 执行规则

每个 GOAL 进入实现前，先按 `process/agent-iteration-workflow.md` 写执行前检查，明确：

- 本轮只完成该 GOAL，不顺手做后续 GOAL。
- 若触碰 Xcode project，必须保持 scheme、target、bundle id、entitlements、Xcode Cloud 脚本不被重命名或删除。
- 涉及数据迁移、批量改写、历史账单刷新时，默认先做 mock / preview / confirmation，不直接静默改写用户数据。
- 文档、CHANGELOG、iteration-log 与最小回归一起收口。

### 13.3 GOAL 队列

| GOAL | 阶段 | 目标 | 主要范围 | 验收标准 | 最小回归 | 依赖 |
|------|------|------|----------|----------|----------|------|
| GOAL-1500 | Phase 0 | 建立 v1.5.0 基线审计 | 检查当前 `MARKETING_VERSION=1.4.0`、Watch / Widget / iPad 设备族配置、现有 SQLite schema、截图管线现状、回归脚本状态 | 输出基线记录；确认不改功能；列出阻断项和可并行项 | `git status`、`xcodebuild -list`、`bash scripts/run_offline_regression.sh`、`bash scripts/run_golden_regression.sh` | 无 |
| GOAL-1501 | Phase 0 | 定义默认账本与今日支出口径 | 明确默认账本、全部账本、支出/退款/收入、日期边界、币种显示、缺失时间兜底 | `versions/v1.5.0-plan.md` 补充口径；新增或更新核心统计测试用例设计 | 文档检查；若有测试草稿则跑对应离线脚本 | GOAL-1500 |
| GOAL-1502 | Phase 0 | 设计候选账单状态模型 | 定义 Raw Input / Candidate / Reviewed / Transaction / Rejected 状态、失败原因、置信度、重复提示字段 | 状态流可以支撑 iPad 批量导入和清洗，不要求先写 UI | 文档检查；模型草稿不破坏现有 `Transaction` | GOAL-1500 |
| GOAL-1503 | Phase 0 | 评估 SQLite / BackupBundle 对多账本与候选账单的 schema 缺口 | `SQLiteTransactionStore`、`BackupBundle`、`Transaction`、自定义分类/来源/商户别名关系 | 输出迁移方案：兼容旧数据、默认账本、候选区是否入库、备份 schema 是否升级 | `bash scripts/run_offline_regression.sh` | GOAL-1501、GOAL-1502 |
| GOAL-1510 | Phase 1 | Watch 今日支出数据服务 | 在 Core 或 App 层提供今日支出 summary：总金额、笔数、最近商户、空状态、离线兜底 | 不改 Watch UI 也可通过离线/单元回归验证 summary 正确 | `bash scripts/run_offline_regression.sh`，必要时新增 summary case | GOAL-1501 |
| GOAL-1511 | Phase 1 | Watch 首屏切换为今日支出 | `AutoLedgerWatch Watch App` 首屏 UI、空状态、同步状态、快速记账入口保留 | 打开 Watch App 首屏即为今日支出；大字号/VoiceOver 不明显破版 | Watch build；必要时截图/模拟器目检 | GOAL-1510 |
| GOAL-1512 | Phase 1 | Watch 左滑最近支出第二屏 | Watch 最近支出列表、当前账本提示、单笔只读详情入口 | 左滑可看到最近 3-5 笔；无数据有空状态；不引入复杂编辑 | Watch build；离线 mock 数据检查 | GOAL-1510、GOAL-1511 |
| GOAL-1520 | Phase 2 | iPhone / Watch Widget 今日支出数据源统一 | iPhone 桌面小组件、iPhone 负一屏 Today View 小组件、后续 Watch complication 使用同一今日支出口径，支持空状态与过期数据 | iPhone `DailyExpenseWidget` 与 Core 今日支出口径对齐；Watch 表盘小组件形成后可复用同一规则 | Widget extension build；summary 数据回归 | GOAL-1510 |
| GOAL-1521 | Phase 2 | 表盘小组件 UI 与跳转 | Watch 小组件文案、本地化、点击进入今日支出页 | 小尺寸不截断；点击路径正确；隐私隐藏预留 | Watch / Widget build；人工截图检查 | GOAL-1520 |
| GOAL-1530 | Phase 3 | iPad 信息架构与入口策略 | 明确 Universal App 设备族、NavigationSplitView、Sidebar 项目、iPad 与 iPhone 入口差异 | 文档和低保真结构确定；不直接铺大而全 UI | 文档检查；若改设备族则跑 iOS build | GOAL-1500 |
| GOAL-1531 | Phase 3 | iPad 工作台骨架 | iPad 专用壳层：Sidebar、列表/表格区域、详情检查器，占位数据 | iPad 横屏可用；iPhone 原路径不回退；不接真实批量导入 | iOS build；iPad simulator smoke；iPhone 主路径目检 | GOAL-1530 |
| GOAL-1532 | Phase 3 | iPad 统计分析基础页 | 按默认账本/月/分类/商户展示基础统计，复用现有月报逻辑 | iPad 上可查看统计总览；数据口径与 iPhone 月报一致或差异明确 | 离线统计回归；iOS build | GOAL-1531、GOAL-1501 |
| GOAL-1540 | Phase 4 | 批量导入队列模型 | 建立导入项、队列状态、失败原因、重试标记；先支持 mock 或文本输入 | 导入项可从待识别到候选状态，不污染正式账本 | 新增离线队列状态测试；`run_offline_regression.sh` | GOAL-1502、GOAL-1503 |
| GOAL-1541 | Phase 4 | iPad 批量导入 UI | iPad 导入箱、状态筛选、失败重试入口、候选列表 | 可展示待识别/待复核/可入账/失败/疑似重复；不自动入账 | iOS build；iPad simulator smoke | GOAL-1540、GOAL-1531 |
| GOAL-1542 | Phase 4 | 批量 OCR / 文本解析执行器 | 将队列项逐条送入 OCR / `LedgerTextInterpreterCore`，生成候选账单和 warning | 大批量不阻塞 UI；失败可重试；低置信度有原因 | Golden / offline 新增批量 case；iOS build | GOAL-1540、GOAL-1541 |
| GOAL-1550 | Phase 5 | 候选账单复核与正式入账 | 候选详情、字段编辑、确认入账、忽略、重复提示 | 用户确认后才写入正式账本；忽略不污染统计 | SQLite / LedgerStore 离线回归；iOS build | GOAL-1542 |
| GOAL-1551 | Phase 5 | 数据清洗规则预览 | 商户统一、分类批量修正、重复检测只做预览与影响范围展示 | 不静默改历史；每个批量动作有确认前预览 | 离线重复检测/别名回归 | GOAL-1550、GOAL-1503 |
| GOAL-1552 | Phase 5 | 数据清洗应用与回滚策略 | 批量应用分类/商户清洗；记录变更范围；提供撤销或可恢复路径 | 清洗动作可追溯；失败不造成半写入 | SQLite 事务回归；备份/恢复回归 | GOAL-1551 |
| GOAL-1560 | Phase 6 | 多账本模型与默认账本迁移 | Ledger model、默认账本、旧账单迁移、账单归属字段 | 旧数据自动进入默认账本；备份恢复兼容 | `run_offline_regression.sh`；迁移测试 | GOAL-1503 |
| GOAL-1561 | Phase 6 | 多账本管理 UI | 创建、重命名、归档、默认账本、账单移动 | iPad 可管理多账本；iPhone 不破坏原路径 | iOS build；SQLite 回归 | GOAL-1560、GOAL-1531 |
| GOAL-1562 | Phase 6 | 统计与 Watch 跟随账本口径 | 统计分析、Watch 今日支出、最近支出按默认/当前账本更新 | 多账本后 Watch 与 iPad 口径一致 | offline summary 回归；Watch build | GOAL-1560、GOAL-1510、GOAL-1532 |
| GOAL-1570 | Phase 7 | Mac Catalyst 接入评估 | 梳理 Catalyst 开关、target 配置、可复用 Core / SwiftUI 组件、macOS 文件权限和菜单差异 | 输出 Mac Catalyst 实施清单；不破坏 iOS / iPad / Watch 发布链 | 文档检查；必要时 `xcodebuild -list` | GOAL-1531、GOAL-1540、GOAL-1560 |
| GOAL-1571 | Phase 7 | Mac 拖拽截图 / 文件导入 | Finder 拖拽图片、PDF、文本或文件夹进入导入队列；安全范围访问与失败提示 | 拖入内容进入候选区或导入预览，不直接写入正式账本 | Mac Catalyst build；导入队列离线回归 | GOAL-1570、GOAL-1540 |
| GOAL-1572 | Phase 7 | Mac CSV / JSON 导入导出 | CSV 字段映射、JSON BackupBundle 导入导出、恢复预览和确认 | CSV / JSON 能导入导出；恢复类操作有二次确认 | BackupBundle 回归；CSV fixture 回归 | GOAL-1570、GOAL-1503 |
| GOAL-1573 | Phase 7 | Mac 快捷键与菜单栏 | `Import` / `Export` / `Backup` / `Settings` 菜单；常用命令键盘快捷键 | 菜单可发现、快捷键可用、不影响 iPad toolbar | Mac Catalyst build；人工菜单 smoke | GOAL-1570 |
| GOAL-1574 | Phase 7 | Mac 大表格与批量编辑 | 更大列表 / 表格区域、排序筛选、批量选择、批量分类 / 商户修正 | 大批量账单可编辑；批量写入前有影响范围预览 | 数据清洗回归；Mac Catalyst build | GOAL-1551、GOAL-1552、GOAL-1570 |
| GOAL-1575 | Phase 7 | Mac 重复账单检查 | 金额、商户、时间、来源、OCR 文本相似度重复提示；批量处理重复项 | 重复项只提示和预览，不静默删除 | 重复检测离线回归；Mac Catalyst build | GOAL-1551、GOAL-1574 |
| GOAL-1580 | Phase 8 | tvOS 只读看板设计 | 本月总览、分类占比、最近趋势、年度 / 月度摘要、家庭大屏隐私模式 | 输出 tvOS 信息架构；明确只读边界和同步口径 | 文档检查 | GOAL-1562 |
| GOAL-1581 | Phase 8 | tvOS 看板实现评估 | tvOS target / scene 可行性、焦点导航、遥控器交互、展示数据入口 | 明确 target 接入成本和最小实现面 | `xcodebuild -list`；必要时 tvOS build smoke | GOAL-1580 |
| GOAL-1582 | Phase 9 | visionOS 空间展示设计 | 月度空间看板、漂浮分类卡片、年度时间线墙、最近账单悬浮列表、隐私模式 | 输出 visionOS 低保真稿和展示口径，不引入写入能力 | 文档检查 | GOAL-1562 |
| GOAL-1583 | Phase 9 | visionOS 展示实现评估 | visionOS target / window / volume 可行性，空间卡片布局和数据复用边界 | 明确实现路径和最小 smoke 验证方式 | `xcodebuild -list`；必要时 visionOS build smoke | GOAL-1582 |
| GOAL-1590 | Phase 10 | iPad / Mac / tvOS / visionOS 截图管线设计 | `screenshots.json` target、平台 scene、fixture、输出目录、`preview.html` 分组设计 | 形成全平台截图实现清单；确认 locale 和平台展示策略 | 文档检查 | GOAL-1531、GOAL-1570、GOAL-1580、GOAL-1582 |
| GOAL-1591 | Phase 10 | iPad 截图管线实现 | `--ipad-only`、raw/store ipad 输出、renderer、preview 分组 | 能导出 iPad 工作台截图；不会破坏 iPhone/Watch 管线 | `export.sh --ios-only`、`--watch-only`、`--ipad-only`；像素/黑屏检查 | GOAL-1590、GOAL-1532、GOAL-1541 |
| GOAL-1592 | Phase 10 | 全平台发布回归矩阵与 Review Notes | iPhone / iPad / Watch / Mac / tvOS / visionOS / backup / IAP / screenshot 全矩阵 | 形成 v1.5.x 回归基线和分平台发布说明 | `run_offline_regression.sh`、`run_golden_regression.sh`、xcodebuild、截图导出 | GOAL-1512、GOAL-1521、GOAL-1562、GOAL-1575、GOAL-1581、GOAL-1583、GOAL-1591 |

### 13.4 推荐推进顺序

第一批只做底座，不做 UI 大展开：

1. GOAL-1500
2. GOAL-1501
3. GOAL-1502
4. GOAL-1503

第二批做 Watch 小闭环，保证用户可见改进先落地：

1. GOAL-1510
2. GOAL-1511
3. GOAL-1512
4. GOAL-1520
5. GOAL-1521

第三批做 iPad 工作台骨架与候选账单队列：

1. GOAL-1530
2. GOAL-1531
3. GOAL-1532
4. GOAL-1540
5. GOAL-1541
6. GOAL-1542

第四批做正式入账、清洗、多账本：

1. GOAL-1550
2. GOAL-1551
3. GOAL-1552
4. GOAL-1560
5. GOAL-1561
6. GOAL-1562

第五批做 Mac Catalyst 生产力扩展：

1. GOAL-1570
2. GOAL-1571
3. GOAL-1572
4. GOAL-1573
5. GOAL-1574
6. GOAL-1575

第六批做只读展示平台：

1. GOAL-1580
2. GOAL-1581
3. GOAL-1582
4. GOAL-1583

第七批做发布资产与收口：

1. GOAL-1590
2. GOAL-1591
3. GOAL-1592

### 13.5 首个可执行 GOAL 建议

下一轮建议从 GOAL-1500 开始，目标不是写功能，而是建立当前 v1.5.0 的工程事实基线：

- 确认 `main` 当前干净状态和版本号。
- 记录 Watch / Widget / iPad 设备族配置。
- 记录当前 SQLite、BackupBundle、Transaction、LedgerStore 对多账本和候选账单的缺口。
- 记录截图管线当前支持 iPhone / Watch，不支持 iPad。
- 跑最小回归包并把结果写回文档。

GOAL-1500 完成后，再进入 GOAL-1501 / GOAL-1502 的口径和模型设计，会比直接改 Watch 或 iPad UI 稳很多。

### 13.6 GOAL-1500 执行记录（2026-06-01）

状态：DONE。

本轮只做基线审计和文档记录，未实现 v1.5.0 功能，未修改业务代码、SQLite schema、截图脚本、Bundle Identifier、DEVELOPMENT_TEAM、App Group、iCloud Container、entitlements、scheme 或 target 名称。

#### 13.6.1 Git 与版本基线

| 项目 | 当前事实 |
|------|----------|
| 当前分支 | `main` |
| 当前 HEAD | `ce6c054 chore: switch to internal 1.5.0 development` |
| 远端状态 | `main...origin/main` |
| 发布基准 tag | `v1.4.0` 指向 `891e196 docs: mark v1.4.0 baseline and plan v1.5.0` |
| App Store 对外版本 | 全 target `MARKETING_VERSION = 1.4.0` |
| Build Number | 全 target `CURRENT_PROJECT_VERSION = 1` |
| 工作区注意事项 | 本轮开始前已有未提交的 `project.pbxproj` 与 `LICENSE` 改动；GOAL-1500 未回退、未混入这些改动 |

#### 13.6.2 Workspace / Scheme / Target 基线

`xcodebuild -list -workspace AutoLedger/AutoLedger.xcworkspace` 通过。当前 workspace 可见 scheme：

- `AutoLedger`
- `AutoLedgerCore`
- `AutoLedgerWatch Watch App`
- `AutoLedgerWidgetsExtension`
- `ControlWidgetExtension`
- `MediaPipeTasksGenAI`
- `MediaPipeTasksGenAIC`
- `Pods-AutoLedger`
- `ReceiptDebugTool`
- `ShareExtension`

当前工程仍使用 `AutoLedger/AutoLedger.xcworkspace` 作为构建入口，符合 CocoaPods 依赖要求。

#### 13.6.3 设备族与发布配置基线

| Target / Extension | Bundle ID | 设备族 / 平台 | 备注 |
|--------------------|-----------|---------------|------|
| AutoLedger App | `top.darkrio326.AutoLedger` | 当前工作区为 iPhone + iPad（`TARGETED_DEVICE_FAMILY = "1,2"`），`SUPPORTED_PLATFORMS = "iphoneos iphonesimulator"`，`SUPPORTS_MACCATALYST = NO` | 该 iPad 设备族变更已存在于本轮开始前的未提交 `project.pbxproj` 中，GOAL-1500 仅记录事实 |
| ShareExtension | `top.darkrio326.AutoLedger.ShareExtension` | iPhone（`TARGETED_DEVICE_FAMILY = 1`） | entitlements 路径仍为 `ShareExtension/ShareExtension.entitlements` |
| AutoLedgerWatch Watch App | `top.darkrio326.AutoLedger.watchkitapp` | Watch（`TARGETED_DEVICE_FAMILY = 4`） | Watch target 仍存在并随主 App build |
| AutoLedgerWidgetsExtension | `top.darkrio326.AutoLedger.AutoLedgerWidgets` | iPhone + iPad（`TARGETED_DEVICE_FAMILY = "1,2"`） | 当前 Widget extension 已覆盖 iPad 设备族 |
| ControlWidgetExtension | `top.darkrio326.AutoLedger.ControlWidget` | iPhone（`TARGETED_DEVICE_FAMILY = 1`） | entitlements 路径仍为 `ControlWidgetExtension/ControlWidgetExtension.entitlements` |

#### 13.6.4 数据模型 / SQLite / BackupBundle 缺口

当前 `Transaction` 模型字段为：

- `id`
- `merchant`
- `amount`
- `occurredAt`
- `category`
- `source`
- `note`

当前 SQLite 表：

- `transactions`：`id`、`merchant`、`amount`、`occurred_at`、`category`、`source`、`note`、`created_at`、`updated_at`，旧库通过迁移补 `deleted_at`。
- `debug_events`：记录导入、OCR、LLM 和 transaction 关联调试信息。
- `subscriptions`
- `category_corrections`
- `merchant_aliases`

当前 `BackupBundle.schemaVersion = 1`，覆盖 transactions、deleted transactions、subscriptions、category corrections、custom categories、custom sources、merchant aliases、subscription metadata 和 app settings。

面向 v1.5.0 的主要缺口：

- 还没有 `Ledger` / 多账本模型，也没有 transaction 的 `ledger_id`。
- 还没有 Raw Input / Candidate / Reviewed / Rejected 的候选账单状态模型。
- SQLite 尚无导入批次、候选账单、清洗操作历史、账本表或账单移动记录。
- BackupBundle v1 尚未覆盖多账本、候选区、批量导入队列、数据清洗历史或默认账本配置。
- `LedgerStore.makeBackupBundle()` 在读取不到 bundle 版本时仍有 `"1.3.0"` fallback，后续版本治理可顺手清理为更稳的动态兜底。

这些缺口不阻断当前 v1.5.0 基线，但会阻断 iPad 批量导入、多账本和数据清洗的可靠实现。建议先进入 GOAL-1501 / GOAL-1502 / GOAL-1503，再展开 UI。

#### 13.6.5 截图管线基线

`tools/appstore-screenshots` 当前事实：

- 支持 iPhone 与 Apple Watch。
- 支持 `zh-Hans`、`zh-Hant`、`en`。
- 配置中存在 `ios_65`、`ios_69`、`watch` target。
- iPhone 场景为 `iosShots`，Watch 场景为 `watchShots`。
- 当前没有 iPad target、`ipadShots`、iPad raw/store 输出目录或 iPad `preview.html` 分组。

因此 v1.5.0 的 iPad 发布资产工作应保留为全平台截图管线阶段中的 GOAL-1590 / GOAL-1591，不应在 iPad 工作台 UI 尚未稳定前临时补脚本。

#### 13.6.6 本轮验证结果

| 验证项 | 结果 | 备注 |
|--------|------|------|
| `git status --short --branch` | PASS | 工作区存在既有未提交 `project.pbxproj`、`LICENSE` 与本轮文档改动 |
| `xcodebuild -list -workspace AutoLedger/AutoLedger.xcworkspace` | PASS | scheme 列表正常 |
| `bash scripts/run_offline_regression.sh` | PASS | 离线回归通过；仅有既有 Swift warning |
| `bash scripts/run_golden_regression.sh` | PASS | Golden regression passed: 32 case(s)；仅有既有 Swift warning |
| `xcodebuild -workspace AutoLedger/AutoLedger.xcworkspace -scheme AutoLedger -configuration Debug -destination 'generic/platform=iOS' build` | PASS | 主 App、Watch App、Widgets、Share Extension 随依赖构建通过；仅有既有 warning |

#### 13.6.7 阻断项与可并行项

当前没有阻断 GOAL-1500 收口的 blocker。

进入功能开发前的强依赖：

- GOAL-1501：定义默认账本与今日支出口径。
- GOAL-1502：设计候选账单状态模型。
- GOAL-1503：评估 SQLite / BackupBundle 迁移方案。

可并行准备但不应抢跑实现的事项：

- iPad 信息架构低保真稿。
- Watch 今日支出视觉草图。
- iPad 截图场景清单与演示数据口径。
- Mac 复用边界笔记。

### 13.7 GOAL-1530 执行记录（2026-06-01）

状态：DONE（iPad 线第一版入口）。

本轮按用户要求先推进 iPad 线，范围限定为 Universal App 设备族、iPad 与 iPhone 入口差异、iPad sidebar 信息架构和最小可构建壳层。未实现候选账单真实数据模型、批量 OCR、数据清洗执行、多账本 schema 或 iPad 截图管线。

#### 13.7.1 工程入口

- 主 App target 已切到 iPhone + iPad：`TARGETED_DEVICE_FAMILY = "1,2"`。
- 主 App 仍限制在 iOS / iPadOS：`SUPPORTED_PLATFORMS = "iphoneos iphonesimulator"`，`SUPPORTS_MACCATALYST = NO`。
- iPhone 入口保持原 `HomeView` TabView，不改变现有手机主路径。
- iPad 入口新增 `IPadWorkspaceView`，在 `AutoLedgerRootView` 中按 `UIDevice.current.userInterfaceIdiom == .pad` 分流。
- Screenshot mode 仍优先走 `ScreenshotHostView`，不受 iPad 工作台入口影响。

#### 13.7.2 iPad 信息架构第一版

iPad 第一版采用 `NavigationSplitView` + sidebar + detail 的工作台结构。

当前 sidebar 项：

| Sidebar | 当前 detail | 状态 |
|---------|-------------|------|
| 导入 | 复用 `InboxView` | 可用 |
| 账本 | 复用 `LedgerView` | 可用 |
| 分析 | 复用 `ReportView` | 可用 |
| 候选账单 | iPad 占位页 | 规划入口 |
| 数据清洗 | iPad 占位页 | 规划入口 |
| 设置 | 复用 `SettingsView` | 可用 |

候选账单与数据清洗本轮只作为 iPad 工作台导航入口出现，避免在 GOAL-1502 / GOAL-1503 前提前写入不稳定数据模型。

#### 13.7.3 本地化

新增 iPad 工作台相关文案，覆盖：

- 简体中文
- 繁体中文
- 英文

新增 key 包括 `ipad.workspace.title`、`ipad.workspace.capture`、`ipad.workspace.ledger`、`ipad.workspace.reports`、`ipad.workspace.review_queue`、`ipad.workspace.cleaning`、`ipad.workspace.settings` 及两个占位说明。

#### 13.7.4 验证结果

| 验证项 | 结果 | 备注 |
|--------|------|------|
| `xcodebuild -workspace AutoLedger/AutoLedger.xcworkspace -scheme AutoLedger -configuration Debug -destination 'generic/platform=iOS' build` | PASS | 修正 iOS 上不可用的 `List(selection:)` 写法后通过 |
| `bash scripts/run_offline_regression.sh` | PASS | 离线回归通过；仅有既有 Swift warning |
| `bash scripts/run_golden_regression.sh` | PASS | Golden regression passed: 32 case(s)；仅有既有 Swift warning |
| `git diff --check` | PASS | 无 whitespace 问题 |

#### 13.7.5 明确未做

- 未把 iPad 工作台接入候选账单队列。
- 未实现批量导入 / 批量 OCR。
- 未新增 SQLite 表或迁移。
- 未修改 BackupBundle schema。
- 未实现多账本。
- 未实现 iPad 截图导出脚本。
- 未把 Mac Catalyst 纳入本轮范围。

#### 13.7.6 后续依赖

GOAL-1530 之后，iPad 线继续推进前仍建议补齐：

- GOAL-1501：默认账本与今日支出口径。
- GOAL-1502：候选账单状态模型。
- GOAL-1503：SQLite / BackupBundle 迁移方案。
- GOAL-1531：iPad 工作台骨架深化，把列表 / 表格 / 检查器从占位推进为真实工作区。

### 13.8 GOAL-1531 执行记录（2026-06-01）

状态：DONE（iPad 工作台深化，可进入真机部署测试）。

本轮继续 iPad 线，目标从“iPad 有入口”推进到“iPad 上有可操作的真实工作台”。范围仍限定在现有 `LedgerStore` 和现有交易链路之上，不抢跑候选账单模型、批量 OCR、数据清洗执行器、多账本 schema 或 BackupBundle 迁移。

#### 13.8.1 iPad 工作台深化

- iPad sidebar 新增“总览”作为默认入口。
- 总览页显示本月支出、全部账单数、Top 商户、最近账单和整理工作流入口。
- 导入、分析、设置继续复用现有主路径，避免拆出另一套不可维护页面。
- 候选账单与数据清洗保留为规划工作区，展示后续 checklist，不写入临时数据模型。

#### 13.8.2 iPad 账本工作区

iPad 账本页从复用手机 `LedgerView` 升级为宽屏工作区：

| 区域 | 行为 |
|------|------|
| 左侧列表 | 展示真实 `store.transactions`，按时间倒序排列，支持选中态和下拉刷新 |
| 右侧检查器 | 展示商户、金额、日期、分类、来源、备注和关键字段 |
| 操作 | 支持新增账单、语音记账、编辑选中账单和删除选中账单 |
| 空状态 | 引导继续通过导入 / 手动新增 / 语音入口进入现有链路 |

当前没有新增 iPad 专属持久化层，所有账单操作仍走主 App 既有 `LedgerStore`、SQLite、Widget 刷新和现有编辑器。

#### 13.8.3 本地化

补齐 iPad 工作台深化相关文案，覆盖：

- 简体中文
- 繁体中文
- 英文

新增 key 覆盖总览指标、最近账单、整理工作流、账单详情、候选账单规划项和数据清洗规划项。

#### 13.8.4 部署验证结果

| 验证项 | 结果 | 备注 |
|--------|------|------|
| `git diff --check` | PASS | 无 whitespace 问题 |
| `xcodebuild -workspace AutoLedger/AutoLedger.xcworkspace -scheme AutoLedger -configuration Debug -destination 'generic/platform=iOS' build` | PASS | 主 App、Watch App、Widgets、Share Extension 随依赖构建通过 |
| `xcodebuild -workspace AutoLedger/AutoLedger.xcworkspace -scheme AutoLedger -configuration Debug -destination 'platform=iOS Simulator,name=iPad Pro 13-inch (M5)' build` | PASS | iPad Simulator 目标构建通过 |
| `xcrun simctl install ... AutoLedger.app` | PASS | 安装到 iPad Pro 13-inch (M5) Simulator 成功 |
| `xcrun simctl launch ... top.darkrio326.AutoLedger` | PASS | 应用进程启动成功 |
| iPad Simulator 截图目检 | PASS | iPad sidebar、总览指标卡、最近账单空状态和整理工作流入口渲染正常；首次启动出现系统通知权限弹窗属预期 |
| `bash scripts/run_offline_regression.sh` | PASS | 离线回归通过；仅有既有 Swift warning |
| `bash scripts/run_golden_regression.sh` | PASS | Golden regression passed: 32 case(s)；仅有既有 Swift warning |

#### 13.8.5 真机 iPad 测试步骤

真机验证建议使用 Xcode：

1. 打开 `AutoLedger/AutoLedger.xcworkspace`。
2. 选择 `AutoLedger` scheme。
3. 选择已信任开发者证书的真机 iPad。
4. 确认 Signing Team 仍为当前发布链配置，不修改 Bundle ID、App Group 或 iCloud Container。
5. Run 到真机。
6. 首次启动允许或拒绝通知均可，不影响 iPad 工作台验证。
7. 检查总览、导入、账本、分析、候选账单、数据清洗、设置七个 sidebar 入口。
8. 在账本页验证新增 / 编辑 / 删除 / 语音入口是否能打开现有流程。

#### 13.8.6 明确未做

- 未实现候选账单真实队列。
- 未实现批量导入 / 批量 OCR。
- 未实现数据清洗执行逻辑。
- 未新增 SQLite 表或迁移。
- 未修改 BackupBundle schema。
- 未实现多账本。
- 未实现 iPad 截图导出脚本。
- 未把 Mac Catalyst 纳入本轮范围。

#### 13.8.7 后续依赖

GOAL-1531 之后，iPad 已可进入真机部署测试；继续做真实 iPad 生产力功能前仍建议按顺序补齐：

- GOAL-1501：默认账本与今日支出口径。
- GOAL-1502：候选账单状态模型。
- GOAL-1503：SQLite / BackupBundle 迁移方案。
- GOAL-1540：批量导入与识别的任务队列边界。

### 13.9 全平台扩展路线更新（2026-06-01）

状态：DONE（文档规划更新，不编码）。

本轮按用户要求把当前 v1.5.0 计划从 iPad / Watch 主线扩展为全平台路线。更新后的顺序是：

```text
iPhone / Watch 快速记录与随手查看
→ iPad 批量导入、复核、清洗、多账本工作台
→ Mac Catalyst 桌面生产力工作台
→ tvOS 家庭大屏只读看板
→ visionOS 空间展示版本
```

#### 13.9.1 Mac Catalyst 路线

Mac 端不再只是“复用评估”，而是 iPad 工作台稳定后的明确下一阶段。首版能力规划为：

- 拖拽截图 / 文件导入。
- CSV / JSON 导入导出。
- 键盘快捷键。
- 更大的表格 / 列表编辑区域。
- 批量选择、批量修正、重复账单检查。
- 基础菜单栏：`Import` / `Export` / `Backup` / `Settings`。

Mac 端仍不进入当前 App Store v1.4.0 首版承诺；它依赖 iPad 的候选队列、数据清洗、多账本和导入导出能力稳定后再推进。

#### 13.9.2 tvOS 路线

tvOS 定位为只读家庭大屏看板，不提供导入、编辑、清洗或删除能力。首版展示：

- 本月支出总览。
- 分类占比。
- 最近消费趋势。
- 年度 / 月度摘要。
- 家庭大屏看板。

tvOS 必须跟随统一统计口径，并提供隐私隐藏，避免大屏暴露具体个人消费明细。

#### 13.9.3 visionOS 路线

visionOS 定位为空间展示版本，不替代 iPad / Mac 的生产力工作台。首版展示：

- 月度支出空间看板。
- 分类支出卡片漂浮展示。
- 年度消费时间线墙。
- 最近账单悬浮列表。

visionOS 首版只读展示，不做拖拽导入、批量编辑或数据清洗。

#### 13.9.4 GOAL 队列调整

新增 / 扩展 GOAL：

- GOAL-1570～GOAL-1575：Mac Catalyst 接入评估、拖拽导入、CSV / JSON、快捷键与菜单栏、大表格批量编辑、重复账单检查。
- GOAL-1580～GOAL-1581：tvOS 只读看板设计与实现评估。
- GOAL-1582～GOAL-1583：visionOS 空间展示设计与实现评估。
- GOAL-1590～GOAL-1592：全平台截图管线设计、iPad 截图管线实现、全平台发布回归矩阵与 Review Notes。

#### 13.9.5 本轮验证

| 验证项 | 结果 | 备注 |
|--------|------|------|
| `git status --short --branch` | PASS | 更新前 main 干净 |
| `git diff --check` | PASS | 文档变更无 whitespace 问题 |

#### 13.9.6 明确未做

- 未开启 Mac Catalyst。
- 未新增 macOS / tvOS / visionOS target。
- 未修改 Xcode project / workspace / scheme / target。
- 未修改 Bundle ID、DEVELOPMENT_TEAM、App Group、iCloud Container 或 entitlements。
- 未实现拖拽导入、CSV / JSON、菜单栏、tvOS 看板或 visionOS 空间界面。

### 13.10 GOAL-1501 执行记录（2026-06-01）

状态：DONE（默认账本与今日支出口径定义）。

本轮只做口径定义，不改业务代码。结论写入 `3.3 默认账本与今日支出口径（GOAL-1501）`，作为 Watch 今日支出、Widget、iPad 总览、Mac Catalyst、tvOS 和 visionOS 展示的共同统计基础。

#### 13.10.1 当前工程事实

- 当前 `Transaction` 没有 `ledgerId`、`currencyCode`、`transactionType`、`createdAt` 或 `updatedAt` 模型字段。
- 当前 `LedgerStore.transactions` 代表活跃正式账单；`deletedTransactions` 单独保存最近删除。
- 当前 `MonthlySnapshot.build` 按 `occurredAt` 的年月过滤，并直接累加 `amount`。
- 当前 `AppFormatters.currency` 固定 CNY / `¥` 展示。

#### 13.10.2 本轮口径

| 主题 | 结论 |
|------|------|
| 默认账本 | 多账本上线前使用虚拟 `default-local-ledger`；当前所有活跃正式账单视为属于该账本 |
| 默认账本名称 | `本地账本` / `Local Ledger` |
| 今日边界 | 用户本地日历日 `[localStartOfDay, nextLocalStartOfDay)`，不使用 UTC 自然日 |
| 日期字段 | 使用 `occurredAt`；导入时间、识别时间、备份时间不决定今日归属 |
| 金额字段 | 只统计 `amount > 0` |
| 已删除账单 | 不计入活跃统计 |
| 候选账单 | 用户确认前不计入正式账本和今日支出 |
| 订阅 | 订阅元数据不计入；只有生成正式 `Transaction` 的扣费才计入 |
| 多币种 | 当前不混合未换算币种；未来需增加币种 / 汇率字段后再扩展 |
| 展示平台 | Watch / Widget / tvOS / visionOS 默认展示默认账本；iPad / Mac 可显式选择账本或全部账本 |

#### 13.10.3 后续测试设计

GOAL-1510 今日支出数据服务实现时，至少补齐：

- 今天正金额正式账单计入。
- 昨天账单不计入今日。
- 已删除账单不计入。
- 候选账单不计入。
- 0 或负数金额不计入。
- 自定义分类 / 自定义来源账单可计入。
- 本地时区 00:00 边界正确。
- 多账本迁移后默认账本与全部账本范围可区分。

#### 13.10.4 本轮验证

| 验证项 | 结果 | 备注 |
|--------|------|------|
| `git status --short --branch` | PASS | 本轮开始时仅有上一轮文档改动 |
| `git diff --check` | PASS | 文档变更无 whitespace 问题 |

#### 13.10.5 明确未做

- 未新增 `Ledger` 模型。
- 未修改 `Transaction`、SQLite 或 BackupBundle。
- 未实现今日支出数据服务。
- 未修改 Watch / Widget / iPad / Mac / tvOS / visionOS UI。
- 未修改 Xcode project、scheme、target、Bundle ID、signing、entitlements、App Group 或 iCloud Container。

#### 13.10.6 下一步

推荐进入 GOAL-1502：候选账单状态模型设计。它会决定 iPad / Mac 批量导入、复核、重复提示和正式入账的状态流。

### 13.11 GOAL-1502 执行记录（2026-06-01）

状态：DONE（候选账单状态模型设计）。

本轮只做模型契约和状态流文档，不改 Swift、SQLite、BackupBundle、Xcode project 或发布链配置。结论写入 `5.7.1 候选账单状态模型（GOAL-1502）`，作为 iPad / Mac 批量导入、复核、清洗、重复提示和正式入账的共同设计基础。

#### 13.11.1 当前工程事实

- 当前 `Transaction` 只有正式账单核心字段，没有候选状态、批次、置信度、重复提示、ledgerId 或 currencyCode。
- 当前 `ImportedReceipt`、`InterpretResult` 已有 `confidence`、`needsReview`、`warnings` 等识别质量信号，但尚未形成可持久化候选队列。
- 当前 `BackupBundle.schemaVersion = 1` 只适合正式账单和删除状态，不覆盖候选区、原始输入或批量导入事件。
- 当前 iPad 工作台已有候选账单入口，但仍是规划工作区，不接真实队列。

#### 13.11.2 本轮口径

| 主题 | 结论 |
|------|------|
| 状态流 | `rawInput -> candidate -> reviewed -> transaction`，并支持 raw / candidate 转 `rejected` |
| 统计边界 | 只有正式 `transaction` 进入账本和统计；其余状态全部排除 |
| 失败原因 | 使用稳定枚举承载 empty input、OCR failure、non-bill、缺金额、缺商户、低置信、疑似重复等场景 |
| 置信度 | High / Medium / Low 只影响复核优先级，不允许批量自动入账 |
| 重复提示 | 只提示和分组，不自动删除候选或正式账单 |
| 隐私 | 原图、OCR 全文、PDF 文本属于敏感输入，备份和导出必须显式区分 |
| 后续落库 | 建议拆分 import batches、raw inputs、candidate transactions、candidate events |

#### 13.11.3 后续测试设计

GOAL-1503 / GOAL-1540 / GOAL-1550 实现模型、队列和清洗能力时，至少覆盖：

- raw input 创建后不进入统计。
- OCR 失败保留 raw input 和失败原因。
- 非账单图片可转 rejected。
- 缺金额 / 缺商户 / 缺日期候选必须复核。
- 高置信候选不能绕过用户确认直接入账。
- 疑似重复只提示，不自动删除。
- reviewed 保存成功后才生成正式 `Transaction`。
- rejected 和 purged raw input 不影响今日支出与月报。

#### 13.11.4 本轮验证

| 验证项 | 结果 | 备注 |
|--------|------|------|
| `git diff --check` | PASS | 文档变更无 whitespace 问题 |

#### 13.11.5 明确未做

- 未新增候选账单 Swift model。
- 未新增 SQLite 表或迁移。
- 未升级 BackupBundle。
- 未实现批量导入队列。
- 未接 iPad / Mac 候选区真实 UI。
- 未修改 Xcode project、scheme、target、Bundle ID、signing、entitlements、App Group 或 iCloud Container。

#### 13.11.6 下一步

推荐进入 GOAL-1503：评估 SQLite / BackupBundle 对多账本与候选账单的 schema 缺口，形成兼容旧数据、默认账本、候选区落库和备份升级方案。

### 13.12 GOAL-1503 执行记录（2026-06-01）

状态：DONE（SQLite / BackupBundle schema 缺口评估）。

本轮只做迁移方案文档，不改 Swift、SQLite、BackupBundle、Xcode project 或发布链配置。结论写入 `5.9 SQLite / BackupBundle 迁移方案（GOAL-1503）`，作为后续多账本、候选队列、批量清洗和 Mac CSV / JSON 导入导出的 schema 基础。

#### 13.12.1 当前工程事实

- `Transaction` 当前不包含 `ledgerId`、`currencyCode`、`transactionType`、`createdAt`、`updatedAt`。
- `transactions` 表已有 `created_at`、`updated_at`、`deleted_at`，但没有 `ledger_id`、`currency_code` 或 `transaction_type`。
- SQLite 当前表包括 `transactions`、`debug_events`、`subscriptions`、`category_corrections`、`merchant_aliases`。
- `debug_events` 保存 OCR / LLM 调试记录，但不是可复核、可重试、可批量确认的候选队列表。
- 自定义分类 / 来源仍主要在 UserDefaults，BackupBundle v1 以数组导出。
- `BackupBundle.schemaVersion = 1`，`BackupValidator` 当前只接受 schema 1。
- `restoreBackup` 通过 `replaceForRestore` 替换正式账单、订阅、分类修正和商户别名，不处理 ledgers、candidates 或 raw inputs。

#### 13.12.2 本轮结论

| 主题 | 结论 |
|------|------|
| 迁移方式 | additive first；新增表 / 可默认列，不重建正式账单表 |
| schema version | 建议引入 `PRAGMA user_version`，保留列检测作为兼容小迁移 |
| 默认账本 | 新增 `ledgers` 表，固定默认 ID 为 `default-local-ledger` |
| 交易扩展 | `transactions` 增加 `ledger_id`、`transaction_type`、`currency_code` 等字段 |
| 候选区 | 独立 `import_batches`、`raw_inputs`、`candidate_transactions`、`candidate_events` |
| 自定义分类 / 来源 | 后续迁入 SQLite，UserDefaults 保留兼容期 |
| BackupBundle | v2 支持 ledgers / transaction extensions / optional candidates；v1 必须继续可恢复 |
| 隐私 | 自动备份默认不包含原始图片、PDF 或 OCR 全文；完整备份需用户明确选择 |

#### 13.12.3 推荐实施顺序

1. 引入 `PRAGMA user_version`。
2. 新增 `ledgers` 表并插入 `default-local-ledger`。
3. 扩展 `transactions` 表并回填旧账单。
4. 新增候选区表。
5. 加候选区读写 API。
6. 设计并实现 BackupBundle v2，同时保留 v1 恢复。
7. 迁移自定义分类 / 来源到 SQLite。
8. 进入 GOAL-1540 / GOAL-1560 的队列和多账本编码。

#### 13.12.4 回归设计

后续编码时至少覆盖：

- 旧库迁移到默认账本。
- 重复迁移幂等。
- 正式账单查询不混入候选区。
- v1 backup 恢复到默认账本。
- v2 backup 在候选字段缺失时仍能恢复正式账单。
- 自动 iCloud backup 不包含 raw input 敏感内容。
- 候选区写入不影响今日支出与月报。

#### 13.12.5 本轮验证

| 验证项 | 结果 | 备注 |
|--------|------|------|
| `git diff --check` | PASS | 文档变更无 whitespace 问题 |
| `bash scripts/run_offline_regression.sh` | PASS | 文档变更后离线回归通过；仅有既有 Swift warning |

#### 13.12.6 明确未做

- 未修改 `Transaction`。
- 未新增 `Ledger` model。
- 未新增或迁移 SQLite 表。
- 未升级 BackupBundle。
- 未修改备份恢复实现。
- 未实现候选队列或多账本 UI。
- 未修改 Xcode project、scheme、target、Bundle ID、signing、entitlements、App Group 或 iCloud Container。

#### 13.12.7 下一步

推荐进入 GOAL-1510：实现今日支出数据服务，先把 GOAL-1501 的统计口径落成可测试服务，为 Watch 首屏和 Widget 铺路。另一条可选路线是进入 GOAL-1560，把本轮 schema 方案落成多账本迁移。

### 13.13 GOAL-1510 执行记录（2026-06-01）

状态：DONE（今日支出数据服务实现）。

本轮把 GOAL-1501 的统计口径落成 Core 级数据服务，不改 Watch UI、不改 Widget、不改 WatchConnectivity payload、不改 SQLite schema。后续 Watch 首屏和表盘小组件可以直接复用同一 summary。

#### 13.13.1 代码改动

| 文件 | 内容 |
|------|------|
| `AutoLedger/AutoLedgerCore/Sources/AutoLedgerCore/Models/TodaySpendingSummary.swift` | 新增今日支出 summary 模型和 `build` 方法 |
| `AutoLedger/AutoLedger/App/LedgerStore.swift` | 新增只读 `todaySpendingSummary` 属性 |
| `scripts/OfflineRegression.swift` | 新增今日支出口径回归用例 |
| `scripts/run_offline_regression.sh` | 将新 Core 文件加入离线编译清单 |

#### 13.13.2 实现口径

- 默认账本 ID 为 `default-local-ledger`，显示名为 `本地账本`。
- 输入为活跃正式账单数组；`LedgerStore` 只传 `transactions`，不传 `deletedTransactions`。
- 候选账单 / raw input / rejected 仍未编码，也不会进入该服务输入。
- 只统计 `amount > 0`。
- 使用传入 calendar 的 `[localStartOfDay, nextLocalStartOfDay)` 本地日区间。
- 最近一笔按 `occurredAt` 倒序。
- 最近展示名优先商户，商户为空时回退分类，再回退来源。

#### 13.13.3 回归覆盖

新增离线用例覆盖：

- 默认账本 ID / 名称。
- 今天正金额账单计入总额和笔数。
- 昨天账单排除。
- 0 / 负金额排除。
- 自定义分类 / 来源账单计入。
- 已删除账单通过 active input contract 排除。
- 空状态 total 为 0，recent 为 nil。
- 本地日边界 `[start, end)`，00:00 计入，次日 00:00 排除。
- 商户为空时展示名回退到分类。

#### 13.13.4 验证结果

| 验证项 | 结果 | 备注 |
|--------|------|------|
| `bash scripts/run_offline_regression.sh` | PASS | 新增 TodaySpendingSummary 用例通过；仅有既有 `nonisolated(unsafe)` warning |
| `xcodebuild -workspace AutoLedger/AutoLedger.xcworkspace -scheme AutoLedger -configuration Debug -destination 'generic/platform=iOS' build` | PASS | 主 App、Watch App、Widgets、Share Extension 随依赖构建通过 |

#### 13.13.5 明确未做

- 未修改 Watch 首屏 UI。
- 未实现 Watch 左滑最近支出第二屏。
- 未实现表盘小组件数据源。
- 未修改 SQLite schema 或 BackupBundle。
- 未新增多账本字段。
- 未修改 Xcode project、scheme、target、Bundle ID、signing、entitlements、App Group 或 iCloud Container。

#### 13.13.6 下一步

推荐进入 GOAL-1511：将 Watch 首屏切换为今日支出，展示今日总金额、笔数、最近商户、空状态和同步状态，同时保留快速记账入口。

### 13.14 GOAL-1520 执行记录（2026-06-01）

状态：PARTIAL DONE（iPhone 桌面 / 负一屏 Widget 今日支出口径已对齐；Watch 表盘小组件仍待后续 target / UI）。

本轮响应“今日支出是否覆盖 iPhone 桌面小组件与负一屏”的产品口径，先把现有 `DailyExpenseWidget` 的数据层与 GOAL-1510 今日支出口径对齐。由于当前工程已有 `AutoLedgerWidgetsExtension`，iPhone 桌面小组件和 iPhone 负一屏 / Today View 使用同一 WidgetKit 配置。

#### 13.14.1 代码改动

| 文件 | 内容 |
|------|------|
| `AutoLedger/AutoLedgerWidgets/AutoLedgerWidgets.swift` | 对齐 `DailyExpenseWidget` 今日支出数据口径 |
| `versions/v1.5.0-plan.md`、`CHANGELOG.md`、`process/iteration-log.md` | 回填 GOAL-1520 范围与执行结果 |

#### 13.14.2 实现口径

- 使用 `Calendar.autoupdatingCurrent` 计算本地今日区间。
- 今日支出 SQL 查询限定 `deleted_at IS NULL` 和 `amount > 0`。
- 日期查询格式改为 `SQLiteTransactionStore` 当前 ISO8601 fractional seconds 格式。
- 解析历史旧日期时保留 `yyyy-MM-dd HH:mm:ss` fallback。
- 最近一笔展示名按商户、分类、来源回退。
- 不新增 Xcode target 依赖，避免本轮把 Widget extension 强行接入 `AutoLedgerCore`。

#### 13.14.3 验证结果

| 验证项 | 结果 | 备注 |
|--------|------|------|
| `git diff --check` | PASS | 无 whitespace 问题 |
| `xcodebuild -workspace AutoLedger/AutoLedger.xcworkspace -scheme AutoLedger -configuration Debug -destination 'generic/platform=iOS' build` | PASS | `AutoLedgerWidgetsExtension` 编译、签名并嵌入主 App 成功 |

#### 13.14.4 明确未做

- 未新增 Watch complication / 表盘小组件 target。
- 未修改 Watch App UI。
- 未修改 Widget UI 视觉布局。
- 未修改 Bundle ID、DEVELOPMENT_TEAM、App Group、iCloud Container、entitlements、scheme 或 target 名称。
- 未新增隐私隐藏开关。

#### 13.14.5 下一步

推荐进入 GOAL-1511：先把 Watch App 首屏切换为今日支出。Watch 表盘小组件可在 GOAL-1521 或单独拆分的 Watch complication GOAL 中继续推进。

### 13.15 GOAL-1511 执行记录（2026-06-01）

状态：DONE（Watch 首屏已切换为今日支出）。

本轮将 GOAL-1510 的今日支出口径接入 iPhone -> Watch 同步 payload，并改造 Watch App 首屏。为避免用户失去最近账单入口，旧首屏列表以轻量形式保留为左滑第二页；更完整的单笔只读详情仍留给 GOAL-1512。

#### 13.15.1 代码改动

| 文件 | 内容 |
|------|------|
| `AutoLedger/AutoLedger/Domain/Services/WatchConnectivityHost.swift` | `syncTransactions` payload 新增 `todaySummary` |
| `AutoLedger/AutoLedgerWatch Watch App/WatchTransaction.swift` | 新增 Watch 侧 `WatchTodaySummary` 与旧 payload fallback |
| `AutoLedger/AutoLedgerWatch Watch App/WatchSessionManager.swift` | 接收并保存今日支出摘要 |
| `AutoLedger/AutoLedgerWatch Watch App/WatchLedgerViewModel.swift` | 暴露 `todaySummary` 给 SwiftUI |
| `AutoLedger/AutoLedgerWatch Watch App/ContentView.swift` | 首屏改为今日支出摘要，最近 5 笔保留为第二页 |
| `AutoLedger/AutoLedgerWatch Watch App/*.lproj/Localizable.strings` | 新增今日支出与最近支出三语文案 |
| `AutoLedger/AutoLedgerWatch Watch App/Screenshots/WatchScreenshotHostView.swift` | 更新 Watch 截图 fixture 的今日支出数据 |

#### 13.15.2 实现口径

- iPhone 端继续发送 `transactions` 与 `customCategories`，新增 `todaySummary` 字典。
- `todaySummary` 包含默认账本名、今日总额、今日笔数、最近展示名和同步更新时间。
- Watch 收到新 payload 后首屏显示今日支出摘要。
- Watch 收到旧 payload 或本地无 `todaySummary` 时，从最近账单按本地日区间做 fallback。
- 首屏保留语音记账和快速记账入口；第二页展示最近 5 笔支出。

#### 13.15.3 验证结果

| 验证项 | 结果 | 备注 |
|--------|------|------|
| `find 'AutoLedger/AutoLedgerWatch Watch App' -path '*lproj/Localizable.strings' -print0 | xargs -0 plutil -lint` | PASS | Watch 三套本地化文件合法 |
| `xcodebuild -workspace AutoLedger/AutoLedger.xcworkspace -scheme 'AutoLedgerWatch Watch App' -configuration Debug -destination 'generic/platform=watchOS' build` | PASS | Watch App 与嵌入主 App 构建通过 |

#### 13.15.4 明确未做

- 未实现单笔只读详情页。
- 未新增 Watch complication / 表盘小组件 target。
- 未新增隐私隐藏开关。
- 未修改 Watch pending 队列持久化格式。
- 未修改 SQLite schema、BackupBundle、多账本字段或候选账单模型。
- 未修改 Xcode project、scheme、target、Bundle ID、DEVELOPMENT_TEAM、entitlements、App Group 或 iCloud Container。

#### 13.15.5 下一步

推荐进入 GOAL-1512：补齐 Watch 第二页最近支出的单笔只读详情、空状态手感和真机尺寸点验。若先推进表盘入口，则进入 GOAL-1521。

### 13.16 GOAL-1512 执行记录（2026-06-01）

状态：DONE（Watch 左滑最近支出第二屏已补齐只读详情）。

本轮在 GOAL-1511 的左滑第二页基础上，补齐最近支出的当前账本提示、短时间文案和单笔只读详情。第二页仍保持只读和轻量，不引入 Watch 端编辑、删除、账本切换或批量操作。

#### 13.16.1 代码改动

| 文件 | 内容 |
|------|------|
| `AutoLedger/AutoLedger/Domain/Services/WatchConnectivityHost.swift` | 最近账单 payload 增加分类和来源展示字段 |
| `AutoLedger/AutoLedgerWatch Watch App/WatchTransaction.swift` | 扩展 Watch 侧交易模型，新增分类、来源、相对日期、详情时间和备注兜底 |
| `AutoLedger/AutoLedgerWatch Watch App/ContentView.swift` | 第二页最近支出行改为可进入只读详情的 `NavigationLink` |
| `AutoLedger/AutoLedgerWatch Watch App/*.lproj/Localizable.strings` | 新增详情、来源、备注、今天 / 昨天三语文案 |
| `CHANGELOG.md`、`process/iteration-log.md`、`versions/v1.5.0-plan.md` | 回填 GOAL-1512 执行结果 |

#### 13.16.2 实现口径

- 第二页顶部展示最近支出标题和当前默认账本名。
- 最近支出仍限制最近 5 笔，按 iPhone 端同步顺序展示。
- 行内时间使用“今天 / 昨天 / MM/dd + HH:mm”，避免完整时间占用 Watch 小屏。
- 点按单笔进入只读详情，展示金额、商户、分类、来源、完整时间和备注。
- 旧 payload 未带分类或来源时，详情页显示安全兜底，不影响旧同步数据。

#### 13.16.3 验证结果

| 验证项 | 结果 | 备注 |
|--------|------|------|
| `git diff --check` | PASS | 无 whitespace 问题 |
| `find 'AutoLedger/AutoLedgerWatch Watch App' -path '*lproj/Localizable.strings' -print0 | xargs -0 plutil -lint` | PASS | Watch 三套本地化文件合法 |
| `xcodebuild -workspace AutoLedger/AutoLedger.xcworkspace -scheme 'AutoLedgerWatch Watch App' -configuration Debug -destination 'generic/platform=watchOS' build` | PASS | Watch App 与嵌入主 App 构建通过 |

#### 13.16.4 明确未做

- 未实现 Watch 端编辑、删除或账本移动。
- 未实现详情页跳转 iPhone 深链。
- 未新增 Watch complication / 表盘小组件 target。
- 未修改 Watch pending 队列持久化格式。
- 未修改 SQLite schema、BackupBundle、多账本字段或候选账单模型。
- 未修改 Xcode project、scheme、target、Bundle ID、DEVELOPMENT_TEAM、entitlements、App Group 或 iCloud Container。

#### 13.16.5 下一步

推荐进入 GOAL-1521 或拆分 Watch complication 专项：实现表盘小组件 UI 与点击进入 Watch 今日支出页。进入下一轮前建议先用真实 Apple Watch 点验首屏、左滑第二页、详情页返回和 iPhone / Watch 同步延迟。

### 13.17 GOAL-1521A 执行记录（2026-06-01）

状态：PARTIAL DONE（Widget accessory UI 已实现；真正 Watch complication target 未接入）。

本轮先在现有 `AutoLedgerWidgetsExtension` 内为 `DailyExpenseWidget` 增加 accessory family UI，复用 GOAL-1520 的今日支出数据源。由于当前工程没有独立 watchOS WidgetKit extension target，本轮不直接改 `project.pbxproj` 和 Watch App 嵌入关系，避免把可发布链路变成不可构建状态。

#### 13.17.1 代码改动

| 文件 | 内容 |
|------|------|
| `AutoLedger/AutoLedgerWidgets/AutoLedgerWidgets.swift` | 新增 accessory inline / circular / rectangular 三种今日支出 UI；扩展 `DailyExpenseWidget.supportedFamilies` |
| `CHANGELOG.md`、`process/iteration-log.md`、`versions/v1.5.0-plan.md` | 记录 GOAL-1521A 范围和 GOAL-1521B target 缺口 |

#### 13.17.2 实现口径

- `.systemSmall` 保留原有 iPhone 桌面 / 负一屏 Widget UI。
- `.accessoryInline` 显示 `今日支出 ¥xx` / `Today ¥xx`。
- `.accessoryCircular` 显示压缩金额和今日笔数。
- `.accessoryRectangular` 显示标题、压缩金额和今日笔数。
- 三种 accessory UI 继续复用 `WidgetLedgerStore.loadMetrics()`，不新增网络、不上传数据。
- 金额在 accessory 形态下使用压缩格式，避免小尺寸截断。

#### 13.17.3 验证结果

| 验证项 | 结果 | 备注 |
|--------|------|------|
| `git diff --check` | PASS | 无 whitespace 问题 |
| `xcodebuild -workspace AutoLedger/AutoLedger.xcworkspace -scheme AutoLedgerWidgetsExtension -configuration Debug -destination 'generic/platform=iOS' build` | PASS | iPhoneOS Widget extension 构建通过；主 App 嵌入验证通过 |

#### 13.17.4 明确未做

- 未新增 watchOS WidgetKit extension target。
- 未把 Widget extension 嵌入 Watch App。
- 未修改 Bundle ID、DEVELOPMENT_TEAM、App Group、iCloud Container、entitlements、scheme 或 target 名称。
- 未实现表盘点击深链。
- 未新增隐私隐藏开关。
- 未做 Apple Watch 表盘真机添加验证。

#### 13.17.5 下一步

推荐单独进入 GOAL-1521B：新增 watchOS WidgetKit extension target，并以 Xcode Cloud 发布链安全为最高优先级验证 Watch complication 真正可添加到表盘。
