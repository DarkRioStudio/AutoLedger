# v1.5.1 版本计划

更新日期：2026-06-15
文档状态：开发中
内部开发线：v1.5.1
App Store 对外版本：v1.4.0
承接版本：`v1.5.0` 开发基线
关联文档：`versions/v1.5.0-plan.md`、`versions/v1.5.0-regression-baseline.md`、`versions/v1.5.0-review-notes.md`、`docs/minimum-platform-baseline-reduction-plan.md`、`docs/autoledgercore-platform-dependency-audit.md`

## 1. 版本定位

`v1.5.1` 不再只是 `v1.5.0` 的补丁号，而是当前开发线正式承接版本。

本轮决策调整如下：

1. `v1.5.0` 已完成 iPhone / iPad / Apple Watch / Mac Catalyst 的主线能力铺设，以及截图、回归资料、Review Notes 等发布前基础准备。
2. `v1.5.0` 不单独做最终 smoke、Xcode Cloud 收口和提审动作；这些门禁延后到 `v1.5.1` 全部内容完成之后统一执行。
3. `v1.5.1` 承接两类任务：
   - `v1.5.0` 遗留的最终收口、smoke、Xcode Cloud 和 App Store Connect 动作
   - 全平台内容补齐与最低系统需求优化
   - `v1.5.0` 遗留的账单 OCR / 文本识别链路 Core 化重构
   - 脱敏外部大模型 API 辅助识别接入
4. 多账本继续顺延，不进入 `v1.5.1` 当前实现范围。

因此，`v1.5.1` 的真实角色是：

- `v1.5.0` 主线成果的整合版
- 全平台第一阶段闭环版
- 最低系统需求下调的实施版
- 识别链路平台无关核心能力的重构版
- 外部大模型辅助识别的受控试点版
- App Store `1.4.0` 的最终候选开发线

## 1.1 版本号策略

| 场景 | 版本规则 |
|------|----------|
| 内部开发文档 / Git Tag | `v1.5.1` |
| TestFlight | App Store 版本保持 `1.4.0`，Build Number 持续递增 |
| App Store | 对外仍发布 `1.4.0` |
| `v1.5.0` | 视为当前实现基线，不再单独执行最终提审 smoke |
| 后续开发线 | `v1.6.0+` 承接多账本和更大范围的数据模型升级 |

## 2. 当前承接内容

### 2.1 从 `v1.5.0` 承接的遗留收口项

以下事项不再在 `v1.5.0` 单独收口，而是在 `v1.5.1` 完成后统一执行：

1. 全量 smoke
2. 最终真机回归
3. Xcode Cloud validation / archive / TestFlight 验证
4. App Store Connect 最终 Review Notes、截图和提审动作
5. Watch 后台刷新与复杂场景复测
6. IAP 真机 / 沙盒购买复测
7. 账单 OCR / 文本识别链路重构与 Core 层解析能力提升
8. 脱敏外部大模型 API 辅助识别接入与隐私边界验证

### 2.2 从 `v1.5.0` 继承的已完成基线

`v1.5.1` 不需要重新定义这些方向，而是在其上继续落地：

- iPhone / iPad / Apple Watch / Mac Catalyst 主线
- iCloud 同步基础链路
- iPad 工作台
- Mac Catalyst 生产力工作流
- iPhone / Watch / iPad / Mac 截图导出基线
- tvOS / visionOS 的 target、设计稿和实现评估

## 3. `v1.5.1` 目标矩阵

### 3.1 平台与最低系统目标

| 平台 / Target | `v1.5.1` 目标最低版本 | 说明 |
|---|---:|---|
| Main App / iPhone / iPad | `iOS 17.0` 优先 | 若共享 Core 当前仍强依赖 `iOS 18` API，则先拆出 `CoreBase`，保证基础记账链路可在 `iOS 17` 编译 |
| Mac Catalyst | `macOS 14` 对应线 | 跟随 `iOS 17` 主线，不另起一套高版本基线 |
| Watch App / Watch Widget | `watchOS 10.0` | 保留今日支出、最近支出、Widget 与表盘能力 |
| tvOS App | `tvOS 17.0` | 实现第一版只读账本看板 |
| visionOS App | `visionOS 1.0` | 实现第一版空间展示版 |
| ControlWidgetExtension | `iOS 18.0` | 允许单独保留高版本，不反向拖住主 App |
| Foundation Models / Apple Intelligence | `iOS 26+` optional enhancement | 只能作为增强层存在，不得污染主 App、`CoreBase`、ViewModel 和通用数据模型 |

### 3.2 结构原则

- 不允许为了通过低系统编译，把主流程、主页面、核心数据类型整体标记为 `@available(iOS 26.0, *)`
- 低系统主链路必须通过 `CoreBase / Adapter / Extension / FeatureGate` 分层实现
- `ClipboardImportIntent` 已迁出 `AutoLedgerCore`，后续 Core 继续保持 Foundation / SQLite / 通用服务边界
- `Intent Adapter`：`AppIntents`、控制组件等高层入口不进入 Core
- `FeatureGate`：Apple Foundation Models、Apple Intelligence 等增强能力继续保持 `iOS 26+` 可选路径

## 4. `v1.5.1` 核心目标

### 4.1 当前主线平台稳定收口

- iPhone / iPad / Apple Watch / Mac Catalyst 继续消化已知交互、同步、后台刷新和截图问题
- 把 `v1.5.0` 的回归资料转为 `v1.5.1` 最终提审基线
- 将最终 smoke、Xcode Cloud、ASC 提交动作统一后置到本版本末尾

### 4.2 tvOS 第一版落代码

按已有设计与评估文档，完成 tvOS 只读账本看板第一版：

- 总览
- 分类占比
- 最近消费趋势
- 年度 / 月度摘要
- 家庭大屏只读模式

边界：

- 只读
- 单一正式账本
- 不做导入 / 编辑 / 清洗 / 多账本切换

### 4.3 visionOS 第一版落代码

按已有设计与评估文档，完成 visionOS 第一版展示：

- 月度支出空间看板
- 分类支出卡片漂浮展示
- 年度消费时间线墙
- 最近账单悬浮列表

边界：

- 首版以 `WindowGroup + SwiftUI` 为主
- 不把 immersive space 作为首版硬门槛
- 只读、单一正式账本

### 4.4 最低系统需求优化实施

按 `docs/minimum-platform-baseline-reduction-plan.md` 执行：

1. 梳理 `AutoLedgerCore` 的平台依赖
2. 必要时拆出 `CoreBase`
3. 下调主 App / Watch / tvOS / visionOS 的 deployment target
4. 保留 `ControlWidgetExtension` 的 `iOS 18`
5. 保留 Apple Foundation Models 的 `iOS 26+` 增强路径

GOAL-1601 审计结论：

- `AutoLedgerCore` 当前唯一高层平台依赖命中是 `AppIntents`。
- 命中文件是 `AutoLedger/AutoLedgerCore/Sources/AutoLedgerCore/Intents/ClipboardImportIntent.swift`。
- 其他 Core 主要代码仍是 `Foundation` / `SQLite3` 级别。
- `v1.5.1` 不需要先做大规模 `CoreBase` 拆包；应先在 `GOAL-1602` 将 `ClipboardImportIntent` 迁出为 App / Extension 层 Intent Adapter，再下调 `AutoLedgerCore/Package.swift`。

GOAL-1602 实施结果：

- `ClipboardImportIntent` 已从 `AutoLedgerCore` 迁出，主 App 与 Control Widget 各保留薄 App Intent 入口。
- `AutoLedgerCore` 已下调到 `iOS 17 / macOS 14 / watchOS 10`。
- 主 App / Share Extension / iOS Widget 已下调到 `iOS 17.0`。
- Watch App / Watch Widget 已下调到 `watchOS 10.0`。
- tvOS 已下调到 `tvOS 17.0`。
- visionOS 已下调到 `visionOS 1.0`。
- `ControlWidgetExtension` 已单独保留 `iOS 18.0`。
- `RealityKitContent` package 和 CocoaPods baseline 已同步到目标矩阵。
- Apple Foundation Models / `SmartReceiptParser.parse` 已通过 `iOS 26+` gate 保留为增强路径，低系统默认走规则解析。

### 4.5 全平台发布资产与 smoke 延后收口

本轮版本结束前，再统一处理：

- iPhone / iPad / Watch / Mac / tvOS / visionOS 截图资产
- 全平台 smoke
- `iOS 17` / `iOS 18` / `iOS 26` 构建验证
- Xcode Cloud validation build
- App Store Connect 最终提交资料

### 4.6 Shortcuts 结构化账单导入补充

为当前单账本主线补一条自动化入口：

- Shortcuts / 剪贴板可传入结构化账单 JSON
- App Intent 解析金额、商户、分类、日期、备注、币种和置信度
- 高置信度自动保存
- 中置信度打开确认页复核
- 低置信度或缺少关键字段返回错误，不写入账本

边界：

- 只支持单笔账单 JSON，不做批量 JSON 数组
- 不保存原始 JSON、截图、小票图片或 OCR 全文
- 不修改正式账本 SQLite schema
- `currency` 暂存入备注，不新增币种字段

### 4.7 账单 OCR / 文本识别链路 Core 化重构

`v1.5.1` 承接 `v1.5.0` 遗留的识别链路稳定性目标，但本轮不再继续在 `ReceiptParser`、`SmartReceiptParser`、`LedgerTextInterpreterCore` 中分别堆叠互相牵扯的规则，而是将平台无关解析能力收回 `AutoLedgerCore`。

目标 Pipeline：

```text
OCR / 原始文本
-> TextClean + PaymentAmountExtractor
-> Merchant Extraction: 规则候选 + LLM 候选
-> MerchantResolver / MerchantNormalizer
-> CategoryResolver
-> TransactionDraft / ImportedReceipt 适配
```

核心原则：

- 金额只由规则提取，不由 LLM 决定。
- LLM 只辅助商户名称候选，不负责金额，不负责最终分类。
- 每条规则输出 `candidate + evidence + confidence`，不直接覆盖最终结果。
- 最终金额、商户和分类由 resolver 统一选择。
- 商户选择不再依赖 OCR 文本中的简单先后顺序，而是基于分词后的商户可能性、上下文证据、黑名单 / 白名单、支付渠道排除和历史别名信号综合排序。
- 平台无关逻辑必须留在 `AutoLedgerCore`，不得引入 `UIKit`、`SwiftUI`、`Vision`、`FoundationModels`、`CloudKit` 等平台依赖。
- 不改 UI、不改 SQLite schema、不改用户可见入账流程，优先做内部架构收口和回归稳定。

第一阶段目标：

- 在 `AutoLedgerCore/Sources/AutoLedgerCore/Services/` 新增平台无关解析模块。
- 将 `LedgerTextInterpreterCore` 里现有金额规则封装为 `PaymentAmountExtractor`，输出 `AmountExtractionResult`，包含 paid amount、候选、confidence、rule、evidence、debug 信息和近似标记。
- 将商户规则封装为 `RuleMerchantExtractor` / `MerchantExtractor`，输出 `[MerchantCandidate]`，候选包含 name、source、confidence、lineIndex、evidence、ruleName。
- 新增 `MerchantResolver`、`MerchantNormalizer`、`CategoryResolver`，让商户选择、商户标准化和分类推断从散落规则中独立出来。
- `MerchantResolver` 需要从“行序推断”升级为“候选评分”模型：先将 OCR 行拆成 merchant token / phrase candidate，再根据支付关键词距离、金额关键词距离、发票 / 小票标题噪声、支付渠道黑名单、主体名特征、历史别名和 LLM 候选一致性计算优先级。
- 修改 `LedgerTextInterpreterCore.interpret` 使用新的 Core Pipeline，保持公开 API 和 `TransactionDraft` 输出结构不变。
- 修改 App 层 `ReceiptParser.swift`，逐步复用 Core 层金额、商户 resolver 和分类 resolver；保留纸质小票、微信详情块、抖音 / 美团券码、滴滴、支付宝 / 淘宝、云闪付等现有特殊逻辑，避免一次性重写导致回归风险。
- 修改 `SmartReceiptParser.swift` 的职责边界：规则先锁定金额，LLM 只输出商户候选；分类仍走 `CategoryResolver`。如果本轮风险较高，可先保留旧入口并加适配层，但不得让 LLM 金额覆盖规则金额。
- 扩展 `scripts/OfflineRegression.swift`，新增 `PaymentAmountExtractor`、`MerchantResolver`、`MerchantNormalizer` 独立回归，并保留既有 `LedgerTextInterpreterCore` 样例。

GOAL-1608A 实施结果：

- 已新增 `PaymentAmountExtractor`、`AmountCandidate`、`AmountExtractionResult` 和 `AmountRole`，全部位于 `AutoLedgerCore`，仅依赖 `Foundation`。
- 已将 `LedgerTextInterpreterCore` 的金额选择入口切换到 `PaymentAmountExtractor`，保持 `InterpretResult` / `TransactionDraft` 公开输出结构不变。
- 已把既有 `TOTAL RM`、合计行优先、找零 / 现金排除、订单号 / 注册号 / 时间 / 商品编号排除和无标签金额 fallback 行为迁入新提取器。
- 已扩展 `scripts/OfflineRegression.swift`，新增 `PaymentAmountExtractor` 独立回归，并保留既有 `LedgerTextInterpreterCore` 金额样例。
- 本步未改商户选择、分类推断、`ReceiptParser` 特殊平台规则、`SmartReceiptParser` LLM 职责、UI、SQLite schema 或用户可见入账流程。

GOAL-1608B 实施结果：

- 已新增 `MerchantCandidate`、`MerchantResolutionResult`、`MerchantCandidateSource`、`MerchantNormalizer`、`RuleMerchantExtractor` 和 `MerchantResolver`，全部位于 `AutoLedgerCore`，仅依赖 `Foundation`。
- 已将 `LedgerTextInterpreterCore` 的商户选择入口切换到 `RuleMerchantExtractor + MerchantResolver`，保持 `InterpretResult` / `TransactionDraft` 公开输出结构不变。
- 已把标签商户、支付渠道 / 发票页眉黑名单、注册号 / 订单号 / 时间 / 商品编号 / 数量 / 金额行排除和简单候选评分迁入新 resolver。
- 已在 debug trace 中补充 `merchant_source` 和 `merchant_confidence`。
- 已扩展 `scripts/OfflineRegression.swift`，新增 `MerchantResolver` 独立回归，并保留既有 `LedgerTextInterpreterCore` 商户样例。
- 本步未改 `ReceiptParser` 特殊平台规则、`SmartReceiptParser` LLM 职责、UI、SQLite schema 或用户可见入账流程。

GOAL-1608C 实施结果：

- 已新增 `CategoryResolver` 和 `CategoryResolutionResult`，全部位于 `AutoLedgerCore`，仅依赖 `Foundation`。
- 已将 `LedgerTextInterpreterCore` 的分类推断入口切换到 `CategoryResolver`，并保留 `LedgerTextInterpreterCore.inferCategory(from:)` 作为兼容门面。
- 已将内置商户关键词分类映射从解释器迁出，并在 debug trace 中补充 `category_source`。
- 已扩展 `scripts/OfflineRegression.swift`，新增 `CategoryResolver` 独立回归，并保留既有 `LedgerTextInterpreterCore` 分类样例。
- 本步未改 `ReceiptParser` 特殊平台规则、`SmartReceiptParser` LLM 职责、UI、SQLite schema 或用户可见入账流程。

GOAL-1608D 实施结果：

- 已新增 `SmartReceiptMergePolicy`、`ReceiptAISuggestion` 和 `SmartReceiptMergeOutcome`，全部位于 `AutoLedgerCore`，仅依赖 `Foundation`。
- 已将 `SmartReceiptParser` 的高置信 LLM 路径从“直接采用 LLM 结果”改为“规则结果先锁定金额，LLM 只做商户 / 分类等字段增强”。
- 当规则解析已有金额时，AI / LLM 候选金额不会覆盖规则金额；当规则解析不可用且 AI 候选高置信、无需确认时，仍可作为增强 fallback 输出候选。
- 已扩展 `scripts/OfflineRegression.swift`，新增 `SmartReceiptMergePolicy` 独立回归，覆盖“AI 给出错误金额但规则金额正确时必须保留规则金额”的契约。
- 本步未改 UI、SQLite schema、Bundle ID、signing、entitlements、CloudKit 配置、`ReceiptParser` 特殊平台规则或用户可见入账流程。

GOAL-1608E 实施结果：

- 已针对真机调试样本修正支付宝支付成功页商户选择：当 OCR 文本中同时存在真实商户、优惠立减、奖励领取、付款方式和推荐红包时，商户不再由简单文本顺序决定。
- `ReceiptParser` 的支付宝支付成功页专用路径新增优惠 / 营销文案过滤，避免“碰友日立减”等折扣行成为商户。
- `AutoLedgerCore` 的 `RuleMerchantExtractor` / `MerchantResolver` 新增 UI / 营销噪声过滤与门店形态加分：过滤 `回首页`、`付款方式`、`立减`、`红包`、`森林能量`、`葵花籽`、`待领取` 等候选，并提高便利店、门店括号、餐厅、咖啡、market / store 等真实商户形态优先级。
- 已扩展 `scripts/OfflineRegression.swift`，新增 Core resolver 与 App 层 `ReceiptParser` 双层回归，覆盖“易择便利（陈塘科创园店）”优先于“碰友日立减”，且金额保持 `¥14.32`。
- 本步未改 UI、SQLite schema、外部 API、LLM 合并策略、Bundle ID、signing、entitlements 或 CloudKit 配置。

GOAL-1608F 实施结果：

- 已将地铁 / 公交储值卡规则前置到 `LedgerTextInterpreterCore` 和 App 层 `ReceiptParser` 的通用解析之前。
- 规则锚定 `地铁：` / `公交：` 标签，金额优先取同一行或后续三行内的 `CN¥` / `CNY` / `RMB` / `¥` 独立金额，路线优先取后续非金额路线行。
- 输出商户形态为 `地铁：示例站A→示例站B` 或 `公交：示例站A→示例站B`，分类固定为 `transport`，避免城市卡名称、余额行、推荐 / 评论 / 社媒噪声抢占商户或金额。
- 已扩展 `scripts/OfflineRegression.swift`，新增脱敏样例覆盖“城市卡 + 地铁金额 + 路线 + 社媒噪声”场景；不保存真实路线、截图或 OCR 原文。
- 本步未改 UI、SQLite schema、外部 API、LLM 合并策略、Bundle ID、signing、entitlements 或 CloudKit 配置。

GOAL-1608G 实施结果：

- 已将 App 入口 `LedgerTextInterpreter` 对 `LedgerTextInterpreterCore` 返回的 `transit_stored_value` 高确定性结果做硬前置：命中地铁 / 公交储值卡规则时直接返回规则账单，不再进入 DeepSeek / Qwen / OpenAI 等外部辅助识别分支。
- 新增通知中心样式脱敏回归，覆盖“储值消费成功通知 + 城市卡 + 地铁：CN¥金额 + 路线行 + 步行 / 游戏 / 天气噪声”文本，输出商户保持 `地铁：示例站A→示例站B`、金额 `2.70`、分类 `transport`。
- 快捷指令调试记录补齐 `llmProvider` 与 `llmLatencyMs`，后续真实外部模型命中时不再只显示 `Provider: unknown`。
- 本步未改 UI、SQLite schema、外部 API key 存储、默认开关、Bundle ID、signing、entitlements 或 CloudKit 配置。

外部 API 接入规划：

- 在默认本地识别链路之外，于 `v1.5.1` 接入第一版“脱敏数据解析增强”接口。
- 外部大模型 API 只能在用户主动开启的独立开关下使用，默认关闭。
- 外部增强仅接收脱敏 / 最小化后的结构化文本，例如移除订单号、卡号尾号、设备信息、地址、手机号、完整 OCR 原文和图片。
- 外部模型职责仍限定为商户候选、账单类型提示和低置信度解释，不得直接决定最终金额，也不得绕过用户确认。
- 该能力作为 `v1.5.1` 内部试点能力接入，可评估为后续低成本 Pro 功能；若 API 成本、隐私文案或稳定性未达标，可以在发布前保持隐藏 / Debug 开关，不阻塞主链路发布。

GOAL-1609A 实施结果：

- 已新增 `ExternalReceiptAssistPayload` 和 `ExternalReceiptAssistPayloadBuilder`，全部位于 `AutoLedgerCore`，仅依赖 `Foundation`。
- 已实现第一版外部辅助识别脱敏 payload：保留商户候选文本和短金额上下文，同时脱敏订单号 / 商户单号 / 交易参考号、卡尾号、手机号、地址行、样本文件 ID 和超长编号。
- 已扩展 `scripts/OfflineRegression.swift`，新增脱敏 payload 独立回归，确保敏感形态不会进入外部辅助请求文本。
- 本步未接入网络请求、真实外部 API provider、API key、UI 开关、`SmartReceiptParser` 主链路或日志输出。

GOAL-1609B 实施结果：

- 已新增 `ExternalReceiptAssistConfiguration`、`ExternalReceiptAssistGate`、`ExternalReceiptAssistGateDecision` 和 `ExternalReceiptAssistBlockReason`，全部位于 `AutoLedgerCore`，用于统一判断外部辅助请求是否允许发起。
- 默认关闭：`ExternalReceiptAssistGate` 在 `isEnabled == false` 时直接阻断请求；缺 endpoint、endpoint 非法、缺 API key、payload 为空也会阻断。
- 已新增 App 层 `ExternalReceiptAssistClient` skeleton，负责把已脱敏 payload 以 JSON POST 给运行时配置的 endpoint，并以 `Authorization: Bearer <apiKey>` 传入运行时 API key。
- 本步不在源码、文档、测试样例或仓库中写入真实 API key；不接 UI；不接 `SmartReceiptParser` 主链路；不开启默认外部调用。
- 若未来接入，需要单独的隐私说明、开关 UI、失败降级、本地优先兜底、日志脱敏和回归样例，不得影响离线默认体验。

GOAL-1609C 实施结果：

- 已新增 `ExternalReceiptAssistSuggestionMapper`，将外部辅助结果映射为 `ReceiptAISuggestion`，只提供商户 / 分类 / 置信度候选，不提供金额。
- `SmartReceiptParser` 已接入默认关闭的外部辅助解析路径：仅当用户设置开启、endpoint 合法且运行时存在 API key 时，才会发送脱敏 payload。
- 外部辅助候选通过 `SmartReceiptMergePolicy` 与本地规则结果合并，规则金额继续优先；外部结果只能补充商户和分类，不覆盖本地金额。
- `LedgerTextInterpreter` 与 `QuickLedgerIntent` 已接入外部辅助分支；开关关闭、配置缺失、网络失败或外部响应不可用时，会完整降级回本地规则 / 既有本地增强链路。
- API key 仅从运行时环境变量 `AUTOLEDGER_EXTERNAL_RECEIPT_ASSIST_API_KEY` 读取，不进入源码、文档样例、回归样例、日志或仓库。
- 本步未接 UI 开关、Keychain 存储、真实 provider 配置、隐私说明页、App Store 文案或多 provider 选择。

GOAL-1609D 实施结果：

- 已新增 `ExternalReceiptAssistSettingsView`，在设置页中提供独立“外部辅助识别”入口，避免与端侧 AI 模型设置混在一起。
- 外部辅助识别继续默认关闭；用户需要显式开启开关、填写 endpoint 并保存 API key 后才可能触发外部请求。
- API key 已改为优先从 Keychain 读取，仍保留运行时环境变量作为开发调试 fallback；源码、文档样例、日志和回归样例中不写真实 key。
- endpoint 仍只保存 URL 字符串，不保存请求 payload、响应内容、截图、完整 OCR 原文或账单数据。
- 已补简体中文 / 繁体中文 / 英文本地化文案，UI 明确提示只发送最小化脱敏 payload，金额仍由本地规则决定。
- 本步未配置真实 provider、未接 Pro / IAP gate、未补正式隐私政策页面或 App Store 文案，仍需要真机手动验证设置页交互。

GOAL-1609E 实施结果：

- 已新增 `ExternalReceiptAssistProvider`，在 `AutoLedgerCore` 固定第一版 provider preset：`DeepSeek`、`Qwen`、`OpenAI` 与 `Custom`。
- 已新增 `ExternalReceiptAssistOpenAICompatibleCodec`，外部辅助识别请求改为 OpenAI-compatible `chat/completions` 格式，返回值从 `choices[0].message.content` 中解码结构化 JSON。
- DeepSeek 默认 endpoint 为 `https://api.deepseek.com/chat/completions`，Qwen 默认 endpoint 为 `https://dashscope.aliyuncs.com/compatible-mode/v1/chat/completions`，OpenAI 默认 endpoint 为 `https://api.openai.com/v1/chat/completions`；模型字段保留为用户可编辑，DeepSeek 默认 `deepseek-v4-flash`。
- 设置页已加入 provider 下拉选择、模型输入和 endpoint 输入；选择 provider 会自动填充默认 endpoint / model，用户仍可手动覆盖。
- 真实请求继续使用 Keychain 中用户自行保存的 API key，以 `Authorization: Bearer <apiKey>` 发送；源码、文档和回归样例不写入真实 key。
- 外部模型 prompt 明确限定只返回 `merchantCandidates`、`categoryHint`、`confidence`、`explanation`，不得返回或决定金额、订单号、卡号、地址、个人信息等字段。
- 已扩展 `scripts/OfflineRegression.swift`，覆盖 provider 默认 endpoint / model、OpenAI-compatible request 生成和 chat completion 响应解码。
- 本步不默认开启外部辅助识别、不接 Pro / IAP gate、不修改本地规则主链路、SQLite schema、Bundle ID、signing、entitlements 或 CloudKit 配置。

GOAL-1609F 实施结果：

- 已在 `ExternalReceiptAssistSettingsView` 增加“测试 Provider”按钮，用于真机填写 API key 后验证当前 provider / model / endpoint 是否可以返回可解析商户候选。
- 测试请求只发送虚构样例 `Demo Coffee / 支付金额 12.34`，不读取当前账本、不读取 OCR、不读取图片、不发送真实账单数据。
- 测试逻辑会复用真实 `ExternalReceiptAssistClient`、Keychain API key、OpenAI-compatible request codec 和 response decoder，因此可以提前验证 DeepSeek / Qwen / OpenAI 配置是否通。
- 测试成功、无候选、缺少 API key 和失败状态均有三语用户提示；失败提示不输出 API key、响应原文或用户数据。
- 本步不默认开启外部辅助识别、不接 Pro / IAP gate、不修改本地规则主链路、SQLite schema、Bundle ID、signing、entitlements 或 CloudKit 配置。

GOAL-1609G 真机验证结果：

- 用户已在真机完成外部辅助识别基本测试，并确认可以看到真实 API 调用。
- Provider / model / endpoint / Keychain API key / OpenAI-compatible request / response decode 主链路已具备真机闭环证据。
- 本次记录不保存 API key、provider 响应原文、请求日志、真实账单 OCR 或截图内容。
- `GOAL-1609` 功能侧可视为完成；发布侧仍需在最终 smoke 前复核隐私文案、App Store 审核说明和是否需要 Pro / IAP gate。

第一版接入边界：

- 新增 App 层外部解析 Adapter，不进入 `AutoLedgerCore`，Core 只接收脱敏后的候选结果。
- API key 不进入仓库，不写入源码，不进入截图、日志或回归样例。
- 请求只发送脱敏后的候选行、金额附近上下文摘要、支付渠道类型、OCR 来源类型和本地规则候选，不发送图片二进制和完整 OCR 原文。
- 返回值只允许转成 `MerchantCandidate` / `Category hint` / `reason` / `confidence`，不能直接生成正式 `ImportedReceipt`。
- UI 上使用独立开关，例如“增强识别（脱敏云端辅助）”，默认关闭，并明确可能使用外部模型服务。
- 低置信度或规则冲突时必须进入用户确认，不得静默入账。
- 网络失败、API 失败、配额耗尽或开关关闭时，本地规则链路必须完整可用。

重点覆盖场景：

- 微信支付独立负数金额行。
- 支付宝支付成功页中付款方式附近的真实商户。
- 小票 `TOTAL RM` / `GRAND TOTAL` / `AMOUNT DUE` / `BALANCE DUE` / `TOTAL SALES`。
- 优惠、红包、折扣、找零、现金支付、税额、数量、订单号、注册号、时间和商品编号排除。
- 多商品价格场景不误取第一条商品价格。
- 商户黑名单不把 `微信支付`、`支付宝`、`银联`、`Apple Pay`、`TAX INVOICE`、`RECEIPT`、`CASH SALE`、`支付成功` 等作为最终商户。
- 商户候选排序不因真实商户出现在支付渠道之后、金额之后或页面底部就被低优先级噪声覆盖。
- 脱敏外部增强开关关闭时，识别链路必须完全保持本地可用。
- 外部 API 开启时，金额仍以本地 `PaymentAmountExtractor` 为准，外部结果只能补充商户候选和解释。

验收：

- `bash scripts/run_offline_regression.sh` 通过。
- `bash scripts/run_golden_regression.sh` 通过。
- 新增核心解析类型不依赖平台框架。
- `LedgerTextInterpreterCore` 公共 API 不破坏。
- `ReceiptParser` / `SmartReceiptParser` 外部调用方式不破坏。
- 金额提取器和商户 resolver 可单独测试。
- debug trace 能看到金额候选、商户候选和 resolver 选中规则的简短信息。
- 外部大模型增强必须有单独开关、脱敏边界、失败降级和日志脱敏，不得作为默认识别路径。

## 5. 非目标

`v1.5.1` 暂不包含：

- 多账本正式模型
- `Ledger` / `ledger_id` 迁移
- 账本切换、账本归档、默认账本管理 UI
- 预算、投资、报销、协作账本
- 服务端账户系统
- 用高版本 availability 包住整个主链路来“假装支持低系统”

多账本继续顺延到 `v1.6.0+` 再重启评审。

## 6. 推荐实施顺序

### Phase A：版本与平台边界收口

- `v1.5.0` 标记为实现基线
- `v1.5.1` 接管当前开发线
- 把多账本从当前执行队列中继续排除

### Phase B：最低系统下调准备

- 审核 `AutoLedgerCore`
- 判断是否必须拆 `CoreBase`
- 列出每个 target 的 deployment target 修改清单
- 标出 `Apple Foundation Models` / `ControlWidget` 的 feature gate 边界

### Phase C：识别链路 Core 化重构

- 先落 `PaymentAmountExtractor` 与金额候选模型
- 再落 `RuleMerchantExtractor` / `MerchantResolver` / `MerchantNormalizer`
- 接入 `LedgerTextInterpreterCore` 并补离线回归
- 最后收敛 `ReceiptParser` / `SmartReceiptParser` 的金额和商户职责边界

### Phase D：脱敏外部 API 辅助识别试点

- 先定义脱敏请求 / 响应 schema
- 再新增 App 层外部解析 Adapter 与独立开关
- 将外部返回结果转成 `MerchantCandidate`，通过 `MerchantResolver` 合并
- 补充失败降级、日志脱敏和低置信度确认页边界

### Phase D2：账单编辑保存链路稳定性

编辑保存链路必须拆成清晰的同步边界：

1. `TransactionEditorView` 只维护表单草稿，保存前使用 `LedgerAmountInputParser` 统一解析金额。
2. 点击保存后由编辑页调用 `onSave(updated, refreshSameMerchantCategory)`，返回 `Bool` 表示这次保存是否真正成功。
3. `LedgerStore.updateTransaction` 先定位原账单，再写入 SQLite；写库成功后才刷新内存中的 `transactions`、排序、刷新 Widget、触发旧备份和 iCloud 增量推送。
4. 写库失败、找不到账单或输入无效时，编辑页不能直接关闭，必须留在当前表单并显示错误。
5. 普通手动账单编辑不学习商户别名；只有高置信识别入账结果被用户修正商户时，才允许提示用户是否保存别名。
6. 同商户分类批量刷新只在当前账单保存成功之后执行，避免“页面看起来保存了，但落盘没成功”的不一致。
7. 商户别名学习必须由用户确认；选择“不保存别名”时只保存当前账单，不写入全局别名规则。

该链路用于解决真机上出现的编辑卡死、商户名输入后保存置灰、点击保存不生效或保存成其他内容等严重问题。后续若仍出现卡顿，应继续沿 `TransactionEditorView -> LedgerStore.updateTransaction -> SQLiteTransactionStore.update -> Widget / Backup / CloudKit` 分段定位。

GOAL-1610C 真机验证结果：

- 用户已在真机完成账单编辑保存链路基本测试，确认编辑保存没有继续出现阻断问题。
- 金额输入、商户编辑、保存按钮状态、保存后展示一致性和商户别名确认式学习功能侧可视为通过。
- 本次记录不保存真实账单内容、商户名、金额或截图。
- `GOAL-1610` 功能侧可视为完成；若后续真实使用仍出现卡顿或保存不一致，再按 `TransactionEditorView -> LedgerStore.updateTransaction -> SQLiteTransactionStore.update -> Widget / Backup / CloudKit` 分段继续定位。

GOAL-1611 实施结果：

- 外部辅助识别调试记录补齐模型链路信息：`SmartReceiptParser` 的 trace 改为通用 provider id / display name，DeepSeek / Qwen / OpenAI / Custom 外部辅助命中后会记录 provider、model、脱敏请求摘要、商户候选、分类提示、置信度、解释和耗时。
- 调试页卡片、单条复制、整页导出和反馈 trace 均会展示模型来源与规则兜底信息，便于判断本次识别是否走了外部模型；日志仍不写 API key、图片、完整未脱敏原始 OCR 或真实 secret。
- iCloud 同步拉取写入本地 SQLite 改为批量 apply：先一次性加载本地 sync records 建索引，再按远端记录应用新增 / 更新 / 删除 / 冲突，避免旧逻辑每处理一条远端记录都全表读取一次导致 UI 无响应卡顿。
- 新增离线回归覆盖批量远端同步 summary 的新增 / 更新 / 删除统计，已验证 `git diff --check`、`run_offline_regression.sh` 与主 App iOS generic build 通过。

### Phase E：tvOS 第一版代码

- 先做总览页骨架
- 再补齐分类 / 趋势 / 摘要
- 接稳定只读数据入口

### Phase F：visionOS 第一版代码

- 先做 `WindowGroup` 骨架
- 再补四区展示
- 最后评估是否需要空间增强层

### Phase G：全平台截图与最终 smoke

- 补齐 tvOS / visionOS 截图资产
- 统一跑全平台 smoke
- 执行 Xcode Cloud / ASC 最终收口

## 7. GOAL 队列（规划稿）

| GOAL | 状态 | 目标 | 说明 |
|------|------|------|------|
| GOAL-1600 | 已完成 | `v1.5.1` 版本接管与收口策略更新 | 已将 `v1.5.0` 变为实现基线，明确 smoke 延后到 `v1.5.1` |
| GOAL-1601 | 已完成 | `AutoLedgerCore` 平台依赖审计与 `CoreBase` 拆分方案 | 已确认 Core 的主要阻塞是 `ClipboardImportIntent` 引入 `AppIntents`；先迁出 Intent Adapter，再下调 Core package，暂不大拆 `CoreBase` |
| GOAL-1602 | 已完成 | 最低系统需求下调实施 | 已完成主 App / Watch / tvOS / visionOS deployment target 调整、Core / RealityKitContent / CocoaPods baseline 下调，以及 Apple FM feature gate 收口 |
| GOAL-1603 | 规划中 | tvOS 第一版只读看板落代码 | 基于 `GOAL-1580 / 1581` 文档实现 |
| GOAL-1604 | 规划中 | visionOS 第一版展示落代码 | 基于 `GOAL-1582 / 1583` 文档实现 |
| GOAL-1605 | 规划中 | 全平台截图与商店资产补齐 | 在现有 iPhone / Watch / iPad / Mac 基础上补 tvOS / visionOS |
| GOAL-1606 | 规划中 | `v1.5.1` 最终 smoke / Xcode Cloud / ASC 收口 | 全平台内容完成后统一执行 |
| GOAL-1607 | 已完成 | Shortcuts JSON 账单导入与确认页 | 新增结构化 JSON App Intent、置信度分流、确认页和离线回归覆盖 |
| GOAL-1608 | 已完成 | 账单 OCR / 文本识别链路 Core 化重构 | 已完成 1608A-G：抽取金额提取器、商户候选模型、商户 normalizer / resolver、分类 resolver、SmartParser 合并策略，并修正支付成功页、地铁 / 公交储值卡和通知中心地铁样式的规则优先级 |
| GOAL-1609 | 已完成 | 脱敏外部 API 辅助识别试点 | 已完成 1609A-G：脱敏 payload、默认关闭门控、主解析分支接入、失败降级、设置入口、Keychain key 存储、DeepSeek / Qwen / OpenAI provider preset、Provider 测试入口和真机 API 调用验证；发布前仍需复核隐私政策 / App Store 文案和 Pro gate 取舍 |
| GOAL-1610 | 已完成 | 账单编辑保存链路稳定性 | 已完成 1610A-C：金额输入解析、编辑保存显式返回成功 / 失败、写库失败不关闭编辑页、商户别名学习改为用户确认，并已通过真机基本测试 |
| GOAL-1611 | 已完成 | 外部辅助调试可观测性与 iCloud 同步卡顿修复 | 外部模型命中会进入调试记录与导出；CloudKit 拉取写入本地从逐条全表读取改为批量索引 apply，降低同步时 UI 无响应风险 |

## 8. 版本门禁

`v1.5.1` 结束时，最终输出必须能回答以下问题：

1. 每个 target 的最终 deployment target 是多少
2. 为降版本改了哪些文件
3. `iOS 17` / `iOS 18` / `iOS 26` 的模拟器或 generic build 结果如何
4. `ControlWidgetExtension` 是否仍稳定留在 `iOS 18`
5. Apple Foundation Models / SmartReceiptParser 的 `iOS 26+` 增强能力是否仍完整保留
6. tvOS / visionOS 是否已有第一版可构建、可演示的产品代码
7. `v1.5.0` 遗留 smoke 是否已全部被 `v1.5.1` 吸收并完成
8. 识别链路是否已完成 Core 化第一阶段重构，且金额不再由 LLM 覆盖规则结果
9. 外部 API 辅助识别是否满足默认关闭、脱敏、失败降级、日志不泄露和本地链路完整可用

### 8.1 GOAL-1602 验证结果

- PASS：`git diff --check`
- PASS：`bash scripts/run_offline_regression.sh`
- PASS：`xcodebuild -workspace AutoLedger/AutoLedger.xcworkspace -scheme AutoLedger -configuration Debug -destination 'generic/platform=iOS' build`
- PASS：`xcodebuild -workspace AutoLedger/AutoLedger.xcworkspace -scheme AutoLedger -configuration Debug -destination 'platform=macOS,variant=Mac Catalyst' build`

### 8.2 GOAL-1608A 验证结果

- PASS：`git diff --check`
- PASS：`bash scripts/run_offline_regression.sh`
- PASS：`xcodebuild -workspace AutoLedger/AutoLedger.xcworkspace -scheme AutoLedger -configuration Debug -destination 'generic/platform=iOS' build`
- 已知 warning：`AppFormatters` 的 `nonisolated(unsafe)` Sendable 提示为既有 warning，本步未处理。

### 8.3 GOAL-1608B 验证结果

- PASS：`git diff --check`
- PASS：`bash scripts/run_offline_regression.sh`
- PASS：`xcodebuild -workspace AutoLedger/AutoLedger.xcworkspace -scheme AutoLedger -configuration Debug -destination 'generic/platform=iOS' build`
- 已知 warning：`AppFormatters` 的 `nonisolated(unsafe)` Sendable 提示为既有 warning，本步未处理。

### 8.4 GOAL-1608C 验证结果

- PASS：`git diff --check`
- PASS：`bash scripts/run_offline_regression.sh`
- PASS：`xcodebuild -workspace AutoLedger/AutoLedger.xcworkspace -scheme AutoLedger -configuration Debug -destination 'generic/platform=iOS' build`
- 已知 warning：`AppFormatters` 的 `nonisolated(unsafe)` Sendable 提示为既有 warning，本步未处理。
- PASS：`xcodebuild -workspace AutoLedger/AutoLedger.xcworkspace -scheme 'AutoLedgerWatch Watch App' -configuration Debug -destination 'generic/platform=watchOS' build`
- PASS：`xcodebuild -workspace AutoLedger/AutoLedger.xcworkspace -scheme ControlWidgetExtension -configuration Debug -destination 'generic/platform=iOS' build`
- PASS：`xcodebuild -workspace AutoLedger/AutoLedger.xcworkspace -scheme AutoLedgerTV -configuration Debug -destination 'generic/platform=tvOS' build`
- PASS：`xcodebuild -workspace AutoLedger/AutoLedger.xcworkspace -scheme AutoLedgerVision -configuration Debug -destination 'generic/platform=visionOS' build`

### 8.5 GOAL-1608D 验证结果

- RED：新增 `SmartReceiptMergePolicy` 离线回归后，`bash scripts/run_offline_regression.sh` 因找不到 `ReceiptAISuggestion` / `SmartReceiptMergePolicy` 失败。
- PASS：`git diff --check`
- PASS：`bash scripts/run_offline_regression.sh`
- PASS：`xcodebuild -workspace AutoLedger/AutoLedger.xcworkspace -scheme AutoLedger -configuration Debug -destination 'generic/platform=iOS' build`
- 已知 warning：`AppFormatters` 的 `nonisolated(unsafe)` Sendable 提示为既有 warning，本步未处理。

### 8.6 GOAL-1609A 验证结果

- RED：新增 `ExternalReceiptAssistPayload` 离线回归后，`bash scripts/run_offline_regression.sh` 因找不到 `ExternalReceiptAssistPayloadBuilder` 失败。
- PASS：`git diff --check`
- PASS：`bash scripts/run_offline_regression.sh`
- PASS：`xcodebuild -workspace AutoLedger/AutoLedger.xcworkspace -scheme AutoLedger -configuration Debug -destination 'generic/platform=iOS' build`
- 已知 warning：`AppFormatters` 的 `nonisolated(unsafe)` Sendable 提示为既有 warning，本步未处理。

### 8.7 GOAL-1609B 验证结果

- RED：新增 `ExternalReceiptAssistGate` 离线回归后，`bash scripts/run_offline_regression.sh` 因找不到 `ExternalReceiptAssistGate` / `ExternalReceiptAssistConfiguration` 失败。
- PASS：`git diff --check`
- PASS：`bash scripts/run_offline_regression.sh`
- PASS：`xcodebuild -workspace AutoLedger/AutoLedger.xcworkspace -scheme AutoLedger -configuration Debug -destination 'generic/platform=iOS' build`
- 已知 warning：`AppFormatters` 的 `nonisolated(unsafe)` Sendable 提示为既有 warning，本步未处理。

### 8.8 GOAL-1609C 验证结果

- RED：新增 `ExternalReceiptAssistSuggestionMapper` 离线回归后，`bash scripts/run_offline_regression.sh` 因找不到 mapper 失败。
- PASS：`git diff --check`
- PASS：`bash scripts/run_offline_regression.sh`
- PASS：`xcodebuild -quiet -workspace AutoLedger/AutoLedger.xcworkspace -scheme AutoLedger -configuration Debug -destination 'generic/platform=iOS' build`
- 已知 warning：`AppFormatters` 的 `nonisolated(unsafe)` Sendable 提示为既有 warning，本步未处理。

### 8.9 GOAL-1609D 验证结果

- PASS：`git diff --check`
- PASS：`bash scripts/run_offline_regression.sh`
- PASS：`xcodebuild -quiet -workspace AutoLedger/AutoLedger.xcworkspace -scheme AutoLedger -configuration Debug -destination 'generic/platform=iOS' build`
- 已知 warning：AppIntents logger、Gemma `LlmInference` deprecation、`SmartReceiptParser` no-async-await 和部分 actor isolation 提示均为当前工程既有 warning，本步未处理。
- 人工验证：真机设置页和外部 provider API 调用已由用户基本验证通过。

### 8.10 GOAL-1609E / 1609F / 1609G 验证结果

- PASS：`git diff --check`
- PASS：`bash scripts/run_offline_regression.sh`
- PASS：`xcodebuild -quiet -workspace AutoLedger/AutoLedger.xcworkspace -scheme AutoLedger -configuration Debug -destination 'generic/platform=iOS' build`
- 人工验证：用户已在真机看到外部 provider API 调用，说明 provider / model / endpoint / Keychain key / request codec 主链路已可运行。
- 保留发布前检查：隐私政策 / App Store 审核说明 / Pro gate 取舍，不作为 `GOAL-1609` 功能开发阻塞项。

### 8.11 GOAL-1610A 验证结果

- PASS：`git diff --check`
- PASS：`bash scripts/run_offline_regression.sh`
- PASS：`xcodebuild -quiet -workspace AutoLedger/AutoLedger.xcworkspace -scheme AutoLedger -configuration Debug -destination 'generic/platform=iOS' build`
- 已知 warning：AppIntents logger、Gemma `LlmInference` deprecation 等为当前工程既有 warning，本步未处理。
- 人工验证：用户已在真机确认编辑保存基本没有问题。

### 8.12 GOAL-1610B 验证结果

- PASS：`git diff --check`
- PASS：`bash scripts/run_offline_regression.sh`
- PASS：`xcodebuild -quiet -workspace AutoLedger/AutoLedger.xcworkspace -scheme AutoLedger -configuration Debug -destination 'generic/platform=iOS' build`
- 已知 warning：AppIntents logger、Gemma `LlmInference` deprecation 等为当前工程既有 warning，本步未处理。
- 人工验证：用户已在真机确认编辑保存基本没有问题；商户别名确认式学习保留为后续真实使用继续观察项。

### 8.13 GOAL-1608E 验证结果

- PASS：`git diff --check`
- PASS：`bash scripts/run_offline_regression.sh`
- PASS：`xcodebuild -quiet -workspace AutoLedger/AutoLedger.xcworkspace -scheme AutoLedger -configuration Debug -destination 'generic/platform=iOS' build`
- 已知 warning：`AppFormatters` / AppIntents logger / Gemma `LlmInference` deprecation 等为当前工程既有 warning，本步未处理。
- 待人工验证：在真机上用包含“易择便利（陈塘科创园店）/ 碰友日立减 / 付款方式 / 红包推荐”的支付成功截图重新识别，确认商户为真实门店、金额为 `¥14.32`，并确认多笔账单提示仍符合预期。

### 8.14 GOAL-1611 验证结果

- PASS：`git diff --check`
- PASS：`bash scripts/run_offline_regression.sh`
- PASS：`xcodebuild -workspace AutoLedger/AutoLedger.xcworkspace -scheme AutoLedger -configuration Debug -destination 'generic/platform=iOS' build`
- 已知 warning：`AppFormatters` formatter isolation warning 为当前工程既有 warning，本步未处理。
- 待人工验证：TestFlight / 真机开启外部辅助识别后复制单条调试记录，确认能看到 provider、model、脱敏请求摘要、外部模型候选、置信度和耗时；iCloud 同步大量账单时观察 UI 无响应是否明显缓解。

### 8.15 GOAL-1608G 验证结果

- PASS：`git diff --check`
- PASS：`bash scripts/run_offline_regression.sh`
- PASS：`xcodebuild -workspace AutoLedger/AutoLedger.xcworkspace -scheme AutoLedger -configuration Debug -destination 'generic/platform=iOS' build`
- 待人工验证：真机使用通知中心“城市卡 / 地铁：CN¥金额 / 路线行”样式截图或快捷指令文本识别时，确认调试记录显示纯规则解析，不再出现 DeepSeek / Qwen / OpenAI 模型链路；商户为 `地铁：起点→终点`，分类为出行。

已知保留警告：

- Mac Catalyst 仍提示 MediaPipe xcframework 没有 Catalyst slice；这是既有依赖形态，当前 Catalyst fallback 保持构建通过。

## 9. 当前结论

`v1.5.1` 现在应被视为：

- 当前开发主线
- `v1.5.0` 遗留内容的统一收口版本
- 全平台内容补齐版本
- 最低系统需求优化版本
- 账单 OCR / 文本识别链路重构版本
- 脱敏外部大模型辅助识别试点版本

多账本继续 delay，不作为本轮 blocker。
